# Tasks — crosswalk_app

Owner: planner (see AGENTS.md). Status values: `todo` / `in-progress` / `review` / `done` / `paused` (deferred by explicit user decision; not to be picked up until the user resumes it).
Derived from gaps found in code inspection (2026-07-16). Each row cites the motivating evidence.
Tasks marked ⛔BLOCKED depend on an Open Question in `docs/PRD.md` and must not start until answered.

## A. Blocked on decisions (resolve Open Questions first)
| ID | Task | Priority | Depends on | Acceptance Criteria | Status |
|---|---|---|---|---|---|
| T1 | Define & document model accuracy target (recall for left/right) + measure on test set | P0 | Open Q #3 answered (2026-07-17) | Numeric target recorded in PRD; measurable on test set. UNBLOCKED (2026-07-17): Open Q #3 answered — deviation (left/right) recall target ≥ 90% / miss rate ≤ 10%, now recorded in PRD Non-Functional Requirements + Risks. Decision portion is DONE; remaining implementer work = actually measuring the shipped model against a labeled test set to confirm it meets ≥ 90% recall. NOTE: front (normal) false-positive tolerance was NOT specified by the user (still undefined). CAVEAT (Open Q #10): current model may be an intermediate checkpoint on insufficient data; a definitive recall validation should be run against the final retrained model. **MEASURED (2026-07-17): TARGET NOT MET — follow-up required (retraining, per Open Q #10).** Eval script `train/eval_model.py` (new; run `python train/eval_model.py`) applied the app-identical preprocessing (224×224 nearest-resize, ImageNet norm, softmax, thresholds front=0.85 / left,right=0.55) to the shipped `model/crosswalk_model.onnx` over `train/data_prepared/test/{front,left,right}`. Sample sizes: front=50, left=6, right=3. Confusion matrix (row=true, col=pred, None=below-threshold no-decision): front→[front 35, left 4, right 3, None 8]; left→[front 0, left 5, right 1, None 0]; right→[front 0, left 0, right 3, None 0]. Results: **left recall = 83.3% (5/6), 1 miss → FAILS the ≥90% target**; left precision = 55.6% (5/9). right recall = 100% (3/3), 0 miss → nominally meets target but n=3 is statistically meaningless (a single wrong image would drop it to 66.7%). front recall = 70.0% (35/50), precision = 100% (35/35). **SAFETY FINDING: front false-alarm rate = 14% (7/50)** — 7 of 50 truly-straight frames were misclassified as a deviation (→left 4, →right 3), i.e. the app would issue a spurious "off-course" warning while the user is walking straight; the other 8 front misses fell below threshold (no-decision, no warning — lower safety concern). **STATISTICAL CAVEAT (recorded, but does NOT soften the miss): the deviation sample is tiny (left n=6, right n=3) so the confidence interval on every figure above is very wide; both facts hold simultaneously — left was MEASURED below target AND the measurement's own reliability is low. This does not turn the miss into a pass.** Reproducibility: `classifier.dart`'s `img.copyResize()` uses image-pkg v4.8.0 default `Interpolation.nearest` (confirmed from library source), reproduced in Python via PIL `Image.NEAREST`. Status kept `in-progress` (NOT `done`): the measurement half of the acceptance criterion is complete, but the shipped model does not meet the ≥90% deviation-recall target, so the task's real objective (a model that meets the target) is unmet and follow-up (retrain on more data → re-measure against the final checkpoint, per Open Q #10) is required. NOTE (unchanged): front false-positive tolerance still undefined by the user — the 14% false-alarm finding gives fresh reason to define one. **POSTURE NOTE (2026-07-18, Open Q #15 / Architecture §16.6 Open Q D, T35):** when the retrain dataset is (re)captured, collection posture can follow the user's stated direction — 카메라를 "정면에 가깝게, 약간 하향"(수평에 가깝되 약간 아래로) 향하게 촬영. CAVEAT: this is only a **qualitative direction** — the user answered with an estimate ("~것 같아"), and the exact tilt angle (도° 단위) and mount height are still undecided (Open Q #15 ANSWERED direction-only). Precisely reproducing the real capture angle still requires user confirmation or physical measurement; do not treat "약간 하향" as a fixed numeric spec. **REMEASURED (2026-08-02, 확장 데이터셋 재학습 후):** `image/`에 신규 359장이 추가되어(front 269→320, left 55→114, right 32→110, none 36→207, 총 392→751장) 4-클래스 모델을 재학습함(PR #54, squash-merge, merge commit `9dc1be1`, CI run 30738314380 "64 tests passed"). 앱 실제 임계값(front=0.5 / left,right=0.55 / none=0.50) 기준 `python train/eval_model.py` 실측 — 테스트셋 표본 front=32, left=12, right=11, none=22 (이전 28/6/4/5에서 확대): left recall **100.0% (12/12)** (이전 50.0%, 3/6) → **T1 목표(≥90%) 충족**; right recall **81.8% (9/11)** (이전 50.0%, 2/4) → 목표 **미충족**; none recall 81.8% (18/22) (이전 40.0%, 2/5); front recall 75.0% (24/32) (이전 78.6%, 22/28). PyTorch 기준(임계값 미적용) accuracy 0.88→0.94, macro F1 0.87→0.93. **실패 방식 개선(안전 관련):** right 미탐 2건이 모두 임계값 미달 **무판정(None)** 이었음 — 직전 모델에서는 우측 이탈 이미지 2장을 확신도 약 0.70으로 "정상(front)"이라 적극 오판했으나 이번 측정에서는 그런 사례 0건, 즉 잘못된 안심 대신 침묵하는 방향으로 개선됨. **임계값 미변경 근거:** deviation 임계값을 0.55→0.45로 낮추는 시험을 실행한 결과 right recall은 81.8% 그대로인 채 미탐 1건이 무판정에서 **반대 방향(left) 오경보**로 바뀌고 front 헛경보가 2건→4건으로 증가 — 현행 임계값 유지가 타당함을 근거로 확인. **잔여 문제 (소프트닝 금지):** right recall 81.8%로 목표 미달이므로 T1 status는 계속 `in-progress`. 잔여 오탐 — 실제 front인데 left로 오판(헛경보) 2건(2/32=6.3%, 이전 10.7%), 실제 none인데 좌/우 이탈 경보 3건(3/22). 표본이 여전히 작아(left n=12, right n=11) 이미지 1~2장 판정이 뒤집히면 recall이 8~9%p 흔들림 — left 100%도 통계적 과신 금지. **REMEASURED (2026-08-21, 843장 데이터셋):** 데이터셋 843장(front 332 / left 127 / right 155 / none 229), 분할 80/10/10 → train 673 / val 82 / test 88. 적용 PR #61 — master `2864a73..60c072c`, develop `fd23324`, CI run `32456801252` "65 tests passed" + APK 빌드 성공(76.5MB). 모델 해시 `90b21f9fd3ce8ed720cdea9c31eb8d2e1d198e2dcc231a8e1160fc9e9ec3c41b`. 학습 스크립트 자체 test set(원시 argmax, threshold 미적용, n=88): front recall 0.91 / left 1.00 / none 1.00 / right 0.94, accuracy 0.95. **앱 실제 동작 기준**(`train/eval_model.py`, threshold front=0.5 / left,right=0.55 / none=0.50 — 이 세 값은 `crosswalk_app/lib/services/classifier.dart:33,38,39` 및 `train/eval_model.py:50-52`와 일치함을 2026-08-21 직접 대조 확인. 이전 두 블록(2026-08-02, 2026-08-21 최초 기록)이 front를 0.65로 잘못 적었으나 이는 서술 오류일 뿐이며, 실제 실행에는 두 파일 모두 2026-07-24 PR #50/#51 이후 줄곧 0.5가 쓰였으므로 **측정 수치 자체는 유효함**): front recall 0.765(26/34)·precision 1.000; left recall 0.929(13/14)·precision 0.867 → **목표 ≥90% 충족**; none recall 0.958(23/24)·precision 0.920; right recall 0.750(12/16)·precision 0.857 → **목표 미달**. **이전 측정(2026-08-02) 대비:** right 81.8%(9/11) → 75.0%(12/16)로 하락. 단, 두 측정 모두 표본이 n=11/16으로 작아 **이 차이가 실제 성능 저하인지 표본 노이즈인지 구분 불가** — 과장 금지. **안전 관련 발견 — front 오경보율 11.8%(4/34):** 직진(front) 프레임 4장이 편향으로 오판됨 (→right 2건: `20251119_131206_041_saved.jpg`, `20260808_000211.jpg`; →left 2건: `20251119_161750_001_saved.jpg`, `20251119_161750_015_saved.jpg`). 직진 중 허위 "이탈" 경고가 발생하는 케이스로, right 미검출보다 안전상 더 위험함. right 미검출 4건은 **전부 임계값 미달 무판정(None)** 이며 반대 방향 오판은 0건 — 실패 양상 자체는 안전한 쪽. **ONNX 익스포트:** T43 트랩이 이번에도 재현됨 — `train_model.py` 내장 익스포터가 IR10/opset18(모바일 비호환)로 덮어써서 `train/export_onnx.py` 재실행으로 IR7/opset12 재생성 후 적용; 커밋된 에셋 직접 검증 결과 ir_version=7, opset=12, output dims=[1,4]. **라벨 순서 검증:** 학습 로그 `class_to_idx {'front':0,'left':1,'none':2,'right':3}` = `crosswalk_app/lib/services/classifier.dart:28` `_labels`와 일치. status는 계속 `in-progress` (right 목표 미달이므로 done 아님). **사양 내/외 분리 재평가 (2026-08-21, 재학습 없음):** 2026-08-21 사용자가 실제 사용 카메라 각도를 **"정면에서 살짝 아래"** 로 확정함 — 이미 출시된 온보딩 문구(`crosswalk_app/lib/localization/app_strings.dart:279`)와 일치. 이에 따라 테스트셋 88장을 두 부류로 나눠 **현재 모델을 재학습 없이** 각각 평가함. **판정 기준:** 지평선·건물·차량·인도 등 배경 맥락이 프레임에 전혀 없고 노면만 100%면 **사양 외**(수직 하방 근접), 그 외는 **사양 내**. **판정 방법:** 테스트셋 88장을 컨택트 시트 3장(`train/sheets/test0~2.jpg`)으로 만들어 육안 판정 — 88장 중 14장(16%)이 사양 외. **평가 스크립트:** `train/eval_split_spec.py` (신규); 전처리·임계값·판정 로직은 `train/eval_model.py`와 동일. **결과 (형식: 전체 n=88 / 사양 내 n=74 / 사양 외 n=14):** front 0.765(26/34) / 0.828(24/29) / 0.400(2/5); left 0.929(13/14) / 0.889(8/9) / 1.000(5/5); none 0.958(23/24) / 1.000(23/23) / 0.000(0/1); right 0.750(12/16) / 0.846(11/13) / 0.333(1/3); front 오경보율 11.8%(4/34) / 10.3%(3/29) / 20.0%(1/5). **해석 (과장 금지):** (1) right 미달 원인의 상당 부분이 사양 외 프레임이었음 — 실사용 조건에서 75.0% → 84.6%. **다만 여전히 목표 90% 미달.** (2) **front 오경보는 사양 외 탓이 아님** — 11.8% → 10.3%로 거의 개선되지 않음. 사양 내에서도 29장 중 3장이 직진인데 편향 경고가 나감. 별개의 실제 문제이며 **T49**로 등록됨. (3) **표본이 더 작아짐** — left n=9, right n=13으로 목표 판정에 필요한 n≈35의 절반 미만. left 0.929→0.889 하락은 9장 중 1건 차이라 **의미를 두면 안 됨**. T1 상태는 `in-progress` 유지 (right 목표 미달). **신뢰구간 산출 (2026-08-21):** 위 "사양 내/외 분리 재평가" 결과에 Wilson 95% 신뢰구간을 계산해 병기함 — 사양 내 front 오경보 10.3% (3/29) → CI [3.6%, 26.4%]; 전체 front 오경보 11.8% (4/34) → CI [4.7%, 26.6%]; 사양 내 right recall 84.6% (11/13) → CI [57.8%, 95.7%]; 사양 내 left recall 88.9% (8/9) → CI [56.5%, 98.0%]. **중요 — 결론 정정:** 사양 내 right recall의 신뢰구간이 **목표 90%를 포함함.** 따라서 현재 표본으로는 **"right가 목표에 미달한다"고 단정할 수 없음.** 이전 기록들이 "미달"이라 단언한 것은 표본 크기를 고려하지 않은 서술이었음 — 미달일 수도, 달성했을 수도 있으며 **현재 데이터로는 판정 불가**가 정확한 표현임. left도 마찬가지로 [56.5%, 98.0%]라 "충족"이라 단언할 수 없음. 판정을 확정하려면 T44의 데이터 보강(클래스당 test n≈35)이 선행되어야 함. T1 상태는 `in-progress` 유지. **⛔ 데이터 누수 발견 및 누수 없는 재측정 (2026-08-22) — 이전 모든 측정값 무효화:** **발견한 문제 —** 843장 중 789장(93.6%)이 연속 촬영 그룹에 속함. 특히 파일명에 `_saved`가 붙은 파일(245장)은 앱이 카메라 프레임을 자동 저장한 **연사 프레임**으로, 64x64 그레이스케일 평균 픽셀차가 2.2~8.5에 불과함(일반 사진끼리는 23~45). 육안으로도 사실상 같은 사진임. 바이트 단위 완전 복제는 0건(843장 전부 고유 sha256)이나 시각적으로는 중복임. 근사 중복(픽셀차 <10) 기준 고유 이미지: front 332→268(19.3% 중복) / left 127→120(5.5%) / right 155→151(2.6%) / none 229→229(0%) / 합계 843→768. **결과 —** `train_model.py`의 `prepare_data()`가 무작위 셔플로 분할하므로, `_001`로 학습한 모델이 `_002`로 평가받는 누수가 발생. **2026-08-22 이전의 모든 측정값이 이 누수 위에서 나왔음.** **세 측정 비교 (같은 데이터, 같은 학습 설정, 분할 방식만 다름) —** 형식은 단일분할 / 무작위 k-fold / **세션 k-fold**: front 76.5% / 92.8% / **79.5%**; left 92.9% / 92.1% / **87.5%**; none 95.8% / 92.1% / **87.8%**; right 75.0% / 93.5% / **90.7%**; front 오경보 11.8% / 3.6% / **10.1%**. 무작위 k-fold가 front를 13.3%p 부풀리고 오경보를 2.8배 축소했음 — 누수 효과가 정량적으로 확인됨. **누수 없는 측정 (주 지표, 권위 있는 값) —** 스크립트 `train/groupkfold_cv.py`(신규), 로그 `train/groupkfold_log.txt`. 방법: EXIF DateTimeOriginal(843장 전부 보유) 기준 시간순 정렬 후 **간격 60초 초과 시 새 세션** → 147개 세션. 같은 세션은 통째로 같은 fold에 배정하고 근사 중복 클러스터도 fold를 넘지 않음. **분할 직후 assert로 강제 검사하여 위반 시 즉시 중단하도록 구현** — 실행 시 통과 확인됨. 평가는 근사 중복 클러스터당 1장만 집계(768장), 5-fold. 세션 간격 60초 선택 근거: 300초/900초는 최대 세션이 181장/199장(전체의 21~24%)이 되어 fold 균형이 무너지므로 기각, 60초는 최대 43장. **측정값 (점추정 / Wilson 95% CI / 클러스터 부트스트랩 95% CI) —** front recall 79.5%(213/268) / [74.2%, 83.9%] / **[71.8%, 86.1%]**; left recall 87.5%(105/120) / [80.4%, 92.3%] / **[80.2%, 94.2%]**; none recall 87.8%(201/229) / [82.9%, 91.4%] / **[82.9%, 91.4%]**; right recall 90.7%(137/151) / [85.0%, 94.4%] / **[85.5%, 95.3%]**; front 오경보 10.1%(27/268) / [7.0%, 14.3%] / **[4.3%, 17.4%]**. **클러스터 부트스트랩(세션 단위 재표본 10000회)이 정확한 구간임** — Wilson은 이미지 독립을 가정하나 같은 세션 이미지들은 상관되어 있어 구간이 실제보다 좁게 나옴. 앞으로 신뢰구간은 부트스트랩 값을 쓸 것. **목표 판정 —** left 87.5% CI [80.2%, 94.2%] → **판정 불가**(90% 포함); right 90.7% CI [85.5%, 95.3%] → **판정 불가**(90% 포함). T1 상태 `in-progress` 유지. **남은 한계 —** fold당 학습 70%(원본 80%)이므로 배포 모델보다 **낮게 나오는 보수적 추정**일 수 있음(과대평가 위험은 없음). 또한 이 측정은 5개 fold 모델의 집계이지 배포된 `crosswalk_model.onnx` 자체의 측정이 아님. 배포 모델의 개별 성능은 여전히 미측정 상태이며, 그것을 누수 없이 재려면 배포 모델의 학습에 쓰인 이미지와 세션이 겹치지 않는 별도 평가셋이 필요함. | in-progress |
| T2 | Confirm target platforms & min OS; align CI accordingly | P0 | Open Q #1,#2 answered (2026-07-17) | Platforms/min OS stated; CI matches. FULLY UNBLOCKED (2026-07-17): Open Q #1 answered — target platforms = Android + iOS; Open Q #2 answered — Android minSdk 26 (Android 8.0) and iOS minimum = iOS 15. All decision inputs are now resolved and recorded in PRD Target Users + Open Q #2; the decision portion of this task is complete. REMAINING implementer work (config, not decisions): (a) set/confirm Android minSdk 26 in the Android Gradle config; (b) set the iOS deployment target to iOS 15 in the Xcode/Runner project — note this iOS-side config is coupled to the iOS build/signing pipeline (T33), which is PAUSED by user decision, so the iOS config cannot be finalized/verified until T33 resumes; (c) CI is Android/APK-only and does not yet build iOS (also part of T33). Android-side config work can proceed now; iOS-side config waits on T33. ANDROID PORTION DONE (2026-07-17): item (a) landed — `crosswalk_app/android/app/build.gradle.kts:27` `minSdk` changed `24`→`26` (Android 8.0), reflecting PRD Open Q #2's answer. Package-compatibility verified NOT on trust: the actual Android `minSdkVersion` of all 6 installed key packages was read directly from each package's `android/build.gradle` in the pub cache — camera_android_camerax 0.6.19+1 = 21, onnxruntime 1.4.1 = 21, permission_handler_android 12.1.0 = 19, vibration 2.1.0 = 19, wakelock_plus 1.4.0 = 21, flutter_tts 4.2.5 = 24; max = 24 (flutter_tts), so raising to 26 introduces no conflict. `README.md:68` updated from "minSdk 미문서화·미결정" to "minSdk 26, 결정됨". `flutter analyze` re-run locally: same 6 pre-existing known issues, zero new. PR #27 (`fix/T2-android-minsdk-26` → `master`), CI PASSED including the "Build APK" step (GitHub Actions run 29579282503) — confirming the minSdk 26 bump builds a real APK cleanly, not just static-check clean; squash-merged (master `6bde598..0cf8bcf`), synced into develop (commit `1b43df4`, `git diff origin/master origin/develop --stat` = 0 lines). REMAINING (why this task is NOT `done`): item (b) iOS deployment target = iOS 15 in the Xcode/Runner project, and item (c) iOS build in CI, are both still blocked on T33 (PAUSED by user decision) and cannot be finalized/verified until T33 resumes. Status kept `todo` — Android portion DONE, iOS portion blocked on T33. | todo |
| T3 | Decide accessibility standard & screen-reader requirement | P1 | Open Q #5 answered (2026-07-24) | Standard chosen; conformance checklist created. UNBLOCKED (2026-07-24): Open Q #5 answered — 접근성 표준 = TalkBack/VoiceOver 실사용 호환성 확보(공식 WCAG 인증 불요), recorded in `docs/PRD.md` Open Q #5. Decision portion DONE; remaining implementer work = 준수 체크리스트 작성 + 실제 Semantics 구현(T16). | todo |
| T4 | Decide multi-language scope | P2 | Open Q #6 answered (2026-07-17) | Language list fixed. DONE (2026-07-17): Open Q #6 answered — multilingual support IS required (not Korean-only), recorded in PRD Assumptions. LANGUAGE LIST NOW FIXED (2026-07-17): the user enumerated the supported languages = 한국어 (Korean) + 영어 (English) — exactly two languages, no others. This closes the earlier gap where multilingual was confirmed but the specific list was not enumerated. NOTE (scope boundary): the user's answer fixes the language SET only; the concrete locale codes (e.g. ko-KR / en-US variants) and the actual translated strings/TTS phrases are implementation-stage decisions to be made under T17/T34, not fixed here. Implementation of multi-language TTS is tracked as T17 and the broader localization work as T34 (both now fully unblocked). | done |
| T5 | Decide in-app safety disclaimer requirement | P1 | Open Q #9 answered (2026-07-17) | Yes/no + copy if yes. UNBLOCKED (2026-07-17): Open Q #9 answered — a disclaimer IS required, shown at onboarding / first launch, recorded in PRD Risks. The exact disclaimer copy was NOT provided by the user (still to be written). Implementation (onboarding screen + disclaimer) is tracked as T36. | todo |
| T6 | Decide low-light/adverse-condition scope for v1 | P2 | Open Q #12 answered (2026-07-17) | In/out of scope recorded. UNBLOCKED (2026-07-17): Open Q #12 answered — low-light/night IS in scope for v1 (improvement needed later); recorded in PRD. Decision DONE. Implementation of low-light handling is tracked as T37. (rain/occlusion not separately addressed by the user.) DONE (2026-07-17): this is a decision task and its sole acceptance criterion ("In/out of scope recorded") is fully met — the decision is recorded in `docs/PRD.md:92` Open Q #12 ("ANSWERED (user, 2026-07-17): low-light/night IS in scope for v1, with improvement needed later. No low-light handling exists in code today — see new task candidate T37. (rain/occlusion not separately addressed by the user.)") and mirrored in this row's own body (`docs/Tasks.md:15`). No new investigation performed — status corrected `todo`→`done` purely to sync the Status column with the already-recorded decision. SCOPE BOUNDARY: T6 covers only the scope DECISION; actual low-light handling logic is out of T6 scope and remains open under T37. | done |

## B. Ready — safety & correctness (no decision needed)
| ID | Task | Priority | Depends on | Acceptance Criteria | Status |
|---|---|---|---|---|---|
| T7 | Populate real SHA-256 for bundled model; enable integrity verification | P0 | Open Q #10 answered (2026-07-17) | `crosswalk_model.onnx.sha256` holds real 64-hex hash; app rejects tampered model. Evidence gap: `assets/model/crosswalk_model.onnx.sha256:1` = `placeholder_hash`. UNBLOCKED (2026-07-17): Open Q #10 answered by the user — confirmed the committed model is a real trained model, not a dummy, byte-verified identical to `model/crosswalk_model.onnx` (`cmp`, 6,098,102 bytes). Task is now ready for an implementer to pick up; it stays `todo` because populating a real SHA-256 hash and enabling the integrity check in code is an actual implementation task, not something resolved by answering the question. CAVEAT (Q #10): if training later completes and the model is replaced, the SHA-256 hash must be regenerated against the new final checkpoint. DONE (2026-07-17): fix landed in commit `794cb0e` (branch `fix/T7-model-integrity-hash`), merged to master as PR #18 (`9fdffc8`), synced into develop — blast radius = exactly one file (+1/-1): replaced `crosswalk_app/assets/model/crosswalk_model.onnx.sha256`'s content from `placeholder_hash` to the real SHA-256 `2454b8960b12e8ef76166cb569137f44e1074c9e890e86bbc7d2a4bd7320c4cb` of the bundled `crosswalk_model.onnx`. NO code change — `classifier.dart`'s existing `_verifyModelIntegrity` logic already activates once a real 64-hex-char hash is present (`classifier.dart` `git diff` empty, confirmed untouched). Independent reviewer verification (not trusted from implementer's report): (a) recomputed the hash with TWO separate tools — Python `hashlib.sha256` AND `sha256sum` — both matching the committed value exactly; (b) byte-level file-content check via `od -c` — exactly 64 hex chars + CRLF, no extra content, so `classifier.dart`'s `.trim()`-based length check passes cleanly; (c) source-level "no false-rejection" trace, verified against real source not the implementer's claim — confirmed via `pubspec.yaml`'s asset declaration a single asset path with no renaming/fingerprinting between repo file and what `rootBundle.load` reads at runtime, and read the ACTUAL locked `crypto` 3.0.7 source (`digest.dart:41,45`, from `pubspec.lock`) confirming `sha256.convert(...).toString()` produces lowercase hex matching the stored hash's case. `flutter analyze` (reviewer, independent): same 6 pre-existing issues, zero new. `flutter test` (reviewer): reproduced the same pre-existing local `flutter_tester.exe` crash (environment issue, unrelated to this change). Regression evidence: CI run 29556305833 (GitHub Actions, PR #18) — Flutter test + APK build passed. CI-DYNAMIC-OVERWRITE SCOPING CAVEAT (reviewer, read directly from `.github/workflows/build_apk.yml:36-50`): CI recomputes/overwrites this same `.sha256` file at build time regardless of what's committed, so this fix's real-world effect is scoped to non-CI/local build-and-run contexts (e.g. a developer running `flutter build apk`/`flutter run` directly against a fresh clone without CI's preprocessing) — not silently irrelevant, but CI success mainly proves "build/format not broken," not runtime-correctness of the check itself (same honest-caveat pattern used for similar cases this session). RESIDUAL GAP (accepted, inherent limitation — same pattern as T21/T23–T26): no automated test exercises `_verifyModelIntegrity` with a real asset load (would require full Flutter asset-bundling + platform channels, infeasible in plain `flutter test`); a low-cost testability follow-up to add regression coverage for the pure hash-comparison logic is tracked as T32. | done |
| T8 | Confirm committed `crosswalk_model.onnx` is the real trained model (not dummy) | P0 | Open Q #10 answered (2026-07-17) | Verified match with `model/crosswalk_model.onnx`. DONE (2026-07-17): Open Q #10 answered by the user — confirmed real trained model, not a dummy — and independently byte-verified identical to `model/crosswalk_model.onnx` via `cmp` (both files 6,098,102 bytes, byte-for-byte identical). This directly and fully satisfies both parts of the acceptance criteria: (1) "not a dummy" — user confirmation plus the byte-match proves the app-bundled asset genuinely IS the training pipeline's actual output file (not a substituted/placeholder file); (2) "Verified match with `model/crosswalk_model.onnx`" — satisfied by the `cmp` check itself. Provenance of `model/crosswalk_model.onnx` as genuine pipeline output was already established under T22 (verified via `onnx.load()`: ir_version=7, opset=12, matching `train/export_onnx.py`), so the full chain train pipeline → `model/crosswalk_model.onnx` → bundled asset is now confirmed end to end. CAVEAT (Q #10, does NOT reopen T8): the user noted training may not be complete/final, so this could be an intermediate checkpoint rather than the final production-quality model — but T8 only asks whether the file is the real trained model vs a dummy (now confirmed yes), NOT whether it is the FINAL model. Model quality/accuracy is tracked separately under Open Q #3 / T1. Any future replacement with a final checkpoint will require re-running this byte-match (and regenerating T7's hash) against the new file. | done |
| T9 | Add unit tests for `Classifier` (smoothing, thresholds, throttle) | P1 | — | Tests exist under `crosswalk_app/test/` and pass. Gap: no test dir. NOTE: T9 is now also a prerequisite for closing T21 — the Classifier unit tests should include a case exercising the corrected softmax path (verify probabilities sum to ~1.0 and thresholds 0.55/0.85 are reachable) given T21's fix. DONE: tests added in commit `eb688a7` (`crosswalk_app/test/classifier_test.dart`, 7 tests via behavior-preserving extract-method refactor exposing `softmax`/`decideFromLogits`/`shouldProcessFrame` as `@visibleForTesting`). CI-verified: GitHub Actions run 29465632108 (PR #2, branch `feat/T9-classifier-tests`) — `flutter test` passed 7/7 covering softmax (sums to ~1.0, numerical stability, thresholds 0.55/0.85 reachable), decideFromLogits smoothing (5-frame sliding window + transition blend), threshold gating (returns null below threshold), and shouldProcessFrame throttle; subsequent APK build/sign/upload steps also passed. Smoothing/threshold/throttle behavior now covered. | done |
| T10 | Add unit tests for `FeedbackService` (cooldown, class-change, front-silence) | P1 | — | Tests cover `feedback_service.dart:17-39`. DONE: decision logic extracted from `alert()` into `@visibleForTesting String? decideMessage(String detectedClass, DateTime now)` (time injected as a parameter for determinism); `crosswalk_app/test/feedback_service_test.dart` added with 7 tests covering front-silence, first-alert, cooldown-suppression, cooldown-boundary-expiry, class-change-bypass, and exact left/right message text (refactor + test file in commit `cf81a77`, branch `feat/T10-feedback-service-tests`, merged to master as PR #7 = `2c06f22`, synced into develop). Independent reviewer verification: PASS (line-by-line behavior-preservation check, boundary-condition arithmetic re-derivation, and test-isolation check) — BUT the reviewer explicitly could NOT confirm the tests actually pass at runtime via a local `flutter test`, because this dev machine's `flutter_tester.exe` crashes at suite-load time (a separate, pre-existing, already-documented environment issue affecting ALL test files on this machine, unrelated to code correctness). LESSON — CI-caught bug undetectable by static review or local execution: the FIRST CI run (PR #7, run 29473791609) FAILED — all 7 new tests errored with `Failed assertion: ... Cannot set the method call handler before the binary messenger has been initialized`, because `FeedbackService`'s constructor eagerly builds `FlutterTts()` (a platform-channel-backed plugin) and the test file used plain `test()` blocks with no `TestWidgetsFlutterBinding.ensureInitialized()`, so merely constructing `FeedbackService()` crashed before any test body ran. This was a real, deterministic (not flaky) bug that passed BOTH implementation AND independent code review. Fix: one line — `TestWidgetsFlutterBinding.ensureInitialized();` as the first line of `main()` in `feedback_service_test.dart` (commit `d61bfa6`); no production code (`feedback_service.dart`) changed — purely a test-setup gap. Re-run CI (PR #7, run 29474000817, https://github.com/ssong2332/crossWalk/actions/runs/29474000817) PASSED: `flutter test` reported 14/14 tests passed (7 pre-existing T9 `classifier_test.dart` + all 7 new `feedback_service_test.dart`) and `flutter build apk --release` succeeded. This demonstrates that, given the broken local `flutter_tester.exe`, CI is currently the SOLE source of real test-execution truth for this project's suite — every new test file (T9, T10) has had zero local execution before its first CI run, and T10 proves a genuine bug can survive implementation and review to be caught only by CI actually running the code. Reinforces CI-first verification discipline as an active risk-mitigation, not a minor inconvenience. | done |
| T11 | Add widget test for `CameraScreen` error/retry states | P2 | — | Covers `_hasError` overlay + retry (`camera_screen.dart:202-278`). DONE: landed across two commits on branch `feat/T11-camerascreen-widget-tests`, merged to master as PR #12 (`f0473b2`), synced into develop. Commit `7bc95a6` (new test file, `crosswalk_app/test/camera_screen_test.dart`, 199 lines): 4 `testWidgets()` tests covering both permission-denial error states (ordinary and permanent) — reachable without mocking the `camera`/ONNX plugins because `_initCamera()` returns early on the permission-denial paths before `_classifier.init()`; mocked `permission_handler`, `flutter_tts`, `wakelock_plus` platform channels, with every channel name/protocol independently verified against actual package source by BOTH implementer and reviewer (reviewer traced one level deeper each — e.g. confirmed `CameraScreen` only ever calls `permission_handler`'s `requestPermissions`, never `checkPermissionStatus`, so the mock needn't handle the latter). No production code touched by this commit. Commit `75d9c40` (REQUIRED follow-up fix): the PR's first CI run (29482435398) FAILED all 4 new tests with `Failed to load dynamic library 'libonnxruntime.so.1.15.1'`. Root cause was NOT a defect in the new test file but a genuine LATENT PRODUCTION-CODE BUG dating back to T24: `Classifier.dispose()` unconditionally called `OrtEnv.instance.release()` regardless of the `_envInitialized` flag; `OrtEnv.instance` is a lazy singleton that loads a native library on FIRST access, so releasing-without-ever-initializing still triggered a doomed native-library load in the `flutter test` sandbox (no native binary bundled). T11's tests were simply the first code in the whole suite to ever exercise `CameraScreen.dispose()`/`Classifier.dispose()` on a path where `Classifier.init()` was never called. Fixed by guarding `OrtEnv.instance.release()` behind `if (_envInitialized) { ... }` in `dispose()` — a single-file `classifier.dart` change (+4/-2). Independent reviewer two-pass verification: (pass 1) PASS on the test file's code quality alone, separately flagging that no PR/CI existed yet as the critical missing step (citing T9's history as precedent); (pass 2, after the follow-up fix) PASS again — (a) traced that `_envInitialized` correctly starts false and is set true only inside `init()`, so the guard skips `OrtEnv.instance` entirely when `init()` was never called (fixing the CI bug) while still firing exactly as before when `init()` DID complete (preserving T24's leak-prevention intent, not weakening it); (b) independently re-fetched BOTH CI run logs via `gh run view` (not trusting the task's framing) — confirmed run 29482435398 literally showed "14 tests passed, 4 failed" (pre-fix) and run 29482819510 literally showed "🎉 18 tests passed." (post-fix, log-line-verified); (c) confirmed blast radius: follow-up commit alone touched only `classifier.dart` (+4/-2), and the full PR vs develop touched only 2 files — `classifier.dart` (production fix) + `camera_screen_test.dart` (new test file), nothing else; (d) scanned for similar "lazy-singleton-touched-unconditionally-in-cleanup-code" patterns elsewhere in `classifier.dart`/`camera_screen.dart` and found none. Reviewer could NOT run `flutter analyze`/`flutter test` itself this pass (no `flutter` on PATH that time) and explicitly flagged that as unconfirmed by it — but the main session ran `flutter analyze` directly afterward: same 6 pre-existing issues, zero new (confirmed `classifier.dart:103`'s pre-existing `unnecessary_null_comparison` warning is unaffected/unshifted, since the `dispose()` fix is well after line 103). Regression evidence: CI run 29482819510 (PR #12) — `flutter test` passed 18/18 (log-verified by reviewer directly via `gh run view`, not merely trusted). LESSON (THIRD distinct pattern, NOT the same as T10's or T27's): the new test file itself had NO defect (reviewer gave it a clean code-quality PASS) — but writing these tests was the first time ANY test in this repo ever exercised a previously-untested execution path in EXISTING production code (`Classifier.dispose()` without a preceding `init()`), and that untested path contained a genuine latent bug pre-dating the task (from T24). New tests can be the first code path to ever exercise an existing, previously-untested branch of production code, surfacing old latent bugs that pre-date the task being worked on. Passing code review on a new test file does NOT guarantee CI success, because new tests can surface old bugs in the code they exercise, not just bugs in themselves. This is distinct from T10's lesson (a test-SETUP bug — missing `TestWidgetsFlutterBinding.ensureInitialized()` — where the defect was IN the test file itself) and from T27's lesson (a regression caught by CODE REVIEW before merge/CI, where review not CI caught it): T11's is a third pattern — a clean new test file surfacing an old latent production bug, caught by CI. | done |
| T21 | Fix CONFIRMED logits-vs-probability-threshold defect (release blocker, highest priority — above T1/T2/T7/T8) | P0 | T9 | CONFIRMED DEFECT (reviewer loaded shipped `assets/model/crosswalk_model.onnx` via onnx/onnxruntime): graph's final ops are `['Flatten','Gemm','HardSigmoid','Mul','Gemm']` — no Softmax node; real inputs produce outputs like `[-0.0838, 0.4418, -0.4263]` (negative, not summing to 1) = raw logits, not probabilities. `train/train_model.py:105-109` builds plain `nn.Linear` final layer, trains with `CrossEntropyLoss` (expects logits); `export_onnx` (~L218-227) exports as-is with no softmax. `classifier.dart:107-108` compares this raw logit against thresholds 0.55 (deviation) / 0.85 (front) (defined `classifier.dart:27-28`) as if probabilities. Safety-critical deviation alert may rarely/never fire in field = false-negative safety bug (not cosmetic). Fix: add softmax at export OR re-derive logit-scale thresholds from validation data, then re-validate detection rate. Acceptance: activation/threshold semantics made consistent and detection rate re-validated on test set. UPDATE: the semantic fix (softmax applied in `classifier.dart`, commit `33786d7`) has landed and passed independent reviewer re-verification (PASS — correct variable/timing/numerical stability confirmed), but empirical detection-rate re-validation against a labeled test set (via T9) is still required before this task can move to `done`. DONE: unit-test-level re-validation satisfied via T9 — CI run 29465632108 (PR #2) ran `flutter test` and passed 7/7, including the two tests specifically validating this fix: `softmax produces probabilities summing to ~1.0 for T21 example logits` and `softmax thresholds 0.55/0.85 are reachable given a sufficiently skewed logit vector`. NOTE: this is unit/synthetic-level validation only — it is NOT field/real-camera accuracy validation on a labeled real-world test set (that concern remains open under T1/T12 and is not covered here). | done |
| T23 | Add re-entrancy guard to `_initCamera()` (concurrency bug, safety-adjacent) | P0 | — | `camera_screen.dart:52-130` is async with no `_isInitializing` flag; can be re-invoked mid-flight from `didChangeAppLifecycleState` (`camera_screen.dart:155-156`) or retry button (`camera_screen.dart:265`) while a prior call is still awaiting `CameraController.initialize()`/`Classifier.init()`. Rapid resume/pause cycling or a double-tap on retry can create two concurrent `CameraController`s/ONNX sessions racing to assign `_controller`, orphaning one controller's dispose (compounds T24). Acceptance: concurrent entry is blocked/serialized; no orphaned controller/session under rapid resume/retry. DONE: fix landed in commit `a9b77f4` (branch `fix/T23-camera-reinit-guard`), merged to master as PR #4 (`cb9e0f0`), synced into develop — adds a `bool _isInitializing` guard wrapping `_initCamera()`'s whole body in try/finally (flag can't get stuck) and disables the retry button while a call is in flight (`camera_screen.dart`, +71/-63). Independent reviewer re-verification: PASS — confirmed atomic check-and-set (no `await` between check and set = no TOCTOU race), `finally` covers every exit path (permission-denied, no-camera, `!mounted`, both catch blocks), `mounted` guards preserved, all three call sites (initState, didChangeAppLifecycleState resume, retry button) can no longer run concurrently, blast radius = exactly one file, and `flutter analyze` shows the same 6 pre-existing issues (content-matched) with zero new issues. Regression evidence: CI run 29467147327 (PR #4) — `flutter test` passed 7/7 (the pre-existing T9 Classifier unit tests, confirming no regression elsewhere) and `flutter build apk --release` succeeded. CAVEAT: no dedicated automated test yet exercises the re-entrancy guard behavior itself (e.g. simulating rapid resume/retry) — the 7 passing tests do not touch `camera_screen.dart` logic; T23's correctness rests on the reviewer's code-trace PASS, not on dedicated guard test coverage. A dedicated concurrency/widget test is a natural fast-follow (relates to T11), not required to close T23 as originally scoped. | done |
| T24 | Release `OrtSession`/`OrtEnv` native resources before re-init (memory leak) | P1 | — | `classifier.dart:37-43` `init()` unconditionally creates a new `OrtSession` without checking/releasing an existing `_session`. Re-entry paths: `camera_screen.dart:75` in `_initCamera()`, called on every app-resume lifecycle transition (`camera_screen.dart:155-156`) and retry button tap (`camera_screen.dart:265`). `Classifier.dispose()` (`classifier.dart:177-180`) only runs once at widget teardown. Repeated background/foreground cycles or retry taps leak native ONNX Runtime memory, risking OOM kill during a long walk. Acceptance: prior session/env released before re-init; no native memory growth across repeated resume/retry cycles. DONE: fix landed in commit `bc0bba8` (branch `fix/T24-ortsession-leak`), merged to master as PR #5 (`03b171a`), synced into develop — `init()` now calls `_session?.release(); _session = null;` before creating a new session, and gates `OrtEnv.instance.init()` behind a new `_envInitialized` bool so it only runs once per instance (previously it ran on every `init()`); `dispose()` now also resets `_session = null` and `_envInitialized = false` (`classifier.dart`, +17/-1). Independent reviewer verification: confirmed the core claim by reading the actual `onnxruntime` 1.4.1 package source (`ort_env.dart:34-58`) — `OrtEnv.instance.init()` is genuinely non-idempotent (allocates a new native env and overwrites the previous pointer without releasing it), not taken on faith from the implementer. Reviewer also confirmed: release-before-reassign ordering is correct; the `_envInitialized` guard correctly permits re-init only after a `dispose()`; traced the model-integrity-check-exception mid-init edge case and found `_session` stays safely `null` in that path (a slight improvement over pre-fix behavior, which could orphan an unreleased old session on a failed re-init). Blast radius = exactly one file, does not touch `_recentProbs`/`_frameCount` (T26, correctly out of scope). `flutter analyze` (run independently): same 6 pre-existing issues (content-matched), zero new. Regression evidence: CI run 29470190177 (PR #5) — `flutter test` passed 7/7 (existing T9 Classifier tests) and `flutter build apk --release` succeeded; reviewer confirmed by reading the test file that none of the 7 tests call `init()`/`dispose()`, so this change is a genuine source-level no-op for them (not merely "happened to pass"). CAVEAT: no dedicated test verifies the actual absence of native-memory-leak growth (would require an instrumented/integration test measuring native heap across repeated init/dispose cycles) — that remains an open residual gap, same pattern as T21/T23's closure notes; correctness of the leak fix rests on the reviewer's source-verified code trace, not on dedicated leak-measurement coverage. | done |
| T25 | Configure `flutter_tts` queue mode to flush/interrupt (stale safety cue risk) | P1 | — | `feedback_service.dart:11-15` sets language/rate/volume only, no `setQueueMode`/`awaitSpeakCompletion`; `alert()` (`feedback_service.dart:17-39`) calls `_tts.speak(message)` without interrupting prior speech. CORRECTED PREMISE: the original row claimed the risk stemmed from "Android default QUEUE_ADD (not QUEUE_FLUSH)" — reviewer verification against `flutter_tts` 4.2.5 source (`FlutterTtsPlugin.kt`) found this is FACTUALLY WRONG for this dependency version: the Android plugin already defaults `queueMode = TextToSpeech.QUEUE_FLUSH`, and `setQueueMode` is documented Android-only. The real load-bearing risk is iOS: `AVSpeechSynthesizer.speak(utterance)` queues new utterances after the current one by default (per Apple docs), so with per-class-only cooldown (`feedback_service.dart:20-28`) a user oscillating near a decision boundary could hear a stale "move left" AFTER already correcting right — on iOS. The fix (a cross-platform `stop()` before `speak()`) is still correct and necessary; it is load-bearing on iOS and a harmless no-op re-flush on Android. Acceptance: flush/interrupt semantics set explicitly and re-verified against the 3s cooldown design. DONE: fix landed in commit `e5ca05f` (branch `fix/T25-tts-queue-mode`), merged to master as PR #6 (`9725413`), synced into develop — a single `await _tts.stop();` added immediately before `await _tts.speak(message);` inside `alert()`, placed AFTER the cooldown check so it does not fire on suppressed repeat-alerts (`feedback_service.dart`, +1/-0). Independent reviewer package-source verification: read `flutter_tts` 4.2.5 Dart, Android Kotlin, and iOS Swift layers — confirmed `setQueueMode` is Android-only, confirmed `stop()` is cross-platform and safe to call when idle (no-op on both Android `TextToSpeech.stop()` and iOS `AVSpeechSynthesizer.stopSpeaking`), and produced the QUEUE_FLUSH/iOS-queueing correction noted above. Blast radius: exactly one line changed; cooldown logic, `announceError()`, and `dispose()` byte-identical to develop. `flutter analyze` (run independently): same 6 pre-existing issues, zero new (the feedback_service.dart warning merely shifted line 36→37 from the added line — confirmed same warning, not new). Regression evidence: CI run 29471306750 (GitHub Actions, PR #6, https://github.com/ssong2332/crossWalk/actions/runs/29471306750) — `flutter test` passed 7/7 and `flutter build apk --release` succeeded. CAVEAT: this CI evidence shows NO regression elsewhere but provides ZERO direct test coverage of the fix itself — reviewer confirmed by reading `classifier_test.dart` that none of the 7 tests reference `FeedbackService`. `FeedbackService` remains untested (dedicated tests tracked separately as T10, still `todo`); T25's correctness rests on the reviewer's source-verified code trace, not on dedicated test coverage. | done |
| T26 | Reset `Classifier` smoothing state on re-init (stale probabilities after resume) | P2 | T23 | `_recentProbs`, `_frameCount` (`classifier.dart:34-35`) are instance fields on a single long-lived `Classifier` (`camera_screen.dart:18`); `init()` never clears them. Stale pre-pause probability vectors get averaged into the first ~5 throttled post-resume frames (smoothing window `classifier.dart:90-98`), delaying correct detection right after the user picks the phone back up mid-crossing. Acceptance: smoothing state cleared on init/re-init; no stale-vector contamination after resume/retry. DONE: fix landed in commit `f888405` (branch `fix/T26-reset-smoothing-state`), merged to master as PR #9 (`fc79e85`), synced into develop — a 2-line addition (`_recentProbs.clear(); _frameCount = 0;`) in `init()` immediately after the existing `_session?.release(); _session = null;` cleanup (`crosswalk_app/lib/services/classifier.dart`, +5/-0). Independent reviewer verification: PASS — (a) placement correct: reset happens before any new session/inference could run, no race window; (b) T24's `_envInitialized` flag untouched, correctly out of scope; (c) `decideFromLogits`/`shouldProcessFrame`/`softmax`/thresholds/`_smoothingWindow`/`_throttleFrames` all unchanged. Reviewer traced the throttle-reset behavior: resetting `_frameCount = 0` means every re-init now deterministically waits exactly 5 frames before the first post-resume classification attempt, versus the pre-fix variable 0-4 frame delay (depending on wherever the counter sat when paused) — a minor, bounded, deterministic behavior change judged a good trade-off for the correctness gain (residual note, not a defect). No-test-added judgment assessed sound: `_recentProbs`/`_frameCount` are plain private fields with no `@visibleForTesting` accessor, so they're genuinely inaccessible from the test file (Dart privacy is per-file/library) even for read-only inspection; and `init()` requires real asset loading (`rootBundle.load`) plus a real native ONNX session (`OrtSession.fromBuffer`), consistent with why T9/T10 only tested the extracted pure `@visibleForTesting` methods (none of which include `init()`) — a dedicated test would require disproportionate scope creep (new test-only accessor plus asset/native mocking). Blast radius = exactly one file. `flutter analyze` (run independently, cross-checked against reviewer expectations): same 6 pre-existing issues, zero new (the `classifier.dart` unnecessary_null_comparison warning merely shifted line 98→103 from the +5 lines — confirmed same warning content, not new). Regression evidence: CI run 29475871845 (GitHub Actions, PR #9, https://github.com/ssong2332/crossWalk/actions/runs/29475871845) — `flutter test` passed 14/14 (pre-existing T9 Classifier + T10 FeedbackService tests) and `flutter build apk --release` succeeded; none of the 14 tests call `init()`, so this change is a genuine source-level no-op for all of them (not merely "happened to pass"). CAVEAT: no automated test verifies the reset's actual runtime effect (that `_recentProbs`/`_frameCount` are genuinely empty/zero after a real re-init cycle) — honest residual gap, same pattern as T21/T23/T24/T25's closure notes; T26's correctness rests on the reviewer's code-trace PASS, not on dedicated coverage. | done |
| T27 | Offer `openAppSettings()` after permanently-denied camera permission (accessibility recovery) | P2 | — | `camera_screen.dart:62-72` shows/speaks "카메라 권한이 필요합니다. 설정에서 허용해주세요." but never calls permission_handler's `openAppSettings()`; retry button just re-calls `Permission.camera.request()`, a no-op once permanently denied on Android. Leaves a visually-impaired user with no actionable in-app recovery path. Acceptance: permanent-denial path offers/triggers `openAppSettings()` as recovery. DONE: fix landed across two commits on branch `fix/T27-permission-settings-recovery`, merged to master as PR #10 (`a036db3`), synced into develop — the full PR vs develop is exactly one file, `camera_screen.dart` (+40/-13). Commit `bd012fe` (initial attempt): added a `_permissionPermanentlyDenied` state field; when `status.isPermanentlyDenied`, shows a distinct message and switches the existing retry button to call `openAppSettings()` (label "설정 열기") instead of `_initCamera()`; ordinary (non-permanent) denial path left unchanged. LESSON — regression caught by REVIEW before merge/CI (analogous to T10's CI-caught bug, but arguably more valuable since review caught it EARLIER, before merge): the reviewer agent's FIRST pass on `bd012fe` alone gave a FAIL verdict with a precise call-site root-cause trace — `didChangeAppLifecycleState`'s guard (`if (_controller == null || !_controller!.value.isInitialized) return;`) blocked the `resumed` branch from ever running while the user was stuck on the permission-error screen (where `_controller` stays null), so after the user granted permission in Settings and returned to the app NOTHING re-triggered `_initCamera()` — a new dead-end loop (endless trips to Settings) that regressed UX for this app's visually-impaired target user who can't easily force-quit/relaunch. Follow-up fix (REQUIRED before merge) in commit `c01a92a`: restructured `didChangeAppLifecycleState` so the null/initialized guard only gates the `inactive`-branch `dispose()` call, and the `resumed`-branch `_initCamera()` call is now unconditional (this follow-up commit alone: +3/-2, touching only `didChangeAppLifecycleState`). The SAME reviewer agent re-reviewed `c01a92a` and gave PASS, re-tracing the full scenario end-to-end (permanent denial → error screen → user taps "설정 열기" → grants permission in Settings → returns → `resumed` fires → `_initCamera()` unconditionally called → permission re-check now succeeds → normal setup proceeds) and confirming NO regression to the normal "app backgrounded while camera actively running" case (dispose still fires under the exact same condition as before). `flutter analyze` (reviewer ran independently on the final commit): same 6 pre-existing issues (content-matched), zero new. Regression evidence: CI run 29477107178 (GitHub Actions, PR #10, https://github.com/ssong2332/crossWalk/actions/runs/29477107178) — `flutter test` passed 14/14 (pre-existing T9 Classifier + T10 FeedbackService tests) and `flutter build apk --release` succeeded; reviewer confirmed via import check that neither test file touches `camera_screen.dart`, so this is a clean no-regression signal but provides ZERO direct coverage of the fix itself. Residual non-blocking gaps (recorded honestly): (1) `openAppSettings()`'s `Future<bool>` return value is still ignored — if it returns `false` (settings fails to open, e.g. some OEM ROMs) the user gets no feedback/fallback; judged a secondary usability gap, not a functional regression. (2) the broadened unconditional `resumed → _initCamera()` now also auto-retries for OTHER error states (e.g. "카메라 없음", "모델 손상"), not just permanent-denial — reviewer judged this likely-benign/arguably-beneficial, worth noting since not explicitly designed for. (3) no dedicated automated test exists for either the lifecycle fix or the permission-recovery flow (same pattern as T21/T23–T26; correctness rests on reviewer code-trace, not coverage) — relates to T11, still `todo`. | done |
| T28 | Make release Gradle signing fail loudly instead of silently falling back to debug keystore | P3 | — | `android/app/build.gradle.kts:33-39` `release { signingConfig = signingConfigs.getByName("debug") }` with `// TODO: Add your own signing config` still present; real signing only via manual CI step (`.github/workflows/build_apk.yml:60-91`, gated on `KEYSTORE_BASE64`). Running `flutter build apk --release` locally without CI yields a debug-signed APK. Acceptance: default config fails loudly outside CI rather than silently debug-signing. DONE: fix landed in commit `5340aab` (branch `fix/T28-release-signing-guard`), merged to master as PR #11 (`1270783`), synced into develop — blast radius = exactly one file, `crosswalk_app/android/app/build.gradle.kts` (+21/-3). The `release` build type now only permits `signingConfig = signingConfigs.getByName("debug")` when `System.getenv("GITHUB_ACTIONS") == "true"` (CI detection) OR `project.hasProperty("allowDebugSigningForRelease")` (explicit local opt-in); otherwise it throws a `GradleException` with a clear message, refusing to silently debug-sign a "release" build. Independent reviewer verification: (a) confirmed `GITHUB_ACTIONS=true` is a genuinely reliable GitHub Actions default env var by fetching GitHub's OWN official docs (`docs.github.com/en/actions/reference/workflows-and-actions/variables`, documented for exactly this "differentiate CI vs local" use case), not from prior knowledge alone; (b) confirmed `.github/workflows/build_apk.yml` was NOT modified (`git diff` empty for that file) and traced that since GitHub Actions always sets `GITHUB_ACTIONS=true` during the run, the existing CI flow (Gradle debug-signs → separate zipalign/apksigner re-sign) is completely unaffected = no regression risk; (c) independently ran `flutter build apk --release` (no flag) on this branch and got a build failure in 6s with the EXACT `GradleException` message at the intended line (line 48, the `else` branch), confirming the intended runtime check firing — not an accidental Kotlin DSL syntax/config-parse error; (d) also ran WITH `-PallowDebugSigningForRelease=true` and confirmed (verbose-log grep) the guard was correctly bypassed and the build proceeded into actual task execution, failing later only for an unrelated pre-existing native cmake/NDK toolchain issue (out of scope). Regression evidence: CI run 29479560358 (GitHub Actions, PR #11, https://github.com/ssong2332/crossWalk/actions/runs/29479560358) — `flutter test` passed 14/14 and `flutter build apk --release` succeeded with NO `Refusing`/`GradleException` triggered (confirming `GITHUB_ACTIONS=true` correctly kept CI's existing behavior intact — verified by grepping the CI log). NOTE: no automated test exists (or is expected) for Gradle build-config behavior — an inherent limitation of this task type, not a coverage gap; correctness rests on the reviewer's independent build-attempt reproduction (both failure path and opt-in path) plus CI. | done |
| T32 | Extract `_verifyModelIntegrity`'s hash-comparison logic into a `@visibleForTesting` pure function and add unit tests | P2 | — | Extract `_verifyModelIntegrity`'s hash-comparison logic into a `@visibleForTesting` pure function taking `modelBytes`/`expectedHash` as parameters (e.g. `bool hashMatches(Uint8List modelBytes, String expectedHash)`), mirroring the `softmax`/`decideFromLogits`/`shouldProcessFrame` extraction pattern from T9 (and the `decideMessage` pattern from T10); add unit tests in `crosswalk_app/test/classifier_test.dart` covering: hash match (returns true / no exception), hash mismatch (returns false / throws `ModelIntegrityException` as appropriate), `placeholder_hash` value (skips verification), wrong-length hash (skips verification). No real asset-bundling or platform-channel mocking required — pure synthetic byte arrays and hand-computed SHA-256 values. This is a low-cost testability improvement, NOT a functional gap: the actual integrity-check LOGIC is already correct and verified (per T7's closure above), so this task is purely about adding automated regression coverage going forward — hence P2 (moderate, not urgent). Evidence: recommended by reviewer during T7's review (see T7 closure note, this section); `crosswalk_app/lib/services/classifier.dart:66-86` is the current inline logic to extract from. DONE: landed across two commits on branch `feat/T32-hash-integrity-tests`, merged to master as PR #20 (`f41b006`), synced into develop. Commit `a0bed93` (extraction + tests): extracted `hashMatches(Uint8List modelBytes, String expectedHashRaw)` as a `@visibleForTesting` pure function from `_verifyModelIntegrity` in `crosswalk_app/lib/services/classifier.dart` (behavior-preserving — `_verifyModelIntegrity` now just delegates: `if (!hashMatches(modelBytes, hashFile)) throw ModelIntegrityException(...)`, outer try/catch unchanged), and added 5 tests to `crosswalk_app/test/classifier_test.dart` (match, mismatch, placeholder-skip, wrong-length-skip, and a CRLF/`.trim()` regression guard). Commit `92fca26` (REQUIRED follow-up fix): corrected the defective CRLF test only. Independent reviewer verification — TWO passes: (pass 1, on `a0bed93` alone) PASSED the `hashMatches` extraction/refactor itself — explicit case-by-case trace of all 4 logic paths (length-skip, placeholder-skip, genuine match, genuine mismatch) confirmed the throw→bool refactor introduced NO boolean-inversion bug, outer try/catch unchanged, and 4 of 5 new tests sound (match/mismatch hash values genuinely computed from/against the right byte arrays, not copy-paste); but FAILED overall specifically because the CRLF test was ineffective — it asserted `hashMatches(bytes, matchingHash + '\r\n')` returns `true`, but tracing showed the 66-char input hits the length-skip branch (66 ≠ 64) REGARDLESS of whether `.trim()` runs, so the test would still pass even if `.trim()` were deleted from the source = ZERO actual regression coverage for the trim call. Fixed in `92fca26` by switching to a MISMATCHING hash + `\r\n` asserting `isFalse`: this genuinely distinguishes trim-present (CRLF stripped → real comparison → mismatch → `false`, passes) from trim-absent (stays 66 chars → length-skip → `true`, test would fail). (pass 2, on `92fca26`) PASS — independently re-traced BOTH trim-present/trim-absent scenarios from scratch (did not just accept the fix commit's own reasoning) and confirmed the corrected test genuinely flips result on `.trim()`'s presence; confirmed the follow-up's diff to `classifier.dart` is EMPTY (production code was correct from the very first review pass and never touched again), blast radius of the follow-up = exactly one file (`classifier_test.dart`), and the other 4 tests byte-for-byte unchanged between the two commits; independently pulled the CI log itself (`gh run view 29566964458 --log`, not merely trusting the citation) confirming the CRLF test passed by name and the "23 tests passed" summary. Regression evidence: CI run 29566964458 (GitHub Actions, PR #20, https://github.com/ssong2332/crossWalk/actions/runs/29566964458) — `flutter test` passed 23/23 (18 pre-existing + 5 new from T32, including the corrected CRLF test by name) and `flutter build apk --release` succeeded. `flutter analyze`: the pass-2 reviewer could NOT run it (no Flutter tooling in that environment) and honestly flagged this as an unconfirmed gap rather than fabricating a result; the main session ran it directly on the final merged code — same 6 pre-existing issues (content-matched, `classifier.dart`'s `unnecessary_null_comparison` warning shifted 103→111 from the added code, same warning not new), zero new. LESSON (distinct from T10's and T27's patterns): this was NOT a production-code bug — `classifier.dart`'s logic passed review cleanly on the first pass and was never modified again — but a TEST-QUALITY defect: a test that looked reasonable by name/intent yet provided zero actual regression protection. Caught specifically by a reviewer manually tracing "does this assertion genuinely differ under the regression scenario (trim deleted)?" rather than trusting the test's name/intent — the load-bearing review question for regression guards. | done |
| T49 | front 오경보(직진 프레임을 left/right로 오판) 원인 규명 — 안전 관련 | P1 | — | **왜 P1인가:** 직진 중인 사용자에게 "왼쪽/오른쪽으로 이탈했다"는 허위 경고를 내보내는 실패임. 시각장애인 사용자를 실제로 위험한 방향으로 유도할 수 있어, right 미검출(전부 무판정=침묵이라 안전한 실패 양상)과는 성격이 다름. **측정된 현황 (2026-08-21, T1의 "사양 내/외 분리 재평가" 블록 참조):** 사양 내(실사용 조건) front 29장 중 3장이 편향으로 오판 = **10.3%**; 전체 88장 기준으로는 4/34 = 11.8%; 사양 외 프레임을 제외해도 거의 개선되지 않음 → 촬영 각도 문제가 아님. **사양 내 오경보 3건 (실사용 조건에서 발생, 우선 조사 대상):** `20251119_161750_001_saved.jpg` → `left`로 오판, `20251119_161750_015_saved.jpg` → `left`로 오판, `20260808_000211.jpg` → `right`로 오판. **사양 외 오경보 1건 (참고, 우선순위 낮음):** `20251119_131206_041_saved.jpg` → `right`로 오판. **중요 — 라벨 오류가 아님이 확인됨:** 위 4건 전부 2026-08-21 사용자가 정답 라벨을 `front`로 직접 확정함. 즉 라벨링 실수가 아니라 모델의 오분류이므로 **라벨 재검토로 시간 쓰지 말 것.** **관찰된 단서 (확인 필요, 단정 금지) —** 단서 1: 3건 중 2건(`20251119_161750_001`, `_015`)이 동일 촬영 세션의 연속 프레임임 → 특정 장면/장소가 체계적으로 혼동을 유발하는지 확인할 가치가 있음. **다만 이것이 원인이라고 단정할 근거는 아직 없음.** 단서 2: 4건 모두 파일명에 `_saved`가 붙은 배치이거나 야간 촬영임. front 클래스의 74%(245/332)가 `_saved` 단일 세션에서 나온 편중이 T44에 이미 기록되어 있음 — 연관 가능성은 있으나 **현재 데이터로는 편중 탓인지 그 장면이 단순히 어려운지 구분 불가(추정)**. **조사 방향 제안 (후보로만 기록, 하나로 확정하지 말 것):** (1) 4건의 softmax 확률 분포를 개별 출력해 front와 오판 클래스의 확률 차이가 얼마인지 확인 — 임계값 조정으로 해결 가능한 범위인지, 아니면 명백한 오분류인지 판별; (2) `_saved` 배치를 제외하고 재학습해 front 오경보율 변화 비교 (T44에 기록된 편중 확인 방법과 동일); (3) front에 대해서만 임계값을 올려 오경보를 줄이는 트레이드오프 검토 — 단 front recall이 떨어지면 "직진인데 침묵"이 늘어나므로 안전상 영향 재평가 필요. **완료 기준:** 원인이 (a) 임계값으로 해결 가능한 경계 사례인지 (b) 데이터 편중인지 (c) 특정 장면 유형의 근본적 혼동인지를 **근거와 함께** 판별하고, 그에 맞는 후속 조치를 별도 태스크로 제안할 것. 이 태스크 자체에서 코드를 고칠 필요는 없음. **착수 전 확정 판정 기준 및 실행 계획 (2026-08-21 기록, 실행은 미착수):** 이 계획은 **기록만 된 상태이며 아직 실행되지 않았음** — 아래 어떤 단계도 실행하지 않았고 새로 측정된 수치도 없음. 착수 시 아래 순서와 판정표를 그대로 따를 것이며, 표에 없는 케이스가 나오면 임의 판단하지 말고 행 추가를 먼저 물을 것. **선결 경고 — 표본이 결론을 내기에 부족함:** 사양 내 front 오경보 3/29 = 10.3%, Wilson 95% 신뢰구간 [3.6%, 26.4%] (T1의 "신뢰구간 산출" 블록 참조). 즉 참값이 4%일 수도 26%일 수도 있음. **이 표본으로는 "오경보율이 몇 %다"라고 말할 수 없고, 조사 결과 어떤 개선이 나와도 그 개선이 진짜인지 표본 노이즈인지 구분할 수 없음.** T44의 데이터 보강(left +223 / right +195, front도 사양 내 표본 확대 필요)이 선행되면 결론의 신뢰도가 올라감. 다만 아래 Step 1/2는 재학습 없이 저비용으로 실행 가능하므로 먼저 해볼 가치는 있음. **Step 1 — 확률 분포 덤프 (재학습 불필요, 저비용):** 무엇 = 오경보 4건의 softmax 확률을 개별 출력해 front 확률과 오판 클래스 확률의 차이를 측정. 대상 = 사양 내 3건(`20251119_161750_001_saved.jpg`→left, `20251119_161750_015_saved.jpg`→left, `20260808_000211.jpg`→right) + 사양 외 1건(`20251119_131206_041_saved.jpg`→right) + **대조군으로 정상 판정된 사양 내 front 24건**. 방법 = `train/eval_split_spec.py`에 확률 출력을 추가하거나 별도 진단 스크립트 작성; 전처리·임계값은 `train/eval_model.py`와 동일하게 유지할 것. **판정표 (착수 전 확정 — 이대로만 적용; 관측값 = 오판 클래스 확률 − front 확률):** (1) 0.10 미만 → 판정 (a) 임계값 경계 사례 → 후속: Step 2로 진행. (2) 0.10 이상 0.30 미만 → 판정 애매 → 후속: Step 2 먼저 실행 후 개선 없으면 Step 3. (3) 0.30 이상 → 판정 (c) 근본적 오분류 → 후속: Step 3으로 진행, 임계값 조정으로는 해결 불가. (4) 4건의 판정이 서로 엇갈림 → 판정 단일 원인 아님 → 후속: 건별로 위 기준을 적용하고 다수 유형을 우선 처리. **Step 2 — 임계값 스윕 (재학습 불필요, 저비용):** 무엇 = front 임계값을 현재 0.5에서 올려가며(0.55 / 0.60 / 0.65 / 0.70) **front 오경보율과 front recall을 동시에** 측정해 트레이드오프 곡선을 만듦. 방법 = `train/eval_model.py`의 `EVAL_FRONT_THRESHOLD` 환경변수 사용 — 재학습·코드수정 없이 일회성 실행 가능. 주의 = front 임계값을 올리면 오경보는 줄지만 **"직진인데 침묵"이 늘어남**; 침묵은 위험 방향 유도보다는 안전하나 사용자가 기기를 신뢰하지 못하게 만드는 별도 문제이므로 양쪽을 함께 보고할 것. **금지 — 이 단계에서 최적값을 스스로 고르지 말 것:** "front 오경보 허용치"가 미정이기 때문임(T1에 2026-07-17부터 미결로 기록됨). Step 2의 산출물은 **결정이 아니라 트레이드오프 표**이며, 이를 사용자에게 제시해 허용치를 정하게 할 것. **Step 3 — 데이터 편중 검증 (재학습 필요, 고비용):** 무엇 = 파일명에 `_saved`가 붙은 배치를 제외하고 재학습해 front 오경보율 변화를 비교. 근거 = front 332장 중 245장(74%)이 2025-11-19~20 단일 세션(`_saved`)에서 나왔고 다른 세 클래스에는 이 배치가 0장임(T44에 기록됨); 오경보 4건 중 3건이 이 배치 소속. **혼입 경고 (반드시 명시할 것)** = `_saved`를 빼면 front가 332 → 87장으로 급감함; 그 상태의 비교는 "편중 제거 효과"와 "데이터 급감 효과"가 섞여 **단독으로는 해석 불가**이므로, 대조군으로 다른 클래스도 동일 비율로 줄인 조건을 함께 돌리거나 최소한 이 혼입을 결론에 명시할 것. **완료 판정 (위 완료 기준의 구체화):** 원인이 (a) 임계값 경계 / (b) 데이터 편중 / (c) 장면 유형의 근본적 혼동 중 무엇인지 **위 판정표에 근거해** 결론짓고, 후속 조치를 별도 태스크로 제안. 단, 신뢰구간이 넓다는 선결 경고를 결론에 반드시 병기할 것. 이 태스크 자체에서 앱 코드를 고치지 않음. status는 `todo` 유지 — 계획 기록만 완료했고 실행은 미착수. **⛔ 오경보 실재 확정 (2026-08-22) — 소표본 착시 아님:** 2026-08-21 기록 시점의 근거는 사양 내 front 29장 중 3건(10.3%)으로, Wilson CI가 [3.6%, 26.4%]로 매우 넓어 "표본 노이즈일 수 있다"는 선결 경고를 달아두었음. 2026-08-22 누수 없는 세션 k-fold 측정(T1의 "데이터 누수 발견 및 누수 없는 재측정 (2026-08-22)" 블록 참조)으로 이 경고가 해소됨 — front 오경보 **10.1%(27/268)**, 클러스터 부트스트랩 95% CI **[4.3%, 17.4%]**; 하한 4.3%가 0을 명확히 배제 → **실재하는 문제로 확정**; 점추정이 이전 10.3%와 거의 동일하여 이전 관측이 우연이 아니었음이 확인됨. 다만 크기는 4~17%로 아직 정밀하지 않음 — "정확히 몇 %"를 말하려면 세션 다양성 확대가 필요(T44 참조). **T49 우선순위 근거 강화:** 오경보가 실재하고 10% 내외이므로 P1 유지가 타당함. **Step 1 실행 비용 절감 —** Step 1(확률 덤프)은 이제 재학습이 불필요함. `train/groupkfold_out/all_probs.json`에 **843장 전체의 누수 없는 softmax 확률이 이미 저장되어 있음**(파일명·정답라벨·세션·중복클러스터 포함). 이 파일에서 오경보 사례를 뽑아 front 확률과 오판 클래스 확률의 차이를 바로 계산하면 됨. 기존 판정표(0.10/0.30 경계)를 그대로 적용할 것. **이전 기록 정정 —** 2026-08-21 기록의 "사양 내 오경보 3건" 중 `20251119_161750_001_saved.jpg`와 `20251119_161750_015_saved.jpg`는 같은 세션의 연사 프레임으로 시각적으로 거의 동일함. **구별되는 실패 장면은 3건이 아니라 2건에 가까움.** 조사 시 이 둘을 별개 사례로 세지 말 것. status는 `todo` 유지. | todo |
| T50 | 5-class 재라벨링 (843장 전체를 확정 정의로 재분류) | P0 | — (착수 가능. T1·T49의 선행 조건이므로 이 둘보다 먼저 처리) | BACKGROUND (2026-08-22 사용자 결정): 기존 `front` 클래스가 성격이 다른 두 상황을 섞고 있었음이 통계적으로 확인됨 — 오경보 27건 중 12건(44.4%)이 "인도 위에서 촬영", 정상 판정 대조군 27건 중 1건(3.7%)만 인도 위, 오즈비 20.8, Fisher 정확검정 양측 p=0.00088. 세션 군집을 보수적으로 반영해도(오경보군 6/13 가정) p=0.0026으로 결론 유지. 원인: 인도에서 본 횡단보도는 카메라 축과 횡단보도 축이 어긋나 기하학적으로 실제 이탈과 구별되지 않음 — 모델 결함이 아니라 학습 데이터가 모순된 것을 가르치고 있었음. 확정된 5-class 정의(`front`/`left`/`right`/`approach`/`none`)와 판정 기준은 `docs/PRD.md` "§클래스 정의 (2026-08-22 사용자 확정)" 절이 권위 값. SCOPE: (1) 843장 전체를 그 확정 정의에 맞춰 재분류하고, 특히 기존 `front` 332장에서 `approach`를 분리한다. (2) `left`/`right`도 점검 대상이다 — 인도에서 비스듬히 찍은 것이 섞여 있는지 아직 확인되지 않았음. front에서 확인된 혼재가 left/right에도 있을 가능성이 높다(추정 — 확인 방법: 동일 판정 기준으로 left/right 전수 재판정). (3) 작업 단위는 이미지가 아니라 촬영 세션이다 — EXIF 기준 60초 간격으로 나눈 147개 세션이 있고 그중 front 포함 세션은 62개. 세션 대표 이미지로 1차 판정한 뒤, 세션 내에서 자세가 바뀐 경우만 펼쳐서 개별 판정한다. 참고 자료(이미 생성됨): `train/sheets/front_sessions0.jpg`, `train/sheets/front_sessions1.jpg` (front 포함 62개 세션 대표 이미지). ACCEPTANCE: 843장 전부가 5개 클래스 중 하나에 배정되고, 각 배정 근거가 PRD의 판정 기준(줄무늬가 화면 아래에서 시작하면 위에 있음 / 앞쪽에 떨어져 있고 사이에 인도·연석이 보이면 `approach`)으로 설명 가능할 것. | done |
| T51 | 앱·학습 코드 5-class 대응 | P0 | T50 | ⚠ **최우선 위험 — 라벨 인덱스 전면 이동 (에러 없이 조용히 틀림):** torchvision `ImageFolder`는 폴더명을 알파벳순으로 정렬해 인덱스를 부여하므로 `approach` 추가 시 순서가 전부 밀린다. 현재(4-class) front=0, left=1, none=2, right=3 → 변경 후(5-class) **approach=0, front=1, left=2, none=3, right=4**. `crosswalk_app/lib/services/classifier.dart:28`의 `_labels`를 함께 고치지 않으면 로짓이 엉뚱한 라벨에 매핑되며 에러 없이 조용히 오작동한다. 이 프로젝트에서 이미 한 번 겪은 함정임(T42 기록 참조). SCOPE — 수정 대상 전수 확인 필요: `crosswalk_app/lib/services/classifier.dart` (`_labels`를 5원소로, 순서 `['none','approach','front','left','right']`; `approach` 임계값 신설 필요 여부 판단); `crosswalk_app/lib/services/feedback_service.dart` (`approach`도 침묵 처리 — 현재 front/none이 침묵. **이 파일은 안전상 최중요 파일이므로 최소 변경 원칙을 지킬 것**); `crosswalk_app/lib/localization/app_strings.dart` (`approach` 라벨 문구 ko/en); `crosswalk_app/lib/screens/camera_screen.dart` (색상·아이콘 매핑, 오버레이 표시 조건); `train/train_model.py` (`CLASSES` 5개로); `train/export_onnx.py` (`NUM_CLASSES = 5`); `train/eval_model.py`·`train/groupkfold_cv.py`·`train/eval_split_spec.py` (`LABELS`/`CLASSES` 순서); 테스트 파일들 (4원소 로짓을 쓰는 곳 전수 수정). ACCEPTANCE: 위 대상이 모두 5-class로 정렬되고, **필수 검증 단계** — 학습 로그의 `class_to_idx` 출력값과 `classifier.dart`의 `_labels` 순서를 직접 대조하고 그 근거(출력 원문과 파일:줄)를 보고에 포함할 것. 눈으로 확인했다는 서술만으로는 불충분함. 앱 동작은 이번 변경으로 바뀌지 않아야 한다(`approach` = 침묵). | done | DONE (2026-08-23): PR #68로 병합(master `b860db3`), develop 동기화 완료 (`git diff origin/master origin/develop --quiet` exit 0). 정정 — 이 행에 원래 적혀 있던 인덱스 순서 `['approach','front','left','none','right']`는 **측정 전 예측값이었고 틀렸다.** 사용자가 폴더명에 숫자 접두사(`0_none`~`4_right`)를 붙였으므로 알파벳순 정렬 결과가 곧 의미 순서가 된다. 실측 `ImageFolder('image').class_to_idx == {'0_none':0,'1_approach':1,'2_front':2,'3_left':3,'4_right':4}` 기준으로 위 표기를 정정했다. 예측을 사실처럼 적어둔 것이 이 프로젝트에서 라벨 순서 사고를 반복시킨 원인이라, T54의 자동 검증 장치를 함께 도입했다.

## C. Ready — performance
| ID | Task | Priority | Depends on | Acceptance Criteria | Status |
|---|---|---|---|---|---|
| T12 | Define performance targets (FPS, latency, battery, min device) | P1 | Open Q #11 partially answered (2026-07-24) | Targets in PRD. PARTIALLY UNBLOCKED (2026-07-24): Open Q #11 answered for 2 of 4 sub-items — min device tier = 기존 minSdk 기준 그대로(Android 8.0/minSdk26, iOS15, 저사양 포함, Open Q #2와 동일); battery budget = 1시간 연속 사용 시 10% 이내. min FPS / alert latency는 사용자가 "선호 없음"으로 답해 여전히 open — 기존 방침대로 실기기 테스트 후 결정, recorded in `docs/PRD.md` Open Q #11. T12를 `done`으로 닫으려면 FPS/지연시간 수치가 추가로 필요. | todo |
| T13 | Investigate moving YUV→RGB + inference off UI isolate | P2 | T12 | Measured frame-drop/latency improvement vs baseline. Gap: `_onFrame` runs sync on UI thread (`camera_screen.dart:132-148`) | todo |

## D. Documentation sync (docs agent)
| ID | Task | Priority | Depends on | Acceptance Criteria | Status |
|---|---|---|---|---|---|
| T14 | Fix `ARCHITECTURE.md` drift: throttle 10→5, threshold 0.70→0.85/0.55, init order, remove `export_onnx.py`, build command `--no-shrink`→`flutter build apk --release` | P1 | — | Doc matches code (see PRD "Documentation Drift"). Gap: `ARCHITECTURE.md` states `--no-shrink` but CI runs `flutter build apk --release` (`.github/workflows/build_apk.yml:58`). DONE: fix landed in commit `ac64abc` on `develop` directly (pure documentation — no code/test to CI-verify, so no PR). Hybrid approach: added a prominent legacy/superseded pointer at the top of the root `ARCHITECTURE.md` (confirmed present, `ARCHITECTURE.md:5-9`) directing readers to `docs/Architecture.md` as the authoritative/actively-maintained doc (per the `README.md` Documentation Index convention), then corrected every objectively false claim IN PLACE rather than leaving stale numbers that could be copy-pasted. Scope ended up a strict SUPERSET of T14's originally-listed items: the doc had drifted far beyond that narrow list (an entire session's fixes — T9/T10/T11/T21/T23–T28 — post-dated it and weren't reflected). Corrected items: (a) init order rewritten to match `camera_screen.dart:54-140` — TTS → permission request with ordinary/permanently-denied branching (T27) → `Classifier.init()` with `ModelIntegrityException` handling → camera setup, plus the `_isInitializing` re-entrancy guard (T23) and try/finally note; (b) Classifier constants: single `_confidenceThreshold:0.70` → actual asymmetric `_deviationThreshold=0.55`/`_frontThreshold=0.85`, and `_throttleFrames` 10→5 (throttling section ~3fps→~6fps); (c) added the previously-missing softmax step before smoothing (T21, raw output is logits not probabilities); (d) `export_onnx.py` IR-version claim corrected 8→7 (T22-verified actual), cross-referenced to `docs/Architecture.md` §11.1/§11.2 as authoritative; (e) CI section rewritten from a stale 8-step list to the actual current steps, adding the previously-unmentioned `flutter test` step (T9) and the APK 서명 (zipalign/apksigner) step, and removing the nonexistent `--no-shrink` flag claim (build command → `flutter build apk --release`); (f) new subsections added — model integrity check (SHA-256, `_verifyModelIntegrity`), session/env lifecycle (`_envInitialized`, release-before-reinit T24, smoothing-state reset T26), TTS `stop()`-before-`speak()` (T25), permanently-denied→`openAppSettings()` flow (T27), release-signing guard in `build.gradle.kts` (T28); (g) dependency table gained `crypto` (used by the SHA-256 integrity check). Scope discipline: did NOT touch `docs/Architecture.md`, `docs/PRD.md`, `docs/Tasks.md`, `docs/CHANGELOG.md`, or any code. This closes T14 fully as scoped — nothing in the original acceptance criteria (throttle, thresholds, init order, export_onnx.py, `--no-shrink`→`flutter build apk --release`) was left unaddressed. VERIFIED: `git show ac64abc --stat` run directly — confirms exactly one file changed, `ARCHITECTURE.md | 111 +++++++++++++++++++++++++++++++++++++++++---------------`, 1 file changed, 82 insertions(+), 29 deletions(-). No other file touched by this commit. | done |
| T15 | Fill `CLAUDE.md` project overview + verified build/test/run commands | P2 | — | Placeholders replaced with real values. DONE: filled in commit `ff59d89` on `develop` directly (pure documentation — merged via PR #14 = `93d052d` alongside T14's work, CI passed). Scope confirmed limited to `CLAUDE.md` only (did NOT touch `## Prohibitions`, `## Report Template`, `## Agent Workflow`, nor `docs/PRD.md`, `docs/Architecture.md`, `README.md`, `AGENTS.md`, or any code). Filled: (a) Project Overview — Name `crosswalk_app` (cited `pubspec.yaml:1`), Goal (cited `docs/PRD.md` Goal section + `pubspec.yaml:2` + `feedback_service.dart:30-38`), Stack (Flutter/Dart SDK constraint + key packages with versions cited `pubspec.yaml:10-20`, rationale pointer to `docs/Architecture.md` §2); (b) Verified Commands table. HONEST-NON-FABRICATION DISCIPLINE (this project's own CLAUDE.md rule "no guesses stated as facts", followed by the agent that filled CLAUDE.md itself): Build (`flutter build apk --release`) and Test (`flutter test`) were marked CI-only-verified with real GitHub Actions run citations / `docs/Tasks.md` rows, explicitly noting NEITHER has a fully-successful standalone LOCAL verification this session (Test fails locally via the documented `flutter_tester.exe` crash; Build fails locally by design per T28's signing guard, and even with the opt-in flag hit the separate unresolved native cmake/NDK issue) — NO invented local-success claim. Run (device/emulator execution) was explicitly left marked "not yet verified — no device/emulator used this session" rather than filled with a guessed command. Added note: `flutter analyze` IS verified locally multiple times this session and `flutter pub get` is implicitly verified as a prerequisite. VERIFIED: `git show ff59d89 --stat` run directly — confirms exactly one file changed, `CLAUDE.md | 14 ++++++++------`, 1 file changed, 8 insertions(+), 6 deletions(-). No other file touched by this commit. | done |
| T22 | Trace/document actual ONNX export path from `train/` to shipped `crosswalk_model.onnx` | P1 | — | Gap (ORIGINAL PREMISE CORRECTED): the original row claimed "no `export_onnx.py` found in repo" — this was FACTUALLY WRONG (premise-correction in the same spirit as T25). `train/export_onnx.py` genuinely exists (25 lines) with real git history; the false claim came from an earlier agent pass that never actually listed `train/`'s contents. TRUE picture: there are TWO separate ONNX export paths — (1) `train/train_model.py`'s embedded `export_onnx()` (lines 217-232, `opset_version=17`, runs immediately after training as pipeline step [6]); (2) the standalone `train/export_onnx.py` script which does NOT retrain — it loads an already-saved `.pt` checkpoint and re-exports with DIFFERENT settings (`opset_version=12`, `dynamo=False`). Document (or restore) the real export path and its opset/IR settings so the model can be reproduced. DONE: two-script export flow documented in `docs/Architecture.md` §11.1 (flow table) with reproducibility check in §11.2 (doc commit `1cdbe8d`). The ACTUAL currently-shipped `crosswalk_app/assets/model/crosswalk_model.onnx` was verified directly via Python `onnx.load()` (not assumption): `ir_version: 7`, `opset imports: [('', 12)]` — this exactly matches `train/export_onnx.py`'s `opset_version=12`, confirming THAT script (not `train_model.py`'s embedded opset-17 exporter) produced the shipped model. (Minor pre-existing note: `export_onnx.py`'s own code comment claims "IR version 8" but the verified actual value is 7 — a harmless comment inaccuracy in the script, not requiring a fix.) Git history: commit `30a799e` introduced the initial ONNX migration; commit `3153d50` ("ONNX 모델 IR version 다운그레이드 (10→7)") modified `train/export_onnx.py` specifically to fix an IR-version-too-high problem for mobile onnxruntime compatibility and replaced the shipped `.onnx` binary in the same commit. Reproducibility check (docs agent): training images, prepared data splits, the `.pt` checkpoint, and the `.onnx`+`.onnx.data` asset are ALL committed to the repo (not missing) — but BOTH `train_model.py` and `export_onnx.py` hardcode absolute Windows paths (`C:\crossWalk\...`) not matching this repo's clone location, a portability gap (scripts can't run as-is), separate from a missing-data concern. Architecture.md Open Question B updated "untracked"→"RESOLVED"; `docs/PRD.md` Documentation Drift section corrected to drop the false "absent from `train/`" claim and point to Architecture §11.1/§11.2; `docs/CHANGELOG.md` entry added. Follow-up for the path-portability gap tracked as T31. | done |
| T31 | De-hardcode `C:\crossWalk\...` absolute paths in training scripts so the pipeline is runnable from a fresh clone | P3 | — | Replace hardcoded `C:\crossWalk\...` absolute paths in `train/train_model.py` and `train/export_onnx.py` with relative paths (e.g. relative to the script's own location, or a configurable env var/CLI arg) so the training pipeline is runnable from a fresh clone without manual editing. Currently these scripts cannot be run as-is by anyone who clones the repo without first manually editing multiple hardcoded path constants (a training/reproducibility dev-experience gap, NOT a safety/correctness issue in the shipped app). Evidence: `train/train_model.py:19-22`, `train/export_onnx.py:5-6` — both hardcode `C:\crossWalk\...` (e.g. `C:\crossWalk\image`, `C:\crossWalk\train\data_prepared`, `C:\crossWalk\model\crosswalk_model.pt`). Found during T22's documentation pass (docs agent, commit `1cdbe8d`). DONE: fix landed in commit `cac4e13` (branch `fix/T31-training-script-paths`), merged to master as PR #16 (`d59f1fc`), synced into develop. Both `train/train_model.py` and `train/export_onnx.py` now compute `REPO_ROOT = Path(__file__).resolve().parent.parent` and derive all path constants relative to it via `pathlib`, replacing the hardcoded `C:\crossWalk\...` strings: `train_model.py` derives `DATA_DIR`/`PREPARED_DIR`/`MODEL_OUT`/`ONNX_OUT` plus new `CONFUSION_MATRIX_OUT`/`TFLITE_OUT_DIR`; `export_onnx.py` derives `MODEL_PT`/`ONNX_OUT`. No pipeline logic changed — verified byte-identical outside the changed hunks (split ratios, `FRONT_SAMPLE`, `IMG_SIZE`, `BATCH_SIZE`, epoch counts, learning rates, class weights, opset/dynamo export settings all unchanged). Independent reviewer verification via ACTUAL Python execution (not static reading): ran `import train_model` in a real Python 3.13.2 + torch 2.12.0 environment in this exact repo checkout and directly observed `REPO_ROOT` resolve to `C:\vibecoding\crossWalk` — specifically confirming `.parent.parent` (two levels up from `train/train_model.py`) is NOT an off-by-one error (a single `.parent` would have wrongly landed on `train/` itself); also directly verified `DATA_DIR`/`PREPARED_DIR`/`MODEL_OUT`/`ONNX_OUT`/`CONFUSION_MATRIX_OUT`/`TFLITE_OUT_DIR` all resolved to correct real paths, with `type(train_model.DATA_DIR)` confirmed as `WindowsPath`. For `export_onnx.py`, the reviewer verified the equivalent `REPO_ROOT` logic via an ISOLATED snippet rather than importing the file directly — a deliberate, correct caution because `export_onnx.py` lacks a `__main__` guard, so importing it would trigger a real ONNX export that could overwrite the committed `model/crosswalk_model.onnx`; confirmed `REPO_ROOT` resolves correctly and `MODEL_PT.exists() == True` (points at the real committed checkpoint). Reviewer also ran actual reproduction checks confirming Path-object compatibility with `os.path.join`, `torch.save`/`torch.load`, `torch.onnx.export`, and `plt.savefig` (executed, not assumed), and confirmed no raw-string `+` concatenation with a Path object exists anywhere in either file (which would have raised `TypeError`). Blast radius confirmed = exactly `train/train_model.py` + `train/export_onnx.py`; `train/convert_to_tflite.py` deliberately left untouched (same hardcoded-path issue, but documented as unused/dead code at `docs/Architecture.md:80`, out of T31's scope — reviewer confirmed empty diff against it and verified the "documented as unused" justification directly, not on trust). CI: GitHub Actions run 29551441763 (PR #16) passed — but honestly noted as an INDIRECT/passive "nothing broke" signal only: this repo has NO CI coverage for Python scripts under `train/` (CI covers the Flutter app build/test), so this is not direct verification of the Python fix (no automated test exists or is feasible without a Python CI harness that doesn't currently exist — an inherent, accepted limitation of this task type). Minor caveat (informational, not a defect): `train/requirements.txt` pins `torch>=2.3.0` as a floor, but Path-compatibility was directly verified on the reviewer's actual torch 2.12.0, not independently reproduced on the exact 2.3.0 floor (low risk — PathLike support in these APIs predates 2.3.0 by years). | done |
| T30 | Document/persist local-dev opt-in for `flutter run --release` after T28's signing guard | P3 | T28 | Side-effect surfaced by reviewer during T28's review (PR #11): T28's guard makes `flutter run --release` ALSO fail locally with the same `GradleException`, because it invokes the same Gradle `release` build type as `flutter build apk --release` but Flutter's CLI has no documented way to pass arbitrary `-P` Gradle project properties (unlike `flutter build apk`, where `-PallowDebugSigningForRelease=true` can be appended). Reviewer confirmed `crosswalk_app/android/gradle.properties` does NOT set `allowDebugSigningForRelease`, so there is no persisted opt-in reachable from `flutter run` — ironically the exact use case the original debug-signing fallback comment referenced ("so `flutter run --release` works"). Acceptance: EITHER (a) add `allowDebugSigningForRelease=true` to `crosswalk_app/android/gradle.properties` with a comment explaining it is for local `flutter run --release` only and must NEVER be relied on for a real distributed build, OR (b) document in README / a code comment near the T28 guard how a developer can locally persist this opt-in. Evidence gap: this is an INFERENCE from Gradle/Flutter build-type semantics plus the confirmed absence of any `flutter run`-reachable opt-in mechanism (reviewer, PR #11) — NOT a directly reproduced failure (no device/emulator was available during the T28 review to reproduce `flutter run --release` end-to-end). DONE: closed via option (b) in commit `5ccb598` on `develop` directly (pure documentation — no code/CI to verify, so no PR), extending `README.md`'s existing "Build & Run" notes section. JUDGMENT CALL — option (a) DELIBERATELY REJECTED as literally worded: the acceptance criteria offered (a) add `allowDebugSigningForRelease=true` directly to `crosswalk_app/android/gradle.properties`, or (b) document elsewhere how to persist the opt-in. The main session verified BEFORE dispatching the docs agent (via `git ls-files`) that `crosswalk_app/android/gradle.properties` IS tracked/committed in this repo — so choosing (a) literally would set the opt-in to `true` for EVERY clone/developer permanently, silently reintroducing exactly the debug-signing risk T28 exists to prevent (T28 makes a bare local `flutter build apk --release` fail LOUDLY instead of silently debug-signing). (a)-as-written would defeat T28's entire purpose, so (b) was implemented instead. WHAT WAS WRITTEN (verified NOT to touch `crosswalk_app/android/gradle.properties` or any code file): (i) explained BOTH `flutter build apk --release` AND `flutter run --release` fail locally by design without an opt-in, citing `build.gradle.kts:33-55`'s guard logic; (ii) for `flutter build apk --release`: documented the direct CLI flag `-PallowDebugSigningForRelease=true` (pre-existing from T28, cross-referenced here); (iii) for `flutter run --release` (the actual T30 gap — Flutter's CLI has no way to forward `-P` flags to `flutter run`): the fix is to add `allowDebugSigningForRelease=true` to the developer's OWN personal, per-machine, NEVER-committed Gradle file — `%USERPROFILE%\.gradle\gradle.properties` (Windows) / `~/.gradle/gradle.properties` (GRADLE_USER_HOME), explicitly NOT the project's committed `crosswalk_app/android/gradle.properties`. The Gradle property-file precedence claim (user-home `gradle.properties` merges into/overrides every build on that machine, taking precedence over project-level) was verified against Gradle's OWN official docs (https://docs.gradle.org/current/userguide/build_environment.html, fetched via WebFetch), not assumed; (iv) added an explicit WARNING that setting this in the personal file makes EVERY Gradle release-type build on that machine debug-signed by default (not just this project), so it must NEVER be relied on for actual distribution — only for local `flutter run --release` testing. SCOPE DISCIPLINE: `crosswalk_app/android/gradle.properties` and all code files were left UNTOUCHED. Optional docs agent suggestion (a future code comment near the T28 guard in `build.gradle.kts` pointing to this same guidance) judged NOT worth a separate task — it is a low-priority documentation cross-reference, not a functional gap. VERIFIED: `git show 5ccb598 --stat` run directly — confirms exactly one file changed, `README.md | 5 +++++`, 1 file changed, 5 insertions(+), 0 deletions. No other file touched by this commit. | done |

## E. Deferred / decision-gated features
| ID | Task | Priority | Depends on | Acceptance Criteria | Status |
|---|---|---|---|---|---|
| T16 | Native Semantics / screen-reader compatibility | P2 | T3 | Meets chosen standard. UNBLOCKED (2026-07-24): T3's Open Q #5 now answered — target = TalkBack/VoiceOver 실사용 호환(공식 WCAG 인증 없음). 실제 Semantics 위젯 적용 등 구현은 아직 todo. | todo |
| T17 | Multi-language TTS | P3 | T4 DONE (2026-07-17; language list fixed = 한국어 + 영어) | Languages selectable/auto. FULLY UNBLOCKED (2026-07-17): Open Q #6 answered — multilingual required — and T4 now fixes the language list = 한국어 (Korean) + 영어 (English), so this is no longer blocked on an unenumerated list. Scope: TTS must support both Korean and English (concrete locale codes/voices are implementer decisions). Related to broader localization task T34. REOPENED → todo (2026-07-17, user decision): the reviewer flagged that the "selectable/auto" acceptance wording was ambiguous, and the user was asked directly. The user explicitly answered that auto-detection ALONE is insufficient — a manual language-selection UI must exist for T17 to count as complete ("수동 선택 UI까지 있어야 완료 — T17을 다시 todo로"). Therefore: the "auto" half already delivered by T34 (system-locale auto-detection) remains valid and is NOT reverted, but the "selectable" half (a manual in-app language picker UI) is still missing, so T17 is returned to todo. Only the manual-selection-UI portion remains outstanding; the auto path is done. The prior DONE note below is retained for history but is superseded by this decision. DONE (2026-07-17): satisfied as part of T34's implementation (see T34 row). TTS language is no longer hardcoded to ko-KR — `crosswalk_app/lib/services/feedback_service.dart` now sets the TTS language from `AppStrings`/`ttsLocaleCode()` (`crosswalk_app/lib/localization/app_strings.dart`), which resolves to Korean or English from the detected system locale (Korean fallback on unsupported/undetected locale). This fully meets T17's stated scope ("TTS must support both Korean and English") and the "auto" half of the "selectable/auto" acceptance. Merged via PR #30 (see T34), CI run 29586552329 "35 tests passed" (includes `crosswalk_app/test/app_strings_test.dart`, which covers `ttsLocaleCode()`). CONSTRAINT (same as T34): only AUTO language selection (system-locale detection) is implemented — the "selectable" (manual in-app language picker UI) half of the acceptance is NOT built here; that overlaps with the settings/onboarding scope of T19/T36. Marked done because T17's explicit scope (Korean + English TTS) plus the auto path is met; if a manual TTS language picker is later required, track it under T19/T36 rather than reopening T17. CROSS-REF (2026-07-18): the outstanding "selectable" (manual language-picker UI) half is now being implemented under T39 (SettingsScreen) — see Section F. T17's own status is intentionally left unchanged here; it will be updated after T39 lands. DONE (2026-07-18): T39 landed (see T39 row, Section F — PR #37, master `7c202ad`, develop `7681edc`) and delivers exactly the manual language-selection UI whose absence kept T17 open. JUDGMENT (verified against actual source, not the dispatching report alone): the "selectable" half is now fully met for T17's stated scope. (a) TTS side (T17's core — "Multi-language TTS"): the SettingsScreen `SegmentedButton<AppLanguage>` (`crosswalk_app/lib/screens/settings_screen.dart:103-124`) calls `feedback.updateLanguage(language)` on selection (`settings_screen.dart:60`), which sets `_tts.setLanguage(ttsLocaleCode(language))` on the LIVE shared FeedbackService (`crosswalk_app/lib/services/feedback_service.dart:123-130`), so a user can now manually switch the TTS voice language ko↔en — satisfying the reopening criterion "수동 선택 UI까지 있어야 완료". (b) On-screen text also switches: selection additionally fires `onLanguageChanged` → CameraScreen `_onLanguageChanged`, which `setState`s `_language`/`_strings` (`crosswalk_app/lib/screens/camera_screen.dart:265-271`), so the AppStrings-based UI text (delivered under T34) re-renders in the chosen language too — i.e. "selectable" now covers BOTH TTS and screen text, not just one. The previously-delivered "auto" half (system-locale detection via T34) is unchanged and complements this. Both halves of T17's "selectable/auto" acceptance are therefore satisfied. Regression coverage for the selection path rides on T39's 8 new tests (CI run 29627878912, "47 tests passed"), including `crosswalk_app/test/settings_screen_test.dart`'s language-select test and the `updateLanguage` runtime-setter tests in `feedback_service_test.dart`. Status corrected `todo`→`done` on the basis of T39 landing; no separate T17-specific code change was needed. | done |
| T18 | Optional periodic "on-track" reassurance feedback | P3 | Open Q #13 answered (2026-07-17) | Configurable behavior. DECISION RESOLVED (2026-07-17): Open Q #13 answered — stay SILENT on front (keep current behavior); no on-track reassurance requested. This feature is therefore NOT wanted for now — task parked as won't-do unless the decision is revisited. No code change needed. | todo |
| T19 | Onboarding / phone-posture guidance screen | P3 | Open Q #8,#14 answered (2026-07-17) | Guidance flow present. UNBLOCKED (2026-07-17): Open Q #8 answered — chest-mount (lanyard/chest-mount) posture; Open Q #14 answered — rear camera only. Guidance content is now defined enough to build. NOTE: overlaps with T36 (onboarding screen + first-launch disclaimer) — both need a first-launch/onboarding flow; consider building one onboarding flow that carries BOTH the disclaimer (T36) and the chest-mount posture guidance (T19) rather than two separate screens. CROSS-REF (2026-07-18): implementation now proceeds under T40 (OnboardingScreen — see Section F), which integrates this posture guidance together with T36's disclaimer into a single first-run screen as suggested above. T19's own status is intentionally left unchanged here; it will be updated after T40 lands. DONE (2026-07-18): T40 landed (see T40 row, Section F — PR #39, master `08e7bae..a06557a`, develop `c686e91`) and delivers exactly the chest-mount posture-guidance flow that satisfies T19's "Guidance flow present" acceptance. The posture guidance appears on `OnboardingScreen` (`crosswalk_app/lib/screens/onboarding_screen.dart`) before `CameraScreen`, is read aloud via TTS on entry (for the visually-impaired target user), and is confirmed with a "확인했습니다" button. Regression coverage rides on T40's 4 new onboarding tests (CI run 29631590513, "54 tests passed"). CONSTRAINT (do NOT overstate — same persistence caveat as T40/T36): the guidance is shown on EVERY launch, NOT first-launch-only (a first-launch-only flag would need a new persistence package, e.g. `shared_preferences`, which was out of T40's scope). If posture guidance should appear only on the first run, that requires a separate decision + package approval. Status corrected `todo`→`done` on the basis of T40 landing; no separate T19-specific code change was needed. | done |
| T20 | Remove or repurpose unused `crosswalk_app_scaffold/` | P3 | — | Dir removed or documented. Evidence: default counter app (`crosswalk_app_scaffold/lib/main.dart`). DONE (2026-07-17): the `crosswalk_app_scaffold/` directory was deleted from the local filesystem — reviewer verified directly (`ls crosswalk_app_scaffold` → "No such file or directory"). NO COMMIT/PR EXISTS and none is expected: this directory was listed in `.gitignore:18` (`crosswalk_app_scaffold/`) and was NEVER tracked by git (`git log --all -- crosswalk_app_scaffold` returns nothing; `git show HEAD:crosswalk_app_scaffold/lib/main.dart` → does not exist in HEAD), so the deletion produced ZERO git diff — it is a pure local-filesystem cleanup, not a committed change. Blast-radius check (reviewer, grep): neither `crosswalk_app/` (main app) code nor `.github/workflows/build_apk.yml` (CI) references this directory anywhere. `setup_flutter.ps1:35` references `$env:TEMP\crosswalk_app_scaffold` (a temp-folder path), but that is an unrelated separate path — a false-positive, not this repo-root folder. HONEST LIMITATION (not overstated): `flutter analyze` was NOT re-run in this review because this session had no `flutter` on PATH; since the deletion produced no git diff to any analyzed file, no analysis impact is reasonably estimated, but this is an inference, not a re-executed verification. `docs/PRD.md` "Out of Scope" updated to reflect the directory no longer exists (`docs/PRD.md:62`). | done |
| T29 | Fix or explicitly document-as-dead the dormant BGRA (iOS) preprocessing buffer-offset bug before any iOS work | P2 | Open Q #1 answered (2026-07-17: iOS IS in scope) → now a real prerequisite for T33; Architecture Open Q C | `classifier.dart:119-125` passes `image.planes[0].bytes.buffer` (whole underlying ByteBuffer) instead of a properly offset view — misaligns pixels if `offsetInBytes` ≠ 0 or row padding ≠ width*4. Currently unreachable (camera forced to yuv420 at `camera_screen.dart:99`, CI Android-only) but must be fixed or documented dead before iOS work starts. Acceptance: path fixed with offset-correct view, or explicitly marked dead in code + Architecture Open Q C. DONE (2026-07-17): fixed at the CODE LEVEL (not merely documented dead) so the BGRA path is offset/stride-correct ahead of iOS work — merged to master via PR #23 (`fix/T29-bgra-offset-stride` → `master`, squash-merge, master `85404bd..ceed998`) and synced into `develop` (current branch HEAD `7d91634`; reported `git diff origin/master origin/develop --stat` = 0 lines). Fix: `crosswalk_app/lib/services/classifier.dart` now passes `bytesOffset: image.planes[0].bytes.offsetInBytes` and `rowStride: image.planes[0].bytesPerRow` to `img.Image.fromBytes` (previously passed the whole underlying `.buffer` with no offset/stride, misaligning pixels whenever `offsetInBytes` ≠ 0 or `bytesPerRow` > `width*4`); logic extracted into a behavior-preserving `@visibleForTesting Classifier.convertBGRA8888()` (`classifier.dart:221-226`, verified present in working tree). Regression coverage: 3 new tests in `crosswalk_app/test/classifier_test.dart` (`classifier_test.dart:246-291`, group "Classifier.convertBGRA8888 — buffer offset/row-stride (T29)") exercising non-zero `bytesOffset`, `rowStride` > `width*4`, and both combined (all three verified present in working tree). Doc sync: `docs/Architecture.md` Open Question C set to **RESOLVED (T29)** (`Architecture.md:308`); `docs/CHANGELOG.md` T29 Fixed entry added (`CHANGELOG.md:20`) — both verified present. Independent reviewer traced correctness against the ACTUAL `image` 4.8.0 source (`bytesOffset`/`rowStride` params exist for exactly this case) and `camera_platform_interface` 2.12.0 source, PASS (no bug, no scope creep, no new lint). Regression evidence: CI run 29575803053 (GitHub Actions, PR #23) — "Flutter 테스트" step logged "26 tests passed" including the 3 new BGRA tests (per task-giver's direct log read; CI run number recorded from the dispatching report, not independently re-fetched this session — this session verified the code/test/doc artifacts directly in the `develop` working tree instead). CONSTRAINT (do NOT overstate): this path has NEVER been executed on a real iOS device or simulator — the BGRA branch is still dead code on this repo's Android-only CI (camera forced to `yuv420`, `camera_screen.dart:99`). "done" here means "code-level bug fixed + unit-test regression guard in place, correctness established by package source-tracing"; it does NOT mean "verified working on iOS". Real-device iOS execution verification remains outstanding and is tracked under T33 (iOS build pipeline). | done |
| T33 | Build iOS build/signing pipeline | P2 | Open Q #1 answered (2026-07-17: iOS in scope); Open Q #2 answered (2026-07-17: iOS min = iOS 15); ~~T29 (BGRA path)~~ T29 DONE (2026-07-17, code-level fix + unit tests — see T29 row) | PAUSED (user decision, 2026-07-17): the user confirmed this development environment (Windows) makes iOS builds physically impossible locally, reviewed the option of a GitHub Actions macOS-runner CI pipeline as an alternative, and decided NOT to proceed with T33 for now — it is deferred/held. Do not pick this task up until the user explicitly resumes it. Prerequisites are otherwise resolved: iOS is in scope (Open Q #1) and the iOS minimum version is now decided as iOS 15 (Open Q #2) — so when T33 resumes, the iOS deployment target to set is iOS 15. T29 prerequisite is SATISFIED at the code level — the BGRA preprocessing offset/stride bug is fixed and unit-test-guarded (T29, PR #23) — but neither the BGRA path nor the app end to end has ever run on a real iOS device/simulator (T29 was validated by package source-tracing + unit tests only, on Android-only CI); that real-iOS-execution verification is part of this task and therefore also deferred while T33 is paused. Scope when resumed: establish an iOS build (and signing/CI) path so the app can ship on iOS — CI currently produces an Android APK only (`.github/workflows/build_apk.yml`); no iOS target/build/signing exists. Acceptance (unchanged, applies on resume): iOS build produced (locally or CI) with a documented signing approach; iOS deployment target set to iOS 15. | paused |
| T34 | Multi-language localization (retire ko-KR hardcoding) | P3 | T4 DONE (2026-07-17; language list fixed = 한국어 + 영어) | Broader localization so UI text + voice guidance are not Korean-only. Currently `feedback_service.dart:12,30-32` hardcodes ko-KR TTS language and Korean message strings. NEW (2026-07-17), created from Open Q #6 answer (multilingual required). FULLY UNBLOCKED (2026-07-17): T4 fixes the language list = 한국어 (Korean) + 영어 (English), so localization scope is now definite (two languages). Related to T17 (multi-language TTS specifically). Acceptance: user-facing strings + TTS language externalized/selectable for the agreed languages (Korean + English); concrete locale codes/translated strings decided at implementation time. DONE (2026-07-17): localization infrastructure landed + Korean/English both supported. New file `crosswalk_app/lib/localization/app_strings.dart` introduces `AppLanguage` enum (ko/en), `resolveAppLanguage(Locale?)`, `ttsLocaleCode()`, and an `AppStrings` class holding all user-facing/voice strings; the previously hardcoded ko-KR TTS language and Korean message strings in `crosswalk_app/lib/services/feedback_service.dart` and `crosswalk_app/lib/screens/camera_screen.dart` were replaced with `AppStrings` references. Design decision: a lightweight custom class was chosen over `flutter_localizations`/ARB (the ARB pipeline is overkill for a single-screen app); language is picked from the detected system locale, with Korean as the fallback when the locale is unsupported or detection fails (preserves backward compatibility — existing Korean users see no change). Independent reviewer verified the safety-critical messages (deviation/off-crosswalk warnings) 1:1 against their Korean/English translations — PASS, no meaning loss / no mistranslation. New tests: `crosswalk_app/test/app_strings_test.dart` (9 tests), plus a fixed locale added to `crosswalk_app/test/camera_screen_test.dart` for determinism. `flutter analyze` re-run locally: same 6 pre-existing known issues, zero new. Regression evidence: PR #30 (`feat/T34-multi-language-localization` → `master`), CI PASSED — GitHub Actions run 29586552329, "Flutter 테스트" step logged "35 tests passed" (directly confirms the 9 new T34 tests actually ran and passed, not merely trusted); squash-merged (master `a2589c8..69b1cdb`), synced into develop (commit `2a3ea64`, `git diff origin/master origin/develop --stat` = 0 lines). CONSTRAINT (do NOT overstate): this task delivered the localization infrastructure (string externalization) and two-language (Korean + English) support via SYSTEM-LOCALE AUTO-DETECTION ONLY — it does NOT include a manual language-selection UI (settings screen). A user-facing language picker overlaps with the onboarding/settings scope of T19/T36 and is out of scope here. | done |
| T35 | Re-review frame interpretation/preprocessing for chest-mount posture | P2 | Open Q #8 answered (2026-07-17) | The app assumed a handheld "facing ahead" posture; the confirmed posture is chest-mount (lanyard/chest-mount), which changes camera angle/framing. Re-review whether frame interpretation and preprocessing (`classifier.dart` preprocessing + the model's training-data framing assumptions) still hold for this posture, and record findings/risks. NEW (2026-07-17), created from Open Q #8 answer. Acceptance: assessment recorded of whether chest-mount framing is compatible with current preprocessing/model; any required adjustment identified. NOTE: possible interaction with the model's training data framing — coordinate with T1/model retraining if a mismatch is found. | todo |
| T36 | First-launch onboarding screen with legal safety disclaimer | P1 | Open Q #9 answered (2026-07-17) | A disclaimer must be shown at onboarding / first launch (Open Q #9). No onboarding screen exists today (only `CameraScreen`, `main.dart:20`). Build at minimum a first-launch flow that displays the legal safety/scope-of-use disclaimer (accessible via TTS/screen-reader for the visually-impaired target user). NEW (2026-07-17), created from Open Q #9 answer. OPEN: exact disclaimer copy not yet provided by the user. Related to T5 (disclaimer decision) and T19 (phone-posture onboarding) — consider a single onboarding flow carrying both disclaimer and chest-mount posture guidance. Acceptance: disclaimer shown on first launch; accessible; copy agreed with user. CROSS-REF (2026-07-18): the earlier "OPEN: exact disclaimer copy not yet provided" gap is now closed — the user approved the disclaimer copy (recorded verbatim in T40's acceptance, Section F). Implementation now proceeds under T40 (OnboardingScreen), which carries this disclaimer together with T19's posture guidance in one first-run flow. T36's own status is intentionally left unchanged here; it will be updated after T40 lands. DONE (2026-07-18): T40 landed (see T40 row, Section F — PR #39, master `08e7bae..a06557a`, develop `c686e91`) and delivers exactly the legal safety disclaimer that satisfies T36's acceptance. (a) DISCLAIMER SHOWN + COPY AGREED: the user-approved copy is displayed VERBATIM on `OnboardingScreen` (`crosswalk_app/lib/screens/onboarding_screen.dart`) — reviewer confirmed an exact char-by-char match against the approved original (97 chars, zero mismatch), with 2 verbatim regression-guard tests in `crosswalk_app/test/app_strings_test.dart`, and the required "디자인 초안 문구 — 법률 검토 필요" code comment present. (b) ACCESSIBLE: the disclaimer is read aloud via TTS on screen entry for the visually-impaired target user. Regression coverage rides on T40's tests (CI run 29631590513, "54 tests passed"). CONSTRAINT — DIVERGENCE FROM ORIGINAL ACCEPTANCE (do NOT overstate): T36's acceptance says "disclaimer shown on FIRST LAUNCH", but as implemented the disclaimer is shown on EVERY launch, NOT first-launch-only. Reason: a first-launch-only persisted flag would require adding a new persistence package (e.g. `shared_preferences`), which was out of T40's scope. This is a deliberate, honestly-recorded divergence: the disclaimer is still guaranteed to appear before every crossing session (arguably safer for a safety-critical legal disclaimer), but it is NOT gated to first-run-only as the original wording implied. Switching to first-launch-only requires a separate decision + package approval. Status corrected `todo`→`done` because the disclaimer's substantive purpose (shown before use, accessible, approved copy) is met; the first-launch-only nuance is captured as this CONSTRAINT rather than left as a silent gap. | done |
| T37 | Low-light / night handling (v1) | P2 | Open Q #12 answered (2026-07-17) | Low-light/night is in scope for v1 (Open Q #12), with further improvement expected later. No low-light handling path exists in code today. NEW (2026-07-17), created from Open Q #12 answer. Acceptance: a low-light handling approach implemented for v1 (scope: night/low-light; rain/occlusion not separately in scope per user). NOTE: likely interacts with model training data (low-light samples) and possibly camera exposure settings — coordinate with T1/model retraining. DONE (2026-07-18): a v1 low-light handling approach was implemented and shipped, deliberately scoped NARROW — a user-controllable manual flashlight (torch) opt-in, NOT any model-side or preprocessing-side brightness change. (a) INVESTIGATION (verified against real package source, not trust): read the `camera` 0.11.2+1 and `camera_android_camerax` 0.6.19+1 package sources directly — `ExposureMode.auto` is already hardcoded at camera init (`android_camera_camerax.dart:226,485`), so the explicit `setExposureMode(ExposureMode.auto)` call added in `camera_screen.dart` is effectively a no-op on current Android (kept as defensive/explicit for future iOS). Reviewer independently re-verified these package-source claims (ExposureMode.auto hardcoding etc.) and confirmed them accurate. (b) INTENTIONALLY NOT IMPLEMENTED — auto exposure-brightness boost (positive exposure offset) and preprocessing-stage brightness/gamma correction. Reason: there is no ambient-light-sensing API, so any such boost would apply always (day and night indiscriminately), and changing the input distribution of an already recall-under-target model (measured in T1) WITHOUT re-validation would create a new instance of the same class of risk as T21 (missing softmax) and T35 (training-data posture mismatch). Reviewer confirmed this judgement as "sound logic" and consistent with the project's existing findings (T1/T21/T35). (c) ACTUALLY IMPLEMENTED — a manual torch toggle added to `SettingsScreen` (default OFF, session-scoped only); `CameraScreen` drives the real hardware via `CameraController.setFlashMode()`. `_torchEnabled` always resets to false on `_initCamera()` re-entry (matches the real hardware state that a fresh `CameraController` always starts with flash off). 2 new tests in `crosswalk_app/test/settings_screen_test.dart` (torch toggle default-off + callback fires on tap; reflects initial value passed from parent). `flutter analyze` re-run locally: same 6 pre-existing known issues, zero new (confirmed multiple times). Regression evidence: PR #43 (`feat/T37-low-light-torch-assist` → `master`), CI PASSED — GitHub Actions run 29635600503, "Flutter 테스트" step logged "56 tests passed" (directly confirms the 2 new torch tests ran and passed, not merely trusted); squash-merged (master `3ec56d5..0682975`), synced into develop (commit `08e27f4`, `git diff origin/master origin/develop --stat` = 0 lines). CONSTRAINT — SCOPE IS NARROW, do NOT overstate: the original acceptance ("a low-light handling approach implemented for v1") is satisfied only in the sense that a v1 approach was actually implemented and shipped — but that approach is ONLY a manual, user-toggled flashlight opt-in. This work does NOT improve the model's own low-light recognition accuracy; low-light detection accuracy still depends entirely on T1 (model retraining). Do NOT read this as "low-light scenes are now recognized reliably" — they are not. The torch merely gives the user a manual means to add light; it does nothing automatically and nothing at the model level. Status `todo`→`done` on the basis that the v1 acceptance (an approach implemented) is met within this explicitly narrowed scope; the accuracy gap remains open under T1. | done |
| T52 | 신호등 색 인식 기반 출발 안내 (타당성 조사 결과 — 현 구조로 불가) | P3 | 하드웨어 구조 변경 (전방·상방을 보는 별도 카메라 등) | **2026-08-22 예비 조사 결과 — 현재 구조로는 불가하다고 판단됨. 착수 전에 아래 제약을 반드시 재확인할 것.** 조사 근거(샘플 4장 육안 확인 — 표본이 작아 비율은 단정 불가, 구조적 제약만 확인됨): (1) **신호등이 프레임에 자주 안 들어옴** — 4장 중 2장에만 보임. 카메라가 가슴 높이에서 살짝 아래를 향하는데(T19/T40 확정 자세, `crosswalk_app/lib/localization/app_strings.dart:279`) 신호등은 머리 위 건너편에 있어 각도가 상충함. (2) **보이더라도 모델 입력에서 2~5픽셀** — 4000×3000 원본이 224×224로 리사이즈되면서 색점 수준이 됨. 현재 MobileNetV3-small 분류기 구조로는 인식 불가. 상단 1/3 크롭 시 개선되나 이는 별도 모델·별도 처리 경로를 의미함. (3) **야간 오인 위험** — 상점 간판·네온이 붉은색·초록색으로 가득해 색만으로는 신호등과 구분 불가. **안전상 판단 (가장 중요):** 이탈 경고가 틀리면 사용자가 이상함을 느끼고 무시할 수 있으나, 출발 안내가 틀리면 **차도로 걸어 들어감.** 현재 이탈 경고 오경보율이 10.1%인데 같은 신뢰도로 출발 안내를 하는 것은 허용 불가능한 위험임. 또한 한국은 음향신호기가 상당히 보급되어 있어, 낮은 신뢰도의 카메라 추정으로 대체하는 것은 개선이 아님. ACCEPTANCE: 착수 조건은 전방/상방을 보는 별도 카메라 등 하드웨어 구조 변경이 선행되는 것. 그 전까지는 이 행을 근거로 착수하지 말 것. | todo |
| T53 | `right` recall 퇴행 원인 규명 — 클래스 불균형 이중 보정 가설 검증 | P0 | T51 | BACKGROUND (2026-08-23): `approach` 신설 후 동일 표본 기준 `right` recall이 89.9% -> 70.6%로 퇴행. 퇴행분의 대부분이 `approach`에게 argmax를 빼앗기거나 임계값 미달로 무판정이 됐다. 가설: `groupkfold_cv.py`가 `WeightedRandomSampler(1/빈도)`와 `CrossEntropyLoss(weight=최다/빈도)`를 **둘 다** 걸어 소수 클래스 강조가 곱으로 누적된다. fold0 학습분 기준 실효 강조 none 1.00 / approach 8.78 / front 2.87 / left 6.69 / right 3.51 — 가장 작은 approach가 right보다 2.5배 강조된다. 방법: `USE_LOSS_WEIGHT` 플래그를 신설해 손실가중만 끄고(샘플러 유지) 나머지 조건(모델·에폭·LR·시드· 세션 분할)을 완전히 동일하게 유지한 세션 단위 5-fold 재실행 (`train/groupkfold_noweight.py`, 설정만 덮어쓰고 학습 로직은 import). 착수 **전에** 판정 기준을 확정: 85% 이상 = 이중 보정이 원인 / 78~85% = 부분 해결 / 78% 미만 = 가중치 탓 아님. RESULT (2026-08-23, 동일 표본 109장 — 네 구성 모두에 존재하고 정답이 `right`인 이미지): ① 843장 4-class 98/109 = 89.9% CI [82.8%, 94.3%] / ② 638장 5-class 손실가중 ON 77/109 = 70.6% CI [61.5%, 78.4%] / ③ 561장 4-class approach 제외 96/109 = 88.1% CI [80.7%, 92.9%] / **④ 638장 5-class 손실가중 OFF 82/109 = 75.2% CI [66.4%, 82.4%]**. **판정: 가설 기각.** 75.2%는 사전 확정 기준의 78% 미달이므로 '가중치 탓 아님'에 해당한다. 개선 자체는 실재하나(회복 5건, 새로 놓침 0건) McNemar 양측 정확검정 p = 0.0625로 유의수준 0.05를 넘지 못한다 — '우연을 배제할 수 없다'가 정확한 표현이다. 638장 전수 기준 부가 효과: right 70.8%->75.2%, none 83.0%->87.8%, front 77.7%->78.5%, left 87.6%->85.4%, approach 75.3%->71.4%, front 오경보 7.7% 동일, approach 오경보 11.7%->10.4%. **손실가중 제거는 공짜 이득이므로 다음 배포 재학습에 유지할 것** (right·none 상승, 오경보 증가 없음). 남은 실패 27건의 정체: 무판정 12건(right 확률 0.06~0.548, 7건은 0.36 이하라 임계값으로 못 살림), approach 5건(**가중치를 완전히 제거했는데도 approach 확률 중앙값 0.86 최대 0.92**), none 4건, front/left 각 3건. 즉 모델이 그 사진들을 실제로 approach로 보고 있으며 이는 데이터 문제다. 부수 발견 — 촬영 각도 혼재: EXIF 기준 초기(2025-11, 사용자 진술상 40~60° 하향) 전체 정확도 72.4% vs 이후(2026-07~08) 83.1%. 단 초기 사진은 학습 데이터에서 소수파(right의 12.4%만 초기)이므로 **'가파른 각도가 원리적으로 어렵다'는 결론은 낼 수 없고**, '섞인 데이터셋에서 소수파 각도가 손해를 본다'까지만 말할 수 있다. 계절·조명·장소 차이도 분리되지 않았다. 산출물: `train/groupkfold_noweight.py`, `train/groupkfold_noweight_out/`, `train/noweight_log.txt`. 임계값 조정으로는 해결 불가함도 별도 확인 (저장 확률 기반 스윕): deviation 0.55->0.30은 right를 76.8%까지만 올리고 오경보를 6.0%->7.6%로 키우며, approach 임계값 상향은 right에 **효과 0** (argmax가 이미 approach이므로 임계값을 올리면 approach가 무판정이 될 뿐이다). NEXT: 가중치·임계값 두 축이 모두 소진됐으므로 남은 것은 데이터다. T55(데이터 확보) 참조. | done |
| T54 | 모델 출력 인덱스와 앱 라벨 배열의 동기화 자동 검증 | P0 | T51 | BACKGROUND (2026-08-23): 이 프로젝트에서 라벨-모델 불일치가 세 번 발생했다 — T42(3->4 class), T51(4->5 class), 그리고 T51 브랜치에서 `classifier.dart`의 `_labels`만 5개로 바꾸고 앱 에셋 `crosswalk_model.onnx`는 4-class(`onnx.load()` 실측 출력 `[1,4]`)인 채로 둔 상태. 셋 다 **에러 없이** 잘못된 라벨을 낸다 — 안전 기능에서 가장 나쁜 실패 모드다. DONE: 세 지점을 묶는 검증 사슬 도입. (1) `train/export_onnx.py`가 익스포트 직후 모델의 실제 출력 차원 == `len(LABELS)`를 검사하고 아니면 중단하며, 라벨 순서 + 그 모델 파일의 sha256을 `labels.json`에 기록한다. `NUM_CLASSES` 하드코딩을 폐지하고 `train_model.LABELS`에서 파생시켜 클래스 개수를 적는 곳을 한 군데로 줄였다. (2) `test/labels_sync_test.dart`가 `labels.json`의 labels와 `Classifier.labelsForTest`가 **순서까지** 같은지 검사. (3) 같은 테스트가 `labels.json`의 `model_sha256`과 실제 번들 onnx의 sha256을 대조 — 이것이 있어야 '라벨만 갱신' / '모델만 갱신' 양방향이 모두 잡힌다. 같이 반영: 5-class 모델(출력 `[1,5]`)을 앱 에셋에 복사하고 `.sha256` 재생성. 검증: CI run 32617308147 (PR #68) — 신규 테스트 3건 개별 통과 로그 확인, 총 70 tests passed. CI가 계산한 번들 모델 해시 `7455f201...`이 `labels.json`의 `model_sha256`과 일치하므로 placeholder skip 분기를 타지 않고 실제로 대조가 수행됐다. `flutter analyze` 3 issues (기존 baseline과 동일, 신규 0). 로컬 `flutter test`는 T9/T10/T11/T26의 기존 `flutter_tester.exe` 크래시가 재현되어 실행 불가 — CI가 유일한 검증 수단이었다. | done |
| T55 | 방향 인지를 상태와 분리 — 출력 헤드 2개 구조(상태 + 방향)로 전환 | P1 | T53 (원인 규명 완료), 재라벨링 2차 재측정 결과 | BACKGROUND (2026-08-23, 사용자 결정): `approach`는 '횡단보도까지 거리가 충분히 있는 상태'로 두되, **그 상태에서도 방향(left/front/right)은 인지할 수 있어야 한다.** 방향 정답은 이미 `train/approach_direction.csv`에 77행 전부 기입되어 있다(미기입 0건). 2차 재라벨링에서 옮겨진 18장이 이 CSV의 direction과 100% 일치함을 확인했다(front 10, right 8, 불일치 0). 선택지 비교 (approach 방향별 학습 장수): 가 = approach를 방향별로 쪼개 7-class -> left 11 / front 29 / right 19. **나(채택) = 출력 헤드 2개** — 상태 헤드 {none, approach, 횡단보도 위} + 방향 헤드 {left, front, right}. front/left/right는 그 자체가 방향이므로 방향 헤드에 합쳐진다 -> left 100 / front 169 / right 139. 다 = approach 감지 후 별도 방향 분류기 -> 가와 동일한 11/29/19. '나'를 고른 근거: T53에서 최소 클래스가 체계적으로 불리하다는 것이 실측됐다 (approach 55장일 때 recall 71~76%, 다른 클래스 대비 최하위). 11장짜리 클래스를 만들면 같은 함정을 더 심하게 반복한다. '나'는 데이터를 쪼개지 않고 오히려 방향 학습량을 9배로 키운다. ACCEPTANCE: (1) 학습 스크립트가 상태·방향 두 라벨을 함께 읽고 두 헤드를 학습하며, 방향 라벨은 none 클래스에서 정의되지 않음(손실 마스킹)을 명시적으로 처리할 것. (2) `approach_direction.csv`를 단일 출처로 삼되, front/left/right 폴더 이미지의 방향은 폴더명에서 파생시켜 중복 기입을 만들지 말 것. (3) ONNX 출력이 2개가 되므로 T54의 라벨 동기화 검증 (`labels.json` + `labels_sync_test.dart`)을 두 헤드 모두에 대해 확장할 것 — 출력이 늘어나는 것은 T42/T51에서 세 번 사고를 낸 바로 그 변경 유형이다. (4) 앱 영향 검토 필수: `classifier.dart`의 단일 argmax 구조와 `feedback_service.dart`의 침묵 규칙 (현재 `front`/`none`/`approach`는 무음)이 '거리는 멀지만 방향은 안다'를 어떻게 안내할지 결정해야 한다 — 이는 PRD Open Question으로 올릴 사안이며 구현자가 임의로 정하지 말 것. (5) 세션 단위 k-fold로 재측정하여 5-class 단일 헤드 대비 right/left recall과 오경보를 비교할 것. | RESULT (2026-08-24, 637장 세션 단위 5-fold, 손실가중 OFF, fold 배정·transform·에폭·시드 모두 이전과 동일): **방향 인지는 성공, 이탈 감지 개선은 실패.** 두 목적을 섞어 판단하지 말 것. (1) 방향 head 단독 정확도 — approach(거리 있음) **54/59 = 91.5%** CI [81.6%, 96.3%], on(횡단보도 위) 318/349 = 91.1%, 전체 372/408 = 91.2%. 방향별 left 92.0% / front 88.8% / right 93.5%. 사용자 요구('거리가 있어도 방향은 인지')가 실제로 동작하며, 5-class 구조에서는 측정 자체가 불가능했던 값이다. (2) 5-class 결합 지표(동일 637장, ⑤ 재라벨링 대비): right 75.0->81.7%(+6.7pp, McNemar p=0.0768), left 84.3->79.8%(-4.5pp, p=0.3438), front 83.6->82.1%, approach 66.1->67.8%, none 87.8->88.2%, front 오경보 4.3->8.6%. **이탈 통합(left+right) 78.9->80.9%로 사실상 제자리.** (3) **동일 오경보에서 비교하면 우위가 사라진다** — ⑥의 이탈 임계값을 0.70으로 올려 front 오경보를 ⑤와 같은 4.3%로 맞추면 이탈 통합이 **74.6%로 ⑤(78.9%)보다 4.3pp 낮다.** 착수 전 정한 기준('right 80% 이상이면 유효')은 81.7%로 통과하나, **그 기준에 left와 오경보를 넣지 않은 것이 설계 실수였다.** 통과 여부만 보고하면 오해를 준다 — 정정해 기록한다. (4) 오경보 증가는 유의하지 않다: 6/140 -> 12/140, 불일치 7:1, McNemar 양측 **p = 0.0703**. 12건 **전부 상태 head는 `on`으로 정확히 판정**했고(확신도 0.66~1.00) 방향만 틀렸다. 새 오경보 7건 중 4건은 ⑤에서 '무판정'이던 것 — 원인은 모델 악화가 아니라 **확률이 뾰족해진 것**이다. P(right)=P(on)xP(dir=right)이므로 P(on)이 1에 가까우면 방향 확률이 희석 없이 그대로 나온다. 5-class softmax에서 5칸에 나뉘어 임계값을 못 넘던 것들이 판정으로 바뀐다. **이 한 가지가 right 상승과 오경보 증가를 동시에 설명한다.** (5) 병목이 처음으로 특정됐다: 상태 head 90.6%(none 89.5 / on 95.1 / **approach 67.8**), 방향 head 91.2%. 상태 혼동 최다는 **approach -> on 17건**. right 실패 22건 분해 — 방향은 맞고 상태가 틀림 8건 / 둘 다 맞았으나 임계값 미달 7건 / 방향 자체 오류 9건. 즉 **'방향을 모르는 게 아니라 횡단보도에 올라섰는지를 모른다.'** 산출물: `train/groupkfold_twohead.py`, `train/groupkfold_twohead_out/`, `train/twohead_log.txt`. NEXT: 손실가중(T53)·임계값·구조(T55) 세 축이 모두 소진됐고 매번 같은 곳을 가리킨다 — 인도 위인지 횡단보도 위인지를 사진이 말해주지 않는다. T58(상태 판정용 데이터) 참조.| done |
| T58 | 상태 판정(인도 위 vs 횡단보도 위) 전용 데이터 보강 | P0 | T55 (병목 특정 완료) | BACKGROUND: T55에서 병목이 상태 head로 좁혀졌다 — 방향 91.2%인데 상태의 approach는 67.8%, approach -> on 혼동 17건. 2026-08-23 실패 사진 육안 검토에서도 실패가 두 갈래로 갈렸다: (A) 경계가 프레임에 없음(줄무늬만 가득) (B) 너무 넓게 찍혀 진입 전인지 위인지 구분 불가. **두 원인은 처방이 다르다.** (A)/(B)는 같은 종류를 더 찍어도 해결되지 않고 구도를 바꿔야 하며, approach가 59장(최소 클래스)이라는 표본 부족은 같은 종류를 더 찍으면 해결된다. 착수 전 할 일: approach -> on 오판 17건을 콘택트시트로 확인해 **연석·점자블록·정지선 같은 구분 단서가 프레임에 있는가**를 먼저 가릴 것. 단서가 있는데 틀렸다면 표본 부족(더 찍으면 됨), 단서가 없다면 구도 문제(더 찍어도 무의미, 화각·각도를 바꿔야 함). ACCEPTANCE: (1) 위 분류 결과를 수치로 기록 (2) 그에 맞는 촬영 지침을 if/then 판정표로 확정 (3) 같은 장소에서 '인도 위 / 횡단보도 위'를 짝으로 촬영해 모델이 차이를 배울 재료를 만들 것 (4) 세션 단위 k-fold로 재측정해 상태 head의 approach 정확도가 오르는지 확인. 주의: 세션 수가 장수보다 중요하다 — 한 장소에서 20장보다 20개 장소에서 1장씩이 통계적으로 훨씬 값지다 (2026-08-22 데이터 누수 조사에서 확인). | 판독 결과 (2026-08-24, `approach -> on` 오판 17건 육안 검토, 사용자 확인 완료 — '판독은 얼추 맞아'): **13건은 구분 단서가 프레임에 있다** (연석·점자블록·보도블록·볼라드·난간이 화면 하단에 보임), 4건은 단서 없음(전부 야간 광각, 화면 아래가 전부 차도). 13:4이므로 **주된 원인은 구도가 아니라 표본 부족**이다. `approach`는 59장으로 최소 클래스이고, 단서는 화면 맨 아래 좁은 띠에만 있어 224px로 줄이면 몇 픽셀에 불과하다 — 59장으로 '이 띠를 봐야 한다'를 배우기 어렵다. 야간 편중 실측: 오판 17건 중 11건(65%)이 야간인데 approach 전체의 야간 비중은 46%다. 상태 정확도 — approach 야간 16/27 = **59.3%** vs 주간 24/32 = 75.0%. 대조군 on은 야간 94.4% vs 주간 96.3%로 **1.9pp 차이뿐**이다. 즉 야간 자체가 어려운 것이 아니라 **야간에 연석·점자블록을 구분할 데이터가 없다.** 촬영 지침을 `docs/CaptureGuide.md`에 판정표로 확정했다 (라벨 판정 5행 우선순위표, 방향 판정표, 촬영 우선순위, 세션 규칙, 촬영 자세, 촬영 후 절차). 목표: 야간 approach 15~20세션 + 짝 촬영(같은 자리에서 인도/횡단보도 한 걸음 차이) 15~20쌍 + 주간 approach 5~10세션 -> approach 59장에서 110~140장으로. **이만큼이면 목표를 넘는다는 보장은 없다** — 다만 최소 클래스라는 확실한 불리함을 먼저 없애야 다음 판단이 가능하다. 짝 촬영이 핵심인 이유: 현재 데이터에 '거의 같은 장면인데 라벨이 다른' 짝이 하나도 없어, 모델이 무엇을 기준으로 갈라야 하는지 배울 재료 자체가 없다.| 정정 (2026-08-24, 사용자 지적): 초판 촬영 지침에 넣은 '좌우 경계가 둘 다 프레임 밖이면 재촬영' 규칙은 **근거가 없었고 삭제했다.** 좌우 치우침은 경계선이 아니라 **줄무늬 기울기**로 판별되며, 경계는 프레임에 없어도 된다. 반증: 좌우 경계가 하나도 보이지 않는 근접 사진 `right (2936).jpg`에 대해 방향 head가 **right 0.98**로 정확히 판정했다(상태 head가 on 0.65로 흔들렸을 뿐). 방향 head 전체 91.2%도 같은 근거다 — 데이터 대부분이 이런 근접 사진이다. 두 단서는 답하는 질문이 다르다: **기울기 = 어느 쪽을 향해 가는가(진행 방향 오차, 경계 불필요), 경계선 = 폭 방향 어디에 있는가(현재 위치).** 이탈 경보에 필요한 것은 전자다 — 방향이 틀어지는 것을 잡아야 벗어나기 **전에** 경고할 수 있다. 초판 규칙은 후자만 보고 만든 것이고, 같은 문서의 방향 판정표('축이 오른쪽으로 비스듬함 -> right')와도 모순됐다. 부수 결론: **가로 촬영·광각을 검토한 근거가 소멸했다.** 그 근거는 '수평 화각을 넓혀 경계를 담자'였다. 세로가 진행 방향으로 더 멀리 담아 기울기 판독에 오히려 유리하므로 **세로 유지**한다.| todo |
| T63 | 이탈 강도 등급(약/심) + 구간 전이 안내 + 배터리 절약 모드 + 목표방향 펄스 | P1 | T61 (오디오 정책) | BACKGROUND (2026-08-24, 사용자 4건 요청 — 손그림 시안 첨부): (1) 이탈 '정도'를 숫자로 분석해 크게 벗어나면 강하게, 약하면 기존처럼 안내. (2) 횡단보도 진입/완료 시점 1회 음성 안내. (3) 심한 이탈에서 복귀 시 '즉시 이동하세요' -> '이동하세요' -> '직진하세요' 3단계, 처음부터 직진이면 무음 유지. (4) 색을 방향이 아니라 강도로 나누기(심함=빨강/약함=주황), 화살표는 현재 이탈 방향, 목표 방향은 가장자리 펄스로 별도 표시. (5, 별건) 카메라 프리뷰를 볼지 말지를 '배터리 절약 모드'로 토글. HONESTY CAVEAT(중요): `Classifier`는 좌표·기하 정보를 내지 않는 단일 프레임 분류기라 실제 이탈 각도·거리를 측정하지 못한다. '이탈 정도'는 **left/right 확신도를 근사치로 쓴다** — 확신도가 높을수록 그 방향 특징이 뚜렷하다는 뜻이지 몇 도/몇 미터인지를 재는 것이 아니다. 이 사실을 코드 주석(`Classifier.deviationSeverityThreshold`)과 사용자 응답 양쪽에 명시했다. DONE: (1) `Classifier.deviationSeverityThreshold = 0.80`(**잠정값**, 미측정 — 실기기 보행 테스트로 재보정 필요) 신설. `FeedbackService.decideMessage`가 (클래스, 강도) 쌍을 쿨다운 키로 쓰며 강도가 바뀌면 즉시 재발화한다. 4단계 문구 `left/rightDeviationMessage{Mild,Severe}` 신설. (2) `decidePhaseMessage(previousClass, detectedClass, now)` 신설 — approach->{front,left,right}에서 '횡단보도에 진입했습니다.', {front,left,right}->none에서 '횡단보도를 건넜습니다.' 1회 발화(P1), 3초 재쿨다운으로 상태 떨림(flicker) 시 반복 방지. 발화 여부와 무관하게 진짜 직전 클래스를 추적하는 `_lastRawClass`를 신설했다 — 기존 `_lastAlertClass`는 쿨다운에 걸려 갱신이 안 될 수 있어 전이 감지에 못 쓴다. (3) `decideRecoveryMessage(previousClass, detectedClass, now)` 신설 — left/right->front 전이에서만 '직진하세요'(P0) 1회 발화, 처음부터 front면 항상 무음(기존 규칙 유지), 3초 재쿨다운. (4) `StateFieldPainter`: 화살표가 이제 **현재 이탈 방향**을 가리키도록 뒤집었다(2026-08-24 사용자 확정 — 기존엔 '가야 할 방향'이었다). 목표(가야 할) 방향은 반대편 가장자리의 애니메이션 펄스로 전달한다 — T41의 죽은 코리도 애니메이션 컨트롤러(GuidanceCorridorPainter 제거 후에도 갱신만 하고 아무도 읽지 않던 트윈)를 재사용해 만들었다. `MediaQuery.disableAnimations`(모션 감소 설정) 시 애니메이션 대신 고정 밝기로 표시. 색은 `_colorAmber`(약함)/`_colorSevere`(#FF5A5F, 심함) 2단계이며, 색만으로 강도를 구분하지 않도록 텍스트 라벨(`labelLeftSevere`/`labelRightSevere`)·굵은 화살표(14px vs 10px, 1.2배 크기)·넓은 펄스(88px vs 56px)로 겹쳤다. (5) 배터리 절약 모드(설정 화면, 기본값 켜짐): 켜지면 기존처럼 화살표만 그리는 계기판, 끄면 카메라 프리뷰가 배경에 나온다. 끌 때의 대비 문제(Claude Design 1a가 프리뷰를 없앤 이유)는 완전히 해소할 수 없음을 인정하고, 대신 스크림(불투명도 0.86)을 **수학적으로 계산**해 최악의 경우(순백색 프레임)에도 WCAG AA(4.5:1)를 만족하도록 보장했다: 텍스트 #F1F3F4(휘도 0.92) vs (1-0.86)*1.0+0.86*L(#0E1013) 최악 휘도 -> 대비 4.85:1. 대가로 프리뷰가 거의 안 보이는데, 이는 '라이브 영상 위에서 임의 대비를 보장하는 것은 사실상 불가능하다'는 원래 결론을 재확인해 준다. 배터리 소모 비교는 **실측하지 않았다** — 이 환경에 실기기 프로파일링 도구가 없다. 대신 공학적 근거만 제시했다: 카메라 캡처 자체는 분류를 위해 프리뷰 여부와 무관하게 항상 필요하므로, 프리뷰의 한계비용은 GPU가 매 프레임(연속) 카메라 텍스처를 합성하는 비용 대 상태 변화 시에만 다시 그리는 정적 텍스트의 비용 차이다. 실질적이지만 크기는 미측정이라고 사용자에게 명시했다. 산출물: `Classifier.deviationSeverityThreshold`, `FeedbackService.decidePhaseMessage/decideRecoveryMessage`, `CameraScreen._severe/_pulseEdge/_syncPulseAnimation/_buildEdgePulse`, `SettingsScreen` 배터리 절약 스위치, `test/feedback_service_test.dart`(decidePhaseMessage/decideRecoveryMessage 전 케이스 + 강도 쿨다운), `test/settings_screen_test.dart`(배터리 절약 토글), `test/camera_screen_test.dart`(severe shouldRepaint). 검증: `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). 로컬 `flutter test`는 `flutter_tester.exe` 크래시로 실행 불가 — CI가 유일한 검증 수단(T9/T10/T11/T26과 동일한 기존 환경 문제). | done |
| T64 | 카메라 프리뷰 레터/필러박스 제거 + 목표방향 펄스를 화면 전체로 확장 | P2 | T63 | BACKGROUND (2026-08-24, 사용자가 실기기 스크린샷 첨부 — 배터리 절약 모드를 끈 상태): 화면에 밝은 직사각형 경계가 도드라져 '펄스가 너무 형식적으로 끊겨 있다'는 지적을 받았다. 원인 규명(camera 0.11.2 lib/src/camera_preview.dart 실제 소스 확인): `CameraPreview`는 내부에서 `AspectRatio(aspectRatio: 1/controller.aspectRatio, ...)`로 감싸여 있다. `Positioned.fill`이 주는 타이트한 제약 안에서도 `AspectRatio`는 스트레치가 아니라 '그 안에 들어가는 가장 큰 사각형'을 고르므로, 화면 비율과 카메라 센서 비율이 다르면 레터/필러박스 여백이 생긴다. 스크림(불투명도 0.86, T63)이 그 위를 덮지만 카메라 화소가 실제로 있는 영역만 14%가 비쳐 보이고 여백은 배경색과 거의 같은 색이라, 사각형 경계 자체가 눈에 띄게 도드라졌다. DONE: (1) `_buildFullBleedPreview()` 신설 — 이미 아스펙트비로 맞춰진 CameraPreview를 `Transform.scale`로 균일 확대해 BoxFit.cover와 동일한 효과를 낸다. scale = max(camAspect/boxAspect, boxAspect/camAspect) (camAspect = 1/controller.aspectRatio, 이 앱은 portraitUp 고정이라 가로 분기는 타지 않는다). (2) 목표방향 펄스를 상태 필드 영역(위아래 여백 제외)에 갇혀 있던 56~88px 고정폭 막대에서 **화면 전체 높이**(최상위 Stack, 스크림 위·전경 텍스트 아래)로 옮기고, 폭도 화면 너비 비율(약함 32%/심함 48%)로 바꿨다. 2단 그라디언트가 중간에서 밝기가 급격히 꺾여 보이던 것을 3단(가장자리->중간->투명)으로 완화했다. CAVEAT(중요, 정직하게 기록): 이 환경에는 카메라 기기가 없어 **실기기로 시각 확인을 못 했다.** Transform.scale 공식은 camera_preview.dart 소스를 직접 읽고 수식으로 유도했을 뿐이며, 실기기에서 확대율이 과해 원치 않는 크롭이 생기지 않는지, 펄스가 실제로 부드럽게 보이는지 확인이 필요하다. 검증: `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). 로컬 `flutter test`는 `flutter_tester.exe` 크래시로 실행 불가 — CI가 유일한 검증 수단. | done |
| T65 | 배터리 절약 모드 기본값 변경 + 프리뷰 스크림 완화 + 화살표 실시간 방향추적(보류) | P2 | T64 | BACKGROUND (2026-08-24, 사용자가 실기기 스크린샷 2장 첨부): (1) "펄스가 전체적으로 보여 좋다" — 확인만, 코드 변경 없음. (2) "기본값을 절약모드를 끄는 것으로" — 배터리 절약 모드가 아닌 카메라 프리뷰가 항상 보이는 쪽을 기본으로 요청. (3) "절약모드 꺼서 카메라를 볼 때 화면이 너무 어두워" — T63에서 화면 전체에 깐 0.86 스크림이 상태 필드(화살표) 영역까지 어둡게 만든 것에 대한 지적. (4) "화살표가 좌우만 가리키는 게 아니라 횡단보도 방향을 실시간 동기화해 가리켜야" — 보류 결정(아래 CAVEAT 참고). DONE: (2) `CameraScreen._powerSaveMode` 기본값 `true`→`false` (`camera_screen.dart:91`). (3) 화면 전체를 덮던 `Positioned.fill` 스크림(alpha 0.86)을 제거하고, 실제 텍스트가 있는 하단 판독부(헤드라인/서브라인/확신도바/면책문구) 블록만 `Container`로 감싸 그 안에서만 동일한 alpha 0.86 배경을 유지했다 — 대비 보장 계산은 T63과 동일(텍스트 #F1F3F4 대 최악 휘도(순백 프레임) (1-0.86)*1.0+0.86*L(#0E1013) ≈ 0.15 → 대비 4.85:1, AA 4.5:1 충족). 상단 상태 칩·경고 배너는 이미 자체 불투명 배경(원형 칩, 경고 배너 색상 컨테이너)을 갖고 있어 전체 스크림에 의존하지 않았으므로 그대로 둠. 상태 필드(화살표) 영역은 이제 스크림 없이 카메라 원본 밝기로 보인다. CAVEAT(4번, 진행 보류): `Classifier`는 좌표·각도·바운딩박스를 전혀 출력하지 않는 단일 프레임 5-클래스 분류기(`classifier.dart:23-36`)라 "화살표가 실제 횡단보도 방향을 가리키며 실시간 동기화"할 근거 데이터 자체가 없다. 이미 코드 주석(`camera_screen.dart:1027-1030`)에 "화살표는 감지된 실제 위치가 아니라 분류 결과를 옮긴 안내 기호"라고 명시되어 있음 — 이를 어기고 그럴듯하게 움직이는 애니메이션을 넣는 것은 DesignBrief의 "false certainty 금지" 원칙 위반이라 임의 진행하지 않고 AskUserQuestion으로 확인, 사용자가 "보류"를 선택함(2026-08-24). 별도 방향 회귀/세그멘테이션 모델이 필요한 큰 작업이며 착수 시 새 태스크로 등록 예정. 검증: `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). 로컬 `flutter test`는 `flutter_tester.exe` 크래시로 실행 불가 — CI가 유일한 검증 수단. 실기기로 밝기 개선 체감을 확인하지 못함(이 환경에 카메라 기기 없음). | done |
| T66 | 횡단보도 줄무늬 방향 실시간 추정 — 옵션 B(고전 CV) 프로토타입 | P3 | T65(보류 결정) | BACKGROUND (2026-08-24, T65의 화살표 실시간 방향추적 보류 후 사용자 후속 질문): "사람이 봤을 때는 기울어진 정도를 파악할 수 있는데 너는 안 되냐"는 질문에 — 원리적으로 불가능한 게 아니라 **현재 모델이 각도 정답으로 학습된 적이 없어서** 안 되는 것이라 설명. 두 갈래(A: 각도 라벨링 후 재학습, B: 학습 없이 기하 계산)를 제시했고 사용자가 B를 먼저 프로토타입해보라고 결정. DONE: `StripeDirectionEstimator`(`lib/services/stripe_direction_estimator.dart`) 신설 — Sobel 에지 검출 + 방향 히스토그램(1도 단위, ±3도 창 가중평균)으로 우세 에지 각도를 추정하는 순수 함수. YUV420 카메라 프레임의 Y 플레인(그레이스케일)을 RGB 변환 없이 그대로 입력받을 수 있게 `rowStride` 파라미터로 패딩된 버퍼를 지원. 수직에 가까운 에지(각도 절댓값 > maxTiltDegrees, 기본 60도)는 건물·가로등 등 배경 구조물로 보고 제외. 우세 방향의 집중도(confidence)가 임계값 미만이면 `Classifier`의 threshold-미달 null 반환과 같은 원칙으로 null 반환. 검증: `test/stripe_direction_estimator_test.dart` 신설 — 합성(이상적) 줄무늬 패턴을 각도 0/±15/±35도로 생성해 추정값이 ±3도 오차 내로 맞는지, 평평한 이미지·무작위 노이즈 이미지는 null인지, 거의 수직인 줄무늬(80도)는 배경 구조물로 제외되는지, 카메라 프레임처럼 행간 패딩이 있는(rowStride > width) 버퍼도 정확히 읽는지 확인. `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). CAVEAT(중요, 정직하게 기록): (1) 로컬 `flutter test`는 `flutter_tester.exe` 크래시로 실행 불가 — CI가 유일한 검증 수단(기존 환경 문제, T9/T10/T11/T26과 동일). (2) **이 추정치는 합성 이상적 패턴으로만 검증됐다.** 실제 카메라 프레임(원근 왜곡, 그림자, 젖은 노면 반사, 차선 등 다른 흰 선, 저조도 노이즈)에서 얼마나 정확한지는 완전히 미검증 — 이 환경에 카메라 기기가 없다. (3) **아직 앱 UI(화살표)에 연결하지 않았다** — 순수 계산 모듈만 만들었다. 실기기 검증 없이 안전 관련 기능(화살표 방향)에 바로 연결하는 것은 위험 부담이 크다고 판단해 사용자 확인 없이 진행하지 않음. 다음 단계(실기기로 각도 값을 로그/디버그 표시해 사람이 보는 각도와 비교하는 것 등)는 사용자 결정 대기. | done |
| T66-2 | T66 검증용 화면 표시 — 실기기 디버그 로그 → 화면 내 각도 텍스트로 전환 | P3 | T66 | BACKGROUND (2026-08-24): 사용자가 "아티팩트(APK) 다운로드해서 핸드폰으로 확인할 수 없냐"고 질문 — CI가 `flutter build apk --release`로 빌드하는데(`build_apk.yml`), release 빌드는 `kDebugMode`가 항상 false로 컴파일되어 T66에서 넣은 `debugPrint` 콘솔 로그는 그 APK를 폰에 설치해도 절대 안 보인다고 설명. 컴퓨터 연결(`flutter run`/adb) 없이 폰만으로 확인하고 싶다면 화면에 직접 표시해야 한다고 안내했고, 사용자가 그 방식을 선택. DONE: `CameraScreen`에 `_stripeDebugEstimate` 상태 필드 추가, `_updateStripeDirectionDebug`가 10프레임마다 1회 `StripeDirectionEstimator.estimate()`를 실행해 `setState`로 저장(콘솔 `debugPrint`는 기존처럼 `kDebugMode`에서만 유지). 하단 판독부의 확신도 막대 아래, 200% 확대(dense) 시에는 숨기는 2차 정보 영역에 점선 테두리 박스로 "실험적 — 줄무늬 각도: N도 (신뢰 M%)" 텍스트를 추가 — 정식 기능처럼 보이지 않도록 흐린 색(`_colorTextDim`)과 "실험적" 라벨로 구분했고, `ExcludeSemantics`/`liveRegion` 밖에 둬 음성 안내를 방해하지 않는다. **의도적으로 `kDebugMode`로 막지 않았다** — release APK를 GitHub Actions Artifacts에서 받아 폰에 그냥 설치해도 보이게 하기 위함. 검증: `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). CAVEAT: 여전히 화살표·안내 로직에는 연결되지 않은 순수 표시용이며, 실제 카메라 프레임 정확도는 미검증(합성 패턴으로만 검증, T66과 동일). | done |
| T66-3 | 무판정 원인 구분 표시 — noEdges/tooVertical/lowConfidence | P3 | T66-2 | BACKGROUND (2026-08-24, 사용자 실기기 관찰): "오른쪽으로 과하게 이탈"이라는 `Classifier` 판정에서도 줄무늬 각도 표시가 무판정으로 나온다고 보고. 원인을 하나로 단정하지 않고 두 후보를 제시: (A) `maxTiltDegrees`(60도) 필터가 근수직 우세 방향을 실제로 걸러냈을 가능성, (B) `Classifier`의 "심함" 판정은 애초에 좌표·각도가 아니라 학습된 이미지 특징으로 내려지는 것이라 두 시스템의 판단 근거가 다르며, 실제로 줄무늬가 안 보이는 프레임일 가능성 — 사용자가 진단 정보 추가를 요청. DONE: `StripeDirectionEstimator`에 `StripeDirectionNullReason`(noEdges/tooVertical/lowConfidence/none) enum과 `StripeDirectionDiagnostic`(estimate + reason + rejectedAngleDegrees) 클래스, `diagnose()` 메서드 신설. 기존 `estimate()`는 `diagnose(...).estimate`를 반환하는 얇은 래퍼로 유지해 하위 호환 보존. 알고리즘: 각도 필터링 전에 전체 181개 bin(−90~90도)에 모든 에지를 먼저 집계한 뒤, `maxTiltDegrees` 범위 안(수평에 가까운 쪽)의 총가중치가 0이면 범위 밖(반드시 근수직)에서 전역 우세 각도를 찾아 `tooVertical` + `rejectedAngleDegrees`로 보고하고, 범위 안에 에지가 있으면 원래(T66)와 **수치가 완전히 동일한** 방식으로 우세 bin·신뢰도를 계산해 신뢰도 미달 시 `lowConfidence`를 반환 — 기존 3개 각도/노이즈/근수직 테스트가 그대로 통과하는 것으로 수치 동일성을 확인(회귀 없음). `CameraScreen`은 `_stripeDebugDiagnostic`(`StripeDirectionDiagnostic`)을 저장하고 `_stripeDebugText()`가 이유별로 다른 한국어 문구("무판정: 뚜렷한 에지 없음(흐림/저대비/저조도)" / "무판정: 우세 방향이 근수직이라 배경으로 간주해 제외 (걸러진 각도 N도)" / "무판정: 우세 방향 없음(방향이 흩어짐)")를 화면에 표시. 검증: `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0), `test/stripe_direction_estimator_test.dart`에 4개 진단 테스트 신설(noEdges/tooVertical+각도값/lowConfidence/none+estimate). 로컬 `flutter test`는 여전히 크래시로 실행 불가 — CI가 유일한 검증 수단. CAVEAT: 이 진단은 여전히 합성 패턴 기준이며, 사용자가 겪은 실제 상황이 tooVertical인지 lowConfidence인지는 실기기에서 이 표시를 직접 봐야 확정됨(미검증). | done |
| T66-4 | 각도가 항상 무판정인 원인 규명 및 수정 — 신뢰도 창 ±3도 → ±10도 | P2 | T66-3 | BACKGROUND (2026-08-24): T66-3의 진단 표시를 붙인 뒤 사용자가 실기기 사진 3장을 제공 — 세 장 모두 줄무늬가 육안으로 뚜렷한데도 **전부 `lowConfidence`("우세 방향 없음")** 로 나왔다. T66-3에서 후보로 제시했던 `tooVertical`(필터 문제)이 **아니었음이 실측으로 확정**됐다. 원인 규명(추측 아님 — Dart 알고리즘을 파이썬으로 그대로 포팅해 실측, 스크래치패드 `diag.py`): 실제 횡단보도는 (1) 원근 왜곡으로 화면 위/아래에서 줄무늬 각도가 다르고 (2) 사진에 보이듯 페인트가 심하게 갈라져 에지 방향이 넓게 퍼진다. 신뢰도를 계산하는 창이 ±3도로 좁아 그 퍼짐을 담지 못해 신뢰도가 임계값(0.2) 밑으로 떨어졌다. 측정값(원근+균열 조건): ±3도 → conf 0.12~0.17(무판정), ±10도 → 0.25~0.62(정상). 기존 테스트가 이걸 못 잡은 이유는 합성 패턴이 **완벽히 평행한** 줄무늬라 원근·균열이 없어 conf가 항상 1.0이었기 때문. DONE: (1) `angleWindowDegrees` 파라미터 신설, 기본값 10. **±12도가 아니라 10인 이유도 실측**: 12 이상으로 넓히면 무작위 노이즈까지 conf 0.21로 통과해 오탐이 생긴다(노이즈 4개 시드에서 ±10도는 0.152~0.182로 전부 정상 거부). (2) 회귀 테스트 4개 신설 — 원근만/원근+균열 30·50에서 무판정이 아닐 것, **창을 ±3도로 되돌리면 무판정이 재현될 것**(원인을 코드로 고정), 창을 넓혀도 노이즈는 거부될 것. 원근 왜곡과 균열을 흉내 내는 `_makePerspectiveStripes` 생성기 신설. (3) **테스트 생성기 버그 발견·수정**: 기존 `_makeNoise`가 LCG의 **하위 8비트**(`state % 256`)를 써서 실제로는 주기가 짧은 규칙적 무늬였다 — ±3도에서는 안 드러났지만 ±10도로 넓히자 4개 시드 중 3개가 방향으로 오탐됐다(conf 0.216/0.249/0.222). 상위 비트(`(state >> 16) & 0xFF`)로 바꿔 해결(0.152~0.177로 전부 거부). 이 수정 없이 창만 넓혔으면 **CI에서 기존 노이즈 테스트가 깨졌을 것**이다. 검증: `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). 로컬 `flutter test`는 여전히 크래시로 실행 불가라, 새 테스트 4개와 기존 테스트의 기대값을 파이썬 포팅(`verify_dart_gen.py` — Dart 생성기를 정확히 재현)으로 미리 검증한 뒤 커밋했다. CAVEAT: 여전히 **합성 패턴 기준**이다. 사용자 실기기 사진 자체로 알고리즘을 돌려본 것이 아니라, 사진에서 관찰한 조건(원근+균열)을 합성으로 재현해 측정했다. 실제 개선 여부는 실기기에서 각도 숫자가 뜨는지로 확인이 필요하다. | done |
| T67 | 화살표가 감지된 횡단보도 방향을 실시간으로 따라가도록 UI 구현 | P2 | T66-4 | BACKGROUND (2026-08-24): T65에서 보류했던 원 요청("화살표가 단순히 좌우만 가리키는 게 아니라 횡단보도의 방향을 가리키도록 애니메이션을 넣어야 돼, 횡단보도와 실시간 동기화가 되도록")을 T66~T66-4로 각도 추정이 가능해진 뒤 착수. DONE: (1) `StripeAngleSmoother`(`stripe_direction_estimator.dart`) 신설 — 순수 상태 기계. 지수이동평균(기본 0.35)으로 떨림을 억제하고, `missTolerance`(기본 3)회 연속 무판정이어야 각도를 놓아 깜빡임을 막으며, `minConfidence`(기본 0.25)로 신뢰도 게이트를 건다. **화면 표시용 임계값(0.2)보다 높게 잡은 이유**: 숫자를 보여주는 것보다 화살표를 실제로 돌리는 쪽이 더 확신을 요구한다 — 확신 없는 각도로 화살표를 돌리면 틀린 방향을 자신 있게 가리키게 된다. (2) `StateFieldPainter`에 `stripeAngleDegrees` 추가 — 값이 있으면 front/left/right 화살표가 **감지된 횡단보도가 뻗은 방향(진행 방향)** 을 가리키도록 회전한다. 기하: 줄무늬는 진행 방향과 수직이고 줄무늬 선 방향이 (cos θ, sin θ)이므로 진행 방향은 (sin θ, −cos θ), 캔버스 회전각은 **θ − π/2**. θ=0이면 −π/2(위쪽)로 기존 'front' 화살표와 정확히 일치함을 파이썬으로 수치 검증했다(진행방향 벡터와 회전각 유도 결과가 소수 4자리까지 일치). null이면 회전하지 않고 기존 고정 화살표로 되돌아간다. (3) `_buildStateField`가 `TweenAnimationBuilder`(220ms, easeOut)로 회전을 보간한다 — 각도 갱신이 10프레임마다 1회(약 3Hz)라 값을 그대로 그리면 뚝뚝 끊긴다. `MediaQuery.disableAnimations`(모션 감소)에서는 보간을 끄고 즉시 목표 각도로 그린다(정보는 유지, 움직임만 제거). (4) `_initCamera`에서 스무더를 reset — 앱 재개 후 전혀 다른 장면에 낡은 각도가 남는 것을 막는다. **좌/우 이탈 정보는 사라지지 않는다**: 화살표 위치(좌 0.30 / 우 0.70)·색·문구·가장자리 펄스·음성·진동이 그대로 전달하므로 색각이상 중복 채널이 유지된다(T63 결정을 각도를 알 때만 회전으로 대체, 모를 때는 기존 이탈방향 화살표 유지). 검증: `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). `test/stripe_angle_smoother_test.dart` 신설(신뢰도 게이트/EMA 수렴/음수 각도/히스테리시스 4종/reset), `test/camera_screen_test.dart`에 shouldRepaint 3종 + 회전 조건·기하 5종 추가. 로컬 `flutter test`는 여전히 크래시로 실행 불가라 EMA·회전각 기대값을 파이썬으로 사전 검증한 뒤 커밋(7.0/−20/29.99993, −π/2, π/6 전부 일치). CAVEAT(중요): 각도 추정 자체가 **실기기 미검증**이므로(T66-4는 합성 패턴 기준 수정) 화살표가 실제로 횡단보도를 정확히 따라가는지는 확인되지 않았다. 신뢰도 게이트와 무판정 복귀가 그 위험을 줄이지만 없애지는 못한다 — 실기기에서 화살표 방향이 실제 횡단보도와 맞는지 눈으로 확인해야 한다. 스무딩 계수 0.35, 보간 220ms도 **잠정값**(실기기 미측정)이다. | done |
| T67-2 | T67 화살표 실패 원인 규명 — 고전 CV 접근법의 한계 확정 | P1 | T67 | BACKGROUND (2026-08-29): 사용자가 실기기 스크린샷 4장과 함께 "횡단보도가 뻗은 방향과 맞지 않고, 움직임도 굼뜨고, 잘 인식이 안 되고, 좌우 모드로도 돌아간다. 노트북 화면에 사진을 띄워놓고 테스트한 것이라 그것 때문인지 모르겠다"고 보고. **노트북 화면 요인 배제 (확정):** 스크린샷 중 한 장의 원본 파일명(`20260722_133731.jpg`)이 식별되어 `image/3_left/`에서 찾아냈고, 그 원본을 알고리즘에 직접 넣어 비교 — 앱이 노트북 화면으로 읽은 값 **−1도/신뢰 24%** vs 원본 파일 직접 측정 **−1.8도/신뢰 24%** 로 거의 일치. 노트북 화면은 측정을 왜곡하지 않았다. **측정 방법:** Dart 알고리즘을 파이썬으로 벡터화 포팅(`real_images.py`)하고, 먼저 합성 패턴에서 기존 루프 구현과 결과가 완전히 일치함을 확인한 뒤 실제 사진에 적용. EXIF Orientation=6(T57 기록된 미해결 이슈)이 섞여 있어 `ImageOps.exif_transpose`로 보정 후 측정 — 보정 전 60.5도 / 보정 후 −1.8도로 90도 차이가 나므로 이 보정 없이는 측정 자체가 무의미하다. **실패 원인 (실측):** (1) **불안정** — 같은 사진인데 축소 크기만 바꿔도 −1.8~11.1도(흔들림 12.9도), 가장자리 5% 크롭만 해도 −4.8~5.9도(11도). 즉 보고되는 값이 실제 각도가 아니라 측정 잡음이다. (2) **신뢰도가 근본적으로 낮음** — 실제 사진 45장 신뢰도 중앙값 26%(합성 평행 패턴은 100%), 화살표 게이트(0.25) 통과 **26/45=58%** → 42%는 좌우 모드로 복귀. 사용자가 관찰한 "좌우 모드로 돌아간다"와 정확히 일치. (3) **근본 원인** — 원근 때문에 가까운 줄무늬와 먼 줄무늬의 각도가 서로 달라 "하나의 대표 각도"가 애초에 잘 정의되지 않는다. 히스토그램이 넓게 퍼지는 것이 정상이며, T66-4에서 창을 ±3→±10도로 넓힌 것은 이 증상을 덮은 것이지 원인을 고친 것이 아니었다. **기각된 개선안 (추측 아님, 실측으로 기각):** "원근 압축이 적은 화면 아래-가운데 영역만 보기"를 시도했으나 **오히려 악화** — 흔들림 12.9→24.6도, 게이트 통과 58→31%. 파라미터 튜닝으로 풀 문제가 아님이 확인됐다. 결론: 전역 에지 히스토그램 접근법(T66의 옵션 B)은 실제 횡단보도에서 화살표를 구동하기에 부적합. 사용자가 **A안(각도 라벨링 후 재학습)** 선택 → T68. | done |
| T68 | 각도 라벨링 도구 (A안 1단계) | P1 | T67-2 | BACKGROUND (2026-08-29): T67-2에서 고전 CV가 부적합으로 확정되어, 사용자가 A안(각도 정답을 매겨 모델 학습)을 선택. A안의 병목은 모델이 아니라 **각도 정답 데이터**이므로 라벨링 도구부터 만든다. **라벨링 규칙 (판정 애매함을 없애는 핵심 결정):** 줄무늬 각도가 아니라 **「이 자리에서 건너려면 걸어갈 방향」** 을 사람이 직접 선으로 긋는다. 이유 — 원근 때문에 줄무늬 각도는 화면 위치마다 달라 사람도 하나로 답할 수 없지만(바로 이것이 T67-2의 실패 원인), 진행 방향은 사람이 보면 하나로 정해진다. 또한 이 값이 곧 **화살표가 가리켜야 할 값 그 자체**라 라벨→출력 사이에 변환·해석이 끼지 않는다. 각도 규칙: 화면 위=0도, 시계방향=+, 반시계방향=−. DONE: `train/label_angles.py` 신설 — Tkinter GUI. 드래그로 방향선을 긋고 Enter/Space로 저장, S=건너뛰기(횡단보도가 안 보이거나 방향이 애매하면 억지 라벨 대신 건너뛴다), Z=이전으로 되돌아가 다시 매기기, R=다시 긋기, Q=종료. `train/angle_labels.csv`에 한 줄씩 즉시 append 되어 중간에 꺼도 안전하고, 재실행 시 완료분을 자동으로 건너뛰고 이어서 진행한다. EXIF 회전을 보정해 표시하므로(T57 이슈) 사람이 보는 방향과 저장되는 좌표가 일치한다. 대상: 횡단보도가 보이는 4개 클래스 **408장**(1_approach 59 / 2_front 140 / 3_left 89 / 4_right 120), `0_none` 229장은 횡단보도가 없어 제외. 검증: tkinter 임포트 확인, 대상 수집 408장 확인, 각도 규칙을 6개 케이스(위 0 / 오른쪽 +90 / 왼쪽 −90 / 오른위 +45 / 왼위 −45 / 아래 180)로 전수 검증해 전부 일치. CAVEAT: **GUI 도구라 이 환경에서 실제 라벨링 동작(마우스 드래그·저장)을 끝까지 실행해보지는 못했다** — 사용자가 직접 실행해야 확인된다. 다음 단계(모델 구조 변경·학습 파이프라인)는 라벨이 모인 뒤 착수. **라벨링 완료 (2026-08-29)**: 사용자가 408장 전량 라벨링(ok 405 / skipped 3). 품질 검증 — 부호 오류 **0건**(3_left 89/89 전부 양수, 4_right 120/120 전부 음수), 2_front 140장 평균 −0.3도·표준편차 4.0도로 0에 밀집, 1_approach는 평균 −9.5도·표준편차 34.6도로 혼재(접근 각도가 다양하므로 정상). 컨택트 시트 24장 육안 확인 결과 화살표가 실제 보이는 횡단보도를 따라가며 줄무늬와 직각을 이룸. **규칙 해석 확인**: 사용자는 "가야 할 방향(left면 오른쪽으로)"으로 이해했고 문서는 "횡단보도 축"이라 적었으나, 몸이 틀어지면 횡단보도가 반대쪽으로 기울어 보이므로 **두 해석이 같은 결과**가 된다 — 재라벨링 불필요. | done |
| T69 | 각도 회귀 모델 학습 (A안 2단계) | P1 | T68 | BACKGROUND (2026-08-29): 라벨 405장이 모여 학습 착수. **기존 CV의 실제 오차를 사람 라벨 기준으로 먼저 측정(기준선 확보)**: `StripeDirectionEstimator`의 평균 절대오차 **34.5도**(중앙 35.9도, ±10도내 37%)로, 학습 없는 "항상 0도" 기준선 **32.4도**보다도 나빴다. 클래스별 중앙오차 front 2.7도 / left **61.3도** / right **45.1도** — 정답이 0에 가까울 때만 맞고 정작 중요한 이탈 상황에서 완전히 실패함이 수치로 확정됐다. 추가로 라벨의 18%(72장)가 CV의 ±60도 필터 밖이라 원리적으로 표현조차 불가능했다. **설계상 반드시 지킨 3가지**: (1) **EXIF 방향** — 라벨은 `label_angles.py`가 `exif_transpose`로 보정한 화면(세로) 방향 기준인데, 기존 분류기 캐시(`build_cache.py`)는 EXIF 보정을 **일부러 하지 않는다**(T57). 그대로 재사용하면 방향이 90도 어긋나므로 **자체 EXIF 보정 캐시**(`train/angle_cache/`)를 만들었다. => 앱 통합 시 이 모델에는 **화면 방향으로 회전된 프레임**을 넣어야 하며, 회전 없이 넣으면 약 90도 계통 오차가 난다(검증 가능한 예측). (2) **증강이 각도도 바꾼다** — 기존 파이프라인의 `RandomHorizontalFlip`/`RandomRotation`을 그대로 쓰면 이미지만 바뀌고 라벨은 그대로라 학습이 망가진다. 합성 화살표 이미지로 9개 케이스를 전수 실측해 규칙을 확정했다: **좌우반전 → −angle**, **rotate(θ) → angle − θ**. 이 변환을 하는 `AngleDataset`을 직접 구현. (3) **누수 없는 분할** — T1에서 확인된 연사 프레임 누수를 피하기 위해 `groupkfold_cv.py`와 **동일 기준**(EXIF 촬영시각 60초 간격 세션, 64x64 그레이 평균 픽셀차 <10 근사중복)으로 묶어 통째로 한 fold에 넣고 assert로 강제 검사. 실측: 405장 → 세션 118개 / 근사중복 클러스터 404개, fold별 81장 균등, 검사 통과. 산출물: `train/train_angle.py`(MobileNetV3-Small + 1출력 회귀 헤드, SmoothL1 loss, 5-fold CV). **측정 결과 (누수 없는 5-fold CV, 405장 전량 홀드아웃 예측):** 1차(에폭 8+12) 평균 17.4도 → 개선(에폭 10+25, 코사인 LR, 좌우반전 TTA) **평균 13.4도 / 중앙 7.5도 / ±10도내 58% / ±15도내 67%**. 대조군: 기존 고전 CV 34.5도, "항상 0도" 32.4도, "항상 평균" 32.5도 → **기존 CV 대비 오차 61% 감소**. 클래스별: 2_front **4.1도(±10도내 95%)**, 1_approach 10.5도, 4_right 17.4도, 3_left 24.5도. **left/right 약세의 원인 규명(추측 아님, 예측값 저장 후 분석):** 회귀직선 기울기 **0.709**(1.0이 이상적)로 모델이 큰 각도를 0쪽으로 축소 예측한다. 구간별 축소율 ±15~40도 0.79~1.01 / ±40~60도 0.75~0.84 / **±60~90도 0.57~0.66(평균오차 24~31도)**. 원인은 라벨 405개 중 167개가 ±15도에 몰린 분포 불균형이며 손실함수가 예측을 중앙으로 끌어당긴 것. **기각된 해결안(실측으로 기각):** 선형 역보정(pred을 기울기로 나눠 되돌리기)을 적용해본 결과 **오히려 악화**(13.4 → 15.8도, ±15도내 67→62%). 이 축소는 고칠 수 있는 편향이 아니라 **불확실성의 최적 반응**이며 억지로 늘리면 잡음까지 증폭된다. 근본 해결은 큰 각도 라벨 데이터 보강(현재 이탈 사진 209장뿐). **실패 양상은 안전한 쪽:** 방향(부호) 정확도가 **left 98% / right 98%** — 모델은 어느 쪽인지는 거의 완벽히 알고 크기만 보수적으로 낸다. 반대 방향을 가리키는 일은 거의 없다(기존 CV는 방향조차 틀렸다). **배포 모델:** `train/train_angle_final.py`가 라벨 전량으로 재학습 후 ONNX 내보내기. T43/T1의 함정(신형 exporter가 IR10/opset18로 모바일 비호환 파일을 만듦)을 피해 `dynamo=False`+opset 12를 쓰고, **내보낸 파일을 직접 열어 검증**(실측: ir_version=7, opset=12, output dims=[1,1]). `model/angle_meta.json`에 angle_scale·입력 방향·sha256·측정 성능을 함께 기록. **미해결 — 앱 통합 시 필수 확인:** 이 모델의 입력은 EXIF 보정된 **화면(세로) 방향**이라 앱에서 프레임을 회전해 넣어야 한다(기존 분류기는 보정 안 함 → 두 모델의 입력 방향이 다름). 회전 없이 넣으면 약 90도 계통 오차가 나며, 이는 실기기에서 검증 가능한 예측이다. 앱 통합은 아직 하지 않았다(사용자 결정 대기). | done |
| T70 | 각도 모델 앱 통합 — 화살표를 학습 모델이 구동 | P1 | T69 | BACKGROUND (2026-08-30): T69 결과(오차 61% 감소)를 보고 사용자가 "지금 앱에 통합"을 선택. DONE: (1) `crosswalk_app/lib/services/angle_estimator.dart` 신설 — ONNX 세션을 별도로 열고 `estimate(CameraImage, rotationDegrees)`로 진행 방향 각도(도)를 낸다. 모델 해시 검증은 `Classifier`와 동일 정책. (2) **회전 보정** — 학습 라벨이 EXIF 보정된 화면(세로) 방향 기준이라 센서 버퍼를 그대로 넣으면 약 90도 계통 오차가 난다. `controller.description.sensorOrientation`만큼 시계방향 회전을 적용한다. 회전 방향은 추측하지 않고 `image` 패키지 4.8.0 소스(`copy_rotate.dart`의 `_rotate90`: `dst(x,y) = src(y, H-1-x)`)를 직접 읽어 **시계방향**임을 확인했다. (3) **성능** — 전체 프레임을 RGB로 디코딩하지 않고 224x224 격자만 YUV에서 직접 샘플링하며 회전을 인덱스 매핑으로 처리한다(약 92만 픽셀 루프 -> 약 5만 회, 분류기의 전체 디코딩보다도 싸다). 또한 분류기 스로틀(5프레임 주기)과 겹치지 않게 `% 10 == 3`에 걸어 두 모델이 같은 프레임에서 연달아 돌지 않게 했다. (4) **게이트** — 회귀 모델은 신뢰도를 내지 않으므로, 분류기가 낸 상태가 front/left/right/approach일 때만 각도를 받는다(none이면 각도라는 개념 자체가 없다). 무판정(`_noCall`)에서도 받지 않는다. 떨림·깜빡임 억제는 T67의 `StripeAngleSmoother`를 `minConfidence: 0.0`으로 재사용. (5) 각도 모델 초기화 실패는 치명적이지 않게 처리 — 각도만 못 쓰고 화살표는 기존 좌/우 표시로 동작한다. (6) 화면 디버그 표시를 모델 각도(스무딩값+원시값)로 바꿔, 실기기에서 회전 보정이 맞는지 바로 확인할 수 있게 했다. 검증: `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). `test/angle_estimator_test.dart` 신설 — 회전 매핑을 0/90/180/270도 전부 고정(좌상단 대응, 범위 내, 일대일, 4회 회전 항등), displaySize 가로세로 교환, angleScale 상수 동기화(90.0) 검증. CAVEAT(중요): **실기기 미검증**이다. 특히 회전 보정이 실제 기기에서 맞는지는 T57(EXIF/센서 방향)이 여전히 미해결이라 확인이 필요하다 — 틀렸다면 화면 각도 표시가 실제와 약 90도 어긋나는 형태로 드러난다. 큰 각도 과소평가(T69 기울기 0.709)도 그대로 남아 있어 심한 이탈에서 화살표가 덜 꺾인다. | done |
| T70-2 | 각도 모델 전처리 버그 수정 — 축소 시 평균 누락 | P0 | T70 | BACKGROUND (2026-08-30): 실기기 스크린샷 6장에서 각도가 전부 0 근처(원시 0/2/5/-15/5/-9, 표준편차 약 7.5)로 나왔다. 교차검증 13.4도였던 모델이 그럴 리 없어 **배포된 ONNX로 직접 갈라 원인을 규명**했다(추측 아님). **(1) 모델은 정상**: 원본 이미지 + 학습 전처리로 돌리면 평균오차 12.3~15.1도, 예측범위 -77~67도로 정상 작동. **(2) 회전 로직도 정상**: 저장된 센서 방향 픽셀을 앱과 동일하게 90도 시계 회전한 경우가 EXIF 보정본과 **수치까지 완전히 일치**(15.1도/표준편차 34.9 동일) — `sensorCoord` 매핑이 옳음을 확인. **(3) 진짜 원인 = 전처리 두 곳이 학습과 어긋남**: (a) 학습은 288 리사이즈 후 중앙 224 크롭인데 앱은 전체를 곧바로 224로 압축해 화각이 넓었다. (b) **더 큰 원인** — 앱이 1280폭을 224로 줄이면서 점 하나씩만 찍어 읽어(최근접) 줄무늬 텍스처가 앨리어싱으로 뭉개졌다. 학습은 평균을 내는 BILINEAR 리사이즈를 썼다. 전체 405장 실측: 현재 앱 16.6도 / 크롭만 고치면 16.0도 / 크롭+2x2평균 13.7도 / **크롭+3x3평균 13.3도**(학습 전처리 기준 12.3도에 근접). 즉 평균 처리가 지배적 요인이었다. DONE: `_preprocess`에 `cropRatio`(224/288) 중앙 크롭과 3x3 격자 평균을 도입. 상수는 테스트로 고정. **노트북 화면 촬영도 실제 요인임을 함께 측정**: 수정된 전처리 기준 실제 촬영 14.2도 vs 노트북 화면 촬영 21.6도(중앙 9.1 -> 17.5도, 표준편차 37.0 -> 24.3). 축소·색조·주변 잡음이 겹쳐 학습 분포 밖으로 밀려난다 — 사용자 질문("노트북 화면이라 그런가?")에 대한 답은 **둘 다 맞다**이며, 정확한 평가는 실제 야외 촬영이 필요하다. 검증: `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). CAVEAT: 수정 효과는 파이썬으로 Dart 알고리즘을 정확히 재현해 측정한 것이며, **실기기에서 직접 확인하지는 못했다**. | done |
| T70-3 | 화살표 각도 검증 + 스무딩 지연 완화 | P1 | T70-2 | BACKGROUND (2026-08-30): 전처리 수정 후 각도가 실제로 다양하게 변하기 시작했으나(−70~+19도), 사용자가 "실제 횡단보도와 일치하지 않는 것 같다, 직접 검증해봐"라고 요청. **정량 검증 (라벨 405장, 앱과 동일한 수정 전처리로 예측):** 평균오차 13.3도 / 중앙 8.5도, **상관계수 0.896**, |정답|>10도에서 **방향(부호) 일치율 95%** — 부호 반대나 무작위가 아님이 확인됐다. 오차 분포: 10도 이내 56% / 20도 이내 77% / 30도 초과 11%. **시각 검증:** 정답(초록)과 예측(빨강) 화살표를 실제 사진에 겹쳐 그린 컨택트 시트로 확인 — 잘 맞는 경우는 두 화살표가 거의 겹치고, 크게 틀리는 경우는 **정답이 ±63~75도인 극단 각도에 몰려 있었다**. **정답 각도 크기별 성능(핵심 발견):** 0~10도 6.5도 / 25~45도 14.2도(방향 95%) / 45~60도 16.0도(방향 97%) / **60~90도 23.6도**(방향 97%, 축소율 0.73). 즉 **45도 미만 평균 8.9도(실용적) vs 45도 이상 19.2도(부족)**. 방향은 어느 구간에서든 95~97% 맞으므로 반대로 가리키는 일은 거의 없고, 큰 각도에서 크기를 과소평가한다. **추가 발견 — 스무딩 지연:** 사용자 스크린샷에서 표시값이 원시값보다 크게 뒤처졌다(표시 9도/원시 24도, 표시 10도/원시 29도). 연사 프레임(사실상 연속 비디오)으로 프레임간 예측 변화량을 실측한 결과 평균 8.1도, 이를 바탕으로 EMA 계수별 특성을 계산: 0.35 -> 응답 0.95초·떨림 3.7도 / 0.60 -> 응답 0.56초·떨림 5.3도. 모델 자체 오차가 13도대라 떨림 5.3도는 그 안에 묻히므로 **계수를 0.35 -> 0.60으로 올려 지연을 절반 가까이 줄였다**. 검증: `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). **남은 한계(데이터 문제):** 45도 초과 구간 개선은 해당 각도대의 라벨 보강이 필요하다. 현재 라벨의 42%가 45도 이상이지만 그 구간에서 축소 예측이 남아 있다. | done |
| T71 | 데이터 보강 1차 — 신규 174장 반영 + 누수 없는 재측정 | P0 | T58 | BACKGROUND (2026-09-02): 사용자가 앱에서 "직진(front)이라 하는데 실제로는 직진이 아니다"라고 보고했고, T58(상태 판정 데이터 보강)이 정확히 그 미착수 과제였다. 사용자가 신규 사진 174장을 촬영해 클래스 폴더로 분류했다. **신규 분포**: approach 79 / right 29 / front 32 / none 19 / left 15 -> 전체 637 -> 811장. approach 59 -> 138장(2.3배)으로, 가장 부족했던 클래스에 집중됐다. **데이터 품질 검사(전수)**: EXIF 촬영시각 누락 0장(811장 전부 보유, 세션 분할 가능). 근사중복(픽셀차<10) 클러스터 810/811 = 99.9% 고유 — 과거 843장 시절의 연사 중복 문제(93.6%)는 재발하지 않았다. 신규 174장은 26세션(장당 6.7장), 기존은 148세션(장당 4.3장). **각도 라벨 155장 추가**(사용자 직접 라벨링, `train/label_angles.py`): 408 -> 563행(ok 558, skipped 5). 품질 검증 통과 — 부호오류 0/558(3_left 104장 전부 양수, 4_right 149장 전부 음수), 2_front 172장 평균 -0.3도/표준편차 3.9도/|각도|>20도 0장. |각도|>45도 라벨이 170 -> 210장(신규 40장)으로 늘어 T70-3이 남긴 극단 각도 부족 문제도 일부 보강됐다. **누수 없는 5-fold 교차검증 재측정**(`train/groupkfold_cv.py`, 세션 174개·근사중복 810개 단일 fold 귀속 assert 통과): 기준값 637장 결과는 `train/groupkfold_5class_out_637baseline/`와 `train/groupkfold5_log_637baseline.txt`에 보존했다(T53 규칙 — 실험별 산출물 분리). **결과 (811장 vs 637장)**: approach recall **75.3% -> 91.3%**(+16.0%p), approach precision **61.7% -> 76.4%**(+14.7%p), approach 오경보(진입 전 -> 편향) **11.7% -> 4.3%**(-7.4%p) — 사용자가 보고한 직진 오판이 정확히 이 개선분이다. right recall 70.5% -> 79.1%(+8.6%p), front recall 77.7% -> 79.1%, front precision 86.3% -> 88.9%, none recall 83.0% -> 83.1%(동일), 전체 정확도 79.5%(507/638) -> 82.1%(666/811). **단, left는 악화: recall 87.6% -> 76.9%(-10.7%p).** 노이즈가 아님을 동일 이미지 짝비교로 확인했다 — 같은 89장에서 637모델 78/89 -> 811모델 71/89(개선 +1, 악화 -8, McNemar p~0.039). 대조적으로 right는 82/120 -> 95/120(+15/-2), front 104/140 -> 110/140(+15/-9), approach 46/59 -> 52/59(+9/-3)로 모두 개선됐다. **원인은 클래스 불균형으로 추정**: 신규 174장 중 left는 15장뿐(approach 79, right 29)이라 left(104장)가 이제 전체 최소 클래스가 됐다. 신규 left 15장 자체 정확도도 60.0%(9/15)로 기존(79.8%)보다 낮으나 표본이 작아 단정하지 않는다. 확인 방법: left 사진 보강 후 동일 CV를 돌려 left recall이 87% 선을 회복하는지 측정. **결정 (사용자, 2026-09-02)**: 배포를 보류하고 left 사진을 더 촬영해 함께 학습하기로 했다(제안 A=즉시배포 / B=left 보강 후 배포 중 **B 선택**). 근거는 왼쪽 이탈 미경고가 시각장애인 사용자에게 직접적 위험이고(놓칠 확률 12.4% -> 23.1%), approach 개선분은 며칠 늦춰도 사라지지 않는다는 것. **미완료(이 커밋 범위 밖)**: (1) left 사진 30~40장 추가 촬영 — 사용자 진행 중, (2) 추가 후 CV 재측정, (3) 전체 학습 -> ONNX 내보내기 -> 앱 반영, (4) 각도 모델 재학습(라벨 558장 확보 완료, 학습은 미실행). 이 커밋은 **측정 결과와 라벨 데이터만** 담으며 앱 코드는 건드리지 않았다(`crosswalk_app/` 변경 0). 주의: `image/`는 `.gitignore` 대상이라 사진 자체는 저장소에 없다 — 재현하려면 원본 사진이 필요하다. | in-progress |
| T72 | 화살표 중앙 고정 + 판정 흔들림 원인 규명 | P1 | T71 | BACKGROUND (2026-09-04): 사용자가 실기기 스크린샷 2장과 함께 두 가지를 보고했다 — (1) 같은 자리에서 상태 판정이 거의 초 단위로 바뀐다(같은 장면인데 한 장은 "직진 57%", 다른 장은 "앞에 횡단보도 71%"), (2) 화살표가 이탈 방향으로 위치까지 옮겨 다니는데 위치는 중앙 고정하고 방향만 바꿔달라. **(2) DONE**: `StateFieldPainter.paint`에서 left(0.30)/right(0.70) 오프셋을 제거하고 세 상태 모두 `size.width/2`에 그린다. 위치라는 중복 채널 하나가 빠지지만 색·문구·가장자리 펄스·음성·진동은 그대로라 색각이상 대비는 유지된다. 검증: `test/camera_screen_test.dart`에 `Canvas.translate` 호출을 기록하는 대역 캔버스(`_TranslateRecordingCanvas`)를 넣어 front/left/right 각각 각도 유무 두 경우, 총 6개 테스트로 중앙(150,100) 고정을 직접 확인한다 — 픽셀 렌더링 없이 위치를 잡는다. 위치가 더 이상 구분 채널이 아니므로 기존 테스트의 설명 문구("형태와 위치로 전달")도 사실에 맞게 고쳤다. **(1) 원인 규명 — 수정 없음, 측정만**: 추측하지 않고 배포 ONNX로 직접 쟀다. EXIF로 같은 세션·5초 이내·같은 클래스인 연속쌍 157쌍(간격 중앙 2초 = 사용자가 말한 "초 단위"와 같은 시간축)을 뽑아 두 프레임의 판정을 비교했다. **결과: 21.0%(33/157)에서 판정이 바뀐다.** 최빈 조합이 `approach -> front` 8건으로, 사용자 스크린샷과 정확히 일치한다. 최고 확신도의 프레임간 변화량은 평균 12.8%p(중앙 6.6, 최대 54.8). **후보 대책 3개를 전부 실측해 전부 기각했다**: (a) **히스테리시스**(현재 라벨을 이기려면 마진을 요구) — 마진 0.05~0.30 전 구간에서 불필요한 전환이 33건 -> 32건으로 거의 그대로였고, 0.30에서는 정당한 전환이 149 -> 147로 오히려 줄었다. 이유는 명확하다: 판정이 뒤집힌 쌍에서 새 라벨이 이전 라벨을 앞선 폭이 **중앙 60%p**(25분위 36%p, 최소 14%p)다 — 경계에서 애매해 흔들리는 게 아니라 모델이 확신을 갖고 반대 답을 낸다. 실제로 811장 CV 전체에서 top1-top2 마진이 0.10 미만인 경우는 2.2%뿐이고, 정답이 approach/front인 310장 중 approach-front 동률은 3장(1.0%)에 불과하다. (b) **평균 구간 확대**(현재 `Classifier._smoothingWindow` 5) — 3프레임 이상 연속 시퀀스 77개로 구간 1~5를 실측했다. 불필요한 전환은 20 -> 25 -> 23 -> 22 -> 21건으로 **줄지 않고**, 정당한 전환 감지가 106/118 -> 58/118로, 정확도가 82.3% -> 52.2%로 무너진다. CAVEAT: 이 시퀀스는 프레임 간격이 2초라 앱의 실제 간격(약 0.17초, 30fps/5스로틀)보다 훨씬 길다 — 앱의 5프레임 창은 약 0.83초 폭이므로 여기 수치를 그대로 앱에 대입할 수는 없다. 다만 "시야가 조금 달라지면 예측이 크게 흔들린다"는 성질 자체는 동일하게 나타난다. (c) **재학습(데이터 보강)** — 같은 157쌍을 누수 없는 CV 예측으로 비교하니 637 모델 27.5%(39/142) vs 811 모델 26.1%(41/157)로 거의 차이가 없다. 즉 T71의 174장 보강만으로는 흔들림이 해결되지 않는다. **결론**: 표시 단계의 필터로 고칠 수 있는 문제가 아니다. 원인은 모델 자체의 **시점 변화에 대한 예측 분산**이며, 근본 해결은 (i) 같은 지점을 여러 각도·거리에서 찍은 데이터 보강, 또는 (ii) 더 강한 백본/시간축을 입력으로 받는 구조 변경이다. 사용자 승인 없이 임의로 진행하지 않고 측정 결과만 보고했다. 검증: `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). 로컬 `flutter test`는 이 머신 고유 문제("Connection closed before test suite loaded", CLAUDE.md 기록)로 실행 불가 — CI로 검증. | in-progress |
| T73 | 분류기 임계값 실측 확정 (0.50 -> 0.40) | P1 | T71 | BACKGROUND (2026-09-04): T51 이래 front/none/approach 임계값 0.50은 코드에 "재학습 후 확정"이라 적힌 **잠정값**이었고, 한 번도 측정으로 정해진 적이 없다. 사용자가 "지금 할 수 있는 게 뭐냐"고 물어, 재학습 없이 **이미 저장된 누수 없는 5-fold CV 확률**로 격자 탐색을 돌렸다. DONE: front/none/approach 0.50 -> **0.40**. **이탈 임계값(0.55)은 건드리지 않았다** — 그래서 좌/우 이탈 recall과 front 오경보가 변하지 않는다. 낮춘 효과는 "확신이 애매해 침묵(무판정)하던" 경우를 정상 판정으로 돌리는 것뿐이다. **실측 (배포 중인 637장 모델, `train/groupkfold_5class_out_637baseline`)**: front recall 77.7% -> 82.3%, none recall 83.0% -> 84.3%, left/right recall 87.6%/70.8% -> 변화 없음, front 오경보 7.7% -> 7.7%(변화 없음), 무판정 6.7% -> 4.4%. 811장 모델에서도 같은 방향(front 79.1% -> 82.6%, none 83.1% -> 85.5%, 무판정 5.3% -> 3.0%, 이탈·오경보 불변). **안전 지표를 따로 쟀다**: 실제 이탈인데 "직진"이라 오도하는 경우가 202장 중 7 -> 8장(1장 증가)으로 사실상 불변이고, 침묵으로 놓치는 경우는 오히려 43 -> 42장으로 줄었다. 즉 놓침 1건이 오도 1건으로 바뀐 것이 전부다. **덤으로 T72의 흔들림에도 도움**: 같은 장소 2초 간격 157쌍의 판정 변동률이 21.0% -> 18.5%로 줄었다(애매한 경계를 넘나드는 횟수가 줄어서). **0.30은 기각**: 지표는 더 좋지만(front 83.1%, 무판정 2.2%) 5-class에서 0.30은 무작위(0.20)의 1.5배에 불과해 "직진"이라 말하기엔 근거가 약하다고 판단했다. 현재값을 어떤 지표에서도 손해 없이 이기는 조합이 124개 있었고 그중 표시 신뢰도가 납득 가능한 0.40을 골랐다. 검증: `thresholdsForTest`를 `@visibleForTesting`으로 노출하고 `test/classifier_test.dart`에 상수 고정 테스트 3개 추가 (값 자체, 이탈이 나머지보다 높을 것, 전부 무작위 수준보다 충분히 높을 것). 상수가 조용히 바뀌면 판정 성향이 통째로 달라지는데 에러는 안 나기 때문이다. 기존 임계값 관련 테스트의 문구(0.50)도 사실에 맞게 갱신했다. `flutter analyze` 3 issues(기존 baseline과 동일, 신규 0). CAVEAT: 격자 탐색의 주 근거는 811장 CV이고 637장 모델에서 재확인하는 방식이었다. 두 모델 모두에서 같은 방향으로 개선되므로 모델 교체(T71 보류 중)와 무관하게 적용 가능하다고 판단했다. 실기기 미검증. | done |
| T74 | 각도 라벨 재작성(v2) + 위치 라벨 신설 + 각도 기반 판정 검증 | P1 | T71 | BACKGROUND (2026-09-04): 사용자가 (1) 기존 각도 라벨의 일관성이 떨어진다며 전체 재라벨링을 요청하고, (2) 라벨링 시 **횡단보도 내 좌우 위치에 따라 위험 가중을 더했다**고 밝혔으며(가장자리에서 바깥으로 향하면 각도를 크게), (3) 각도 기반 상태 판정의 타당성 검증을 요청했다. **(1) v2 라벨 604장**: 617장 전량 재라벨(중간에 85장을 "과적합 방지" 목적으로 건너뛴 것을 확인해 다시 매기게 했다 — 과적합은 누수 없는 CV로 거르는 것이지 라벨을 빼서 막는 것이 아니고, 빼면 학습량만 줄고 v1 대비 비교도 오염된다). 품질: 부호오류 0/604, **front 표준편차 3.9도 -> 0.7도**로 크게 개선. front 범위 ±12.2도 -> **±2.6도**, 이탈 시작 ±10도 -> ±5.0도. 경계(10~25도) 표본 26장(4.7%) -> **188장(31.1%)**. v1과 같은 478장 비교 시 절대변화 평균 15.4도(left 25.9, right 22.5, front 2.7), 부호 뒤집힘 0장 — 방향 기준은 그대로 두고 크기를 전반적으로 줄인 재작성이다. **(2) 위치 라벨 신설 — `train/label_position.py`, `train/position_labels.csv` 602장**: 5단계 부호 척도(-2 왼쪽끝 ~ +2 오른쪽끝). 사용자의 "횡단보도를 감지하니 위치도 안다"는 가정은 **성립하지 않는다** — 배포 ONNX 출력을 직접 확인한 결과 분류기 [1,5](클래스 확률), 각도 [1,1](스칼라)뿐이고 위치를 내는 출력이 없다. **위험 가중이 데이터에서 확인됐다(594장)**: 3_left 위험정렬 41.5도 vs 중앙 23.9 vs 반대 21.7 (+17.6/+19.8), 4_right 위험정렬 36.2 vs 중앙 24.0 vs 반대 18.5 (+12.2/+17.7). 위치 등급별로 끝 51.5 > 치우침 37.3 > 중앙 24.0으로 단조 증가. 2_front는 위치와 무관(모든 위치에서 평균 ±1도 이내, 표준편차 0.6~0.7) — 가중을 이탈에만 적용했다. **"반대" 집단이 중앙보다도 작다는 비대칭**이 의도적 가중의 증거다(단순히 가장자리에서 실제로 더 틀어져 있다면 방향과 무관하게 커야 한다). 선형 모형 |각도| = 27.9 + 7.3*(위험쪽 위치등급) - 2.1*(right), 즉 **위치 한 단계당 +7.3도**. **단, 통계적 제거는 실패**: R^2 0.204, 잔차 12.5도(원래 14.0도, 10% 감소뿐), 보정 후에도 집단 평균이 반대 19.8 / 중앙 24.0 / 위험쪽 30.7로 맞지 않는다. 경계 폭(±2.6~5도)보다 잔차가 훨씬 커서 순수 기하 각도 복원은 불가능하다. **(3) 각도 기반 상태 판정 — 실측 후 기각**: 주 기준을 "각도규칙 판정 vs 폴더 라벨 일치율"(각도 라벨과 독립)로 미리 확정하고 비교했다. v1 최적 ±10도 **92.9%**, v2 최적 ±5도 89.7%, 분류기(811모델 3지 강제) 88.2%. 정확도는 각도규칙이 앞선다(+4.7%p). **그러나 같은 장소 2초 간격 157쌍의 판정 변동률이 분류기 21.0% vs 각도규칙 38.2%로 두 배 가까이 나쁘다** — 각도 자체가 프레임 간 평균 18.0도 흔들리는데 경계 폭이 그보다 좁기 때문이다. 사용자가 가장 먼저 보고한 문제(T72)가 흔들림이라 정확도 +4.7%p와 맞바꾸지 않기로 하고 **분류기 유지**를 결정했다. **주의 — 절대오차 비교 금지**: v2 평균오차 7.5도가 v1 12.2도보다 좋아 보이지만 라벨 각도 자체가 작아져 기준선(항상 0도)도 31.2 -> 16.5도로 내려갔다. 기준선 대비 상대오차는 v1 0.391 / v2 0.457로 **v2가 오히려 나쁘다**. v2에는 (a) front 구간 축소와 (b) 위험 가중이 동시에 들어가 어느 쪽이 손해를 냈는지 분리할 수 없다. 산출물 보존: `angle_labels_v1_558.csv`, `angle_cv_predictions_v1_558.csv`, `angle_train_log_v1_558.txt`, `model/crosswalk_angle_v1_558.pt`, `angle_labels_v2_before_redo85.csv`. | done |
| T75 | v2 각도 모델 배포 + "각도" 표시를 "보정"으로 정정 | P1 | T74 | BACKGROUND (2026-09-04): T74에서 v1/v2 중 어느 라벨로 갈지 사용자가 **v2 채택**을 선택했다. 근거는 (a) 일관성이 압도적(front 표준편차 0.7도 vs 3.9도), (b) 위험 가중이 경고 목적에 부합 — 위험할수록 화살표가 크게 꺾인다, (c) 모델이 실제로 학습한다는 것. 상대오차 열세(0.457 vs 0.391)는 위험 가중을 일부러 넣은 결과라 성능 저하로 보지 않았다. DONE: (1) v2 라벨 604장 전량으로 배포 모델 재학습 -> ONNX 내보내기 -> `crosswalk_app/assets/model/crosswalk_angle.onnx` 교체(+ sha256). (2) **화면 표시 문구 정정** — `각도 -10도 (원시 -10도)` -> `보정 -10 (원시 -10)`. 이 값은 횡단보도가 실제로 몇 도 기울었는지를 재는 측정값이 **아니라** 위험 가중이 섞인 보정량이므로(T74: 위치 한 단계당 +7.3도), "각도"라 쓰면 사용자가 실제 방향으로 읽어 오도된다. (3) `train/train_angle_final.py`의 메타(`model/angle_meta.json`) 갱신 — 출력 설명을 `walking_direction_degrees`에서 `correction_magnitude_degrees (risk-weighted by lateral position)`로 바꾸고, CV 수치를 v2 기준(7.5/4.8)으로 교체하며 기준선(16.5)과 라벨셋 이름을 함께 남겨 v1 수치와 혼동되지 않게 했다. (4) `AngleEstimator` 문서 주석을 v2 기준으로 재작성 — 위험 가중 경고, 상대오차 비교 주의, 상태 판정에 쓰지 않는 이유를 명시. CAVEAT: 실기기 미검증. 배포 전 ONNX는 `/tmp`에 백업했고 v1 학습본은 `model/crosswalk_angle_v1_558.pt`에 남아 있다. | done |
| T56 | 학습 입력 224px 캐시로 전처리 병목 제거 | P1 | — | BACKGROUND (2026-08-23 실측): `image/` 원본이 전부 4000x3000(12MP, 평균 4.8MB)이고 torch는 CPU 전용 빌드(`2.12.0+cpu`, CUDA 불가, 스레드 4)라, 매 에폭 학습 456장을 12MP에서 디코딩하는 비용이 MobileNetV3-small 연산보다 크다. 세션 단위 5-fold 1회가 약 4.75시간(에폭당 2.9분)이었다. DONE: `train/build_cache.py` 신설 — 원본을 `train/cache224/`에 224x224 **PNG(무손실)**로 미리 변환하고, `groupkfold_cv.py`에 `USE_CACHE` 플래그를 추가해 `ListDataset`이 캐시에서 읽도록 했다. 결과가 바뀌지 않는 근거(추정이 아니라 검증): TRAIN_TF/EVAL_TF의 첫 연산이 고정 `Resize((224,224))`이고 증강(Flip/Rotation/ColorJitter)은 그 뒤에 오므로 여기까지는 미리 계산 가능하다. `build_cache.py --verify` 최대 픽셀 차이 **0.00000000**, `ListDataset` 캐시 ON/OFF 텐서 차이 **0.0000000000**. 속도: 장당 376.6ms -> **15.8ms (24배)**, 40장 실측. 증분 동작: 캐시가 원본보다 새것이면 건너뛰고, 원본이 사라졌거나 다른 라벨 폴더로 옮겨지면 옛 캐시를 정리한다. 학습 시작 시 `build_cache.build()`가 자동 호출되므로 **사용자는 이미지를 폴더에 넣기만 하면 된다.** 세션 분할과 근사중복 판정은 **원본을 계속 사용** — 캐시 픽셀로 바꾸면 64x64 다운샘플 경로가 달라져 중복 클러스터 경계가 흔들리고 지금까지의 수치와 비교가 깨지기 때문이다. 주의: `exif_transpose`를 **일부러 적용하지 않았다** (T57 참조). | done |
| T57 | EXIF Orientation 무시가 앱 추론과 정렬되는지 실기기 확인 | P1 | 실기기 1대 | FINDING (2026-08-23, T56 작업 중 발견): `image/` 637장 중 **625장이 EXIF Orientation=6** (표시하려면 90도 회전 필요)인데, 학습 경로 `ListDataset.__getitem__`은 `Image.open(path).convert("RGB")`만 하므로 **회전을 적용하지 않는다.** 즉 모델은 사실상 90도 눕혀진 이미지로 학습돼 있다. 같은 파일 안에서도 `dup_clusters`(groupkfold_cv.py:173)는 `exif_transpose`를 적용하므로 코드 내 처리도 일관되지 않다. 추정(미검증): Android CameraImage는 통상 **센서 좌표계**로 프레임을 주고, `classifier.dart`의 `convertBGRA8888`/`_convertYUV420`은 회전을 전혀 하지 않는다. 사진 역시 센서 좌표계로 저장된 뒤 EXIF로만 회전을 표기하므로, **EXIF를 무시하는 현재 동작이 오히려 앱과 정렬되어 있을 가능성이 높다.** 이것이 사실이라면 학습에 `exif_transpose`를 추가하는 '수정'은 train/serve 불일치를 **새로 만드는** 개악이 된다. 데이터셋 내부 영향은 현재로선 관측되지 않았다: Orientation=1인 12장의 정확도 10/12 = 83.3% vs Orientation=6인 625장 509/625 = 81.4%로 차이가 없고, 12장 중 11장이 가장 쉬운 `none` 클래스다. (표본 12장이라 통계적 결론을 낼 수 없다.) ACCEPTANCE: 실기기에서 앱이 실제로 모델에 넣는 프레임을 덤프해 학습 이미지와 방향이 같은지 눈으로 대조할 것. 다르면 어느 쪽을 기준으로 통일할지 결정하고 양쪽을 함께 바꿀 것. **코드만 보고 어느 한쪽을 고치지 말 것** — 지금은 두 곳이 함께 틀렸거나 함께 맞은 상태이며, 한쪽만 건드리면 확실히 깨진다. | todo |

## F. Design-mockup implementation (approved 2026-07-18)
Source: user-approved design mockup (published as an artifact; reference-only file `crosswalk_design.html` in the session scratchpad — NOT committed to this repo). Applies the pedestrian-signal color language (green=normal, red/orange=off-course, blue=interactive controls) across 4 mockup screens (onboarding, 3-state detection, settings). These three tasks carry the actual implementation of the previously-decision-gated T17/T19/T36 (see cross-references in those rows). No Open Question is outstanding for these three (design + disclaimer copy user-approved) — two persistence sub-decisions are recorded below as in-row caveats, NOT as blockers.

| ID | Task | Priority | Depends on | Acceptance Criteria | Status |
|---|---|---|---|---|---|
| T38 | CameraScreen visual-language redesign (signal color palette + voice/vibration active indicators) | P2 | — (design approved 2026-07-18) | BACKGROUND: `crosswalk_app/lib/screens/camera_screen.dart` currently uses arbitrary Material default colors (`Colors.green`/`red`/`orange`). The approved mockup replaces these with the pedestrian-signal palette (normal=`#35c46a`, left-deviation=`#ff5a5f`, right-deviation=`#ff9f40`, interactive-blue=`#3aa0ff`) and surfaces "voice playing / vibrating" state as icons in the top-right — information the current code does NOT expose (`FeedbackService` does not surface playback/vibration state outward). ACCEPTANCE: (1) replace the color constants with the palette above; (2) add a playback/vibration-state callback or getter to `FeedbackService` (currently none exists); (3) show 2 status pills (voice / vibration) on `CameraScreen`; (4) add a top-right settings-entry gear icon button routing to the T39 SettingsScreen. Verify per docs/DefinitionOfDone.md. DONE (2026-07-18): implemented on branch `feat/T38-camera-screen-visual-redesign`, merged to master via PR #35 (squash-merge, master `38e9e25..5795b5d`), synced into develop (commit `a60c958`; reported `git diff origin/master origin/develop --stat` = 0 lines). WHAT LANDED against the 4 acceptance criteria: (1) SATISFIED — palette constants in `crosswalk_app/lib/screens/camera_screen.dart` replaced with `_colorFront=#35C46A`, `_colorLeft=#FF5A5F`, `_colorRight=#FF9F40`, `_colorAccent=#3AA0FF` (matching the approved mockup). (2) SATISFIED — `FeedbackService` (`crosswalk_app/lib/services/feedback_service.dart`) now exposes outward playback/vibration state via `isSpeaking`/`isVibrating` `ValueNotifier<bool>` (previously no outward signal existed). (3) SATISFIED — two top-right status pills (voice / vibration) added on `CameraScreen` via `ValueListenableBuilder`. (4) PARTIALLY SATISFIED — the top-right settings-entry gear button UI is present, but its `onTap` is a no-op (`// TODO(T39)`) because `SettingsScreen` does not exist yet; routing is intentionally deferred (see CONSTRAINT #1). ROBUSTNESS WORK (6 reviewer re-review rounds): vibration race fixed — `activateVibrationIndicator()` cancels the prior `Timer` before re-scheduling; speech race redesigned — callback-based approach (`setCompletionHandler` etc.) dropped in favor of `awaitSpeakCompletion(true)` + pure `await` sequencing + generation tokens (`_speechGeneration`/`beginSpeechGeneration`/`finishSpeechGeneration`), safety cross-checked against flutter_tts 4.2.5's Android plugin source (`FlutterTtsPlugin.kt`). CRITICAL safety-affecting fix: TTS `onError` does not resolve `speakResult`, so `_speak()` could wait forever, and vibration was previously sequenced to run only AFTER `_speak()` completed — risking vibration being blocked too; fixed by switching `alert()` to `unawaited(_speak(message))` (decoupling vibration from voice-completion wait) and adding a 10s timeout + broad `catch (e)` exception handling to `_speak()`. Accessibility: `Semantics(label:, value:)` on the pills now lets a screen reader distinguish active/inactive state (reviewer caught that the initial labels were state-independent fixed strings — fixed); settings-button touch target enlarged 36dp→48dp. Tests: 4 new in `crosswalk_app/test/feedback_service_test.dart` (2 vibration-race guards, 2 speech-race guards). `flutter analyze` re-run locally: same 6 pre-existing known issues, zero new (re-confirmed multiple times). Regression evidence: CI PASSED — GitHub Actions run 29613718003, "Flutter 테스트" step logged "39 tests passed" (directly confirms the 4 new T38 tests actually ran and passed). CONSTRAINT #1 (do NOT overstate): acceptance criterion (4) is only PARTIALLY met — `_buildSettingsButton()`'s `onTap` is a no-op with a `// TODO(T39)` comment; the gear button UI exists but has NO routing because `SettingsScreen` (T39) is not built yet. Routing to be wired in a separate commit after T39 lands. CONSTRAINT #2 (do NOT overstate): `announceError()` still uses `await _speak(message)`, so if TTS hits the 10s timeout the error screen can be delayed by up to that timeout (occurs at init stage only; still an improvement over the previous infinite-wait risk). Reviewer flagged this as Major but it was judged NOT safety-critical and left unchanged this scope — recorded as a future improvement, not fixed here. | done |
| T39 | SettingsScreen (new) — language selection + TTS-rate/vibration-strength sliders + accessibility placeholder | P2 | T4 DONE (2026-07-17; language list = 한국어 + 영어); T38 DONE (2026-07-18 — CameraScreen gear button UI exists but its `onTap` is a no-op `// TODO(T39)`; T39 must both build SettingsScreen AND wire this button's routing, per T38 CONSTRAINT #1) | BACKGROUND: fulfills the "selectable" requirement of T17 (a manual language-selection UI). Exposes as user-adjustable sliders the currently-hardcoded TTS rate (`0.5`) and vibration duration (`500ms`, `vibration`-package call site) in `crosswalk_app/lib/services/feedback_service.dart`. The "screen-reader optimization" toggle is a DISABLED placeholder only, because its governing standard (T3) is still undecided (⛔Open Q #5). ACCEPTANCE: (1) `AppLanguage` selection UI (Korean/English segmented control, from `crosswalk_app/lib/localization/app_strings.dart`); (2) TTS-rate slider; (3) vibration-strength (duration) slider; (4) disabled "screen-reader" toggle placeholder; (5) route to this screen from `CameraScreen`'s T38 gear icon; (6) values must actually take effect on `FeedbackService` at least within the session. CAVEAT (needs separate decision, NOT a blocker for this task): whether settings PERSIST across app restarts (e.g. `shared_preferences`) is out of this task's scope — session-only application satisfies acceptance; persistence tracked separately if the user requests it. DONE (2026-07-18): implemented on branch `feat/T39-settings-screen`, merged to master via PR #37 (squash-merge, master `09863b4..7c202ad`), synced into develop (commit `7681edc`; reported `git diff origin/master origin/develop --stat` = 0 lines). WHAT LANDED against the 6 acceptance criteria: (1) SATISFIED — `AppLanguage` selection UI is a `SegmentedButton<AppLanguage>` (Korean/English) sourced from `crosswalk_app/lib/localization/app_strings.dart` (`crosswalk_app/lib/screens/settings_screen.dart:103-124`). (2) SATISFIED — TTS-rate slider 0.1–1.0, 9 divisions (`settings_screen.dart:138-146`). (3) SATISFIED — vibration-duration slider 200–1000ms, 8 divisions (`settings_screen.dart:158-166`; label surfaces raw ms rather than an arbitrary 약함/보통/강함 mapping). (4) SATISFIED — disabled "screen reader optimization" placeholder `SwitchListTile(value:false, onChanged:null)` with an in-code note that T3/⛔Open Q #5 is still undecided (`settings_screen.dart:171-188`, class doc `:7-12`). (5) SATISFIED — T38's gear button `onTap` (previously a no-op `// TODO(T39)`) now routes via `Navigator.of(context).push(... SettingsScreen(...))` (`crosswalk_app/lib/screens/camera_screen.dart:286-293`), closing T38 CONSTRAINT #1. (6) SATISFIED — values take effect in-session: `feedback_service.dart`'s previously-hardcoded TTS rate `0.5` and vibration `500ms` are now mutable instance fields (`feedback_service.dart:33-34`) with runtime setters `updateLanguage()`/`updateSpeechRate()`/`updateVibrationDuration()` (`feedback_service.dart:123-143`); SettingsScreen calls them immediately on change (`settings_screen.dart:54-73`). Reviewer-driven robustness: `updateLanguage`/`updateSpeechRate` given `_speak()`-consistent try/catch (fire-and-forget from SettingsScreen, so a platform-channel exception can't escape to the root zone — `feedback_service.dart:125-129,134-138`). Shared-instance verified: CameraScreen passes its existing `_feedback` instance into SettingsScreen (`camera_screen.dart:288-292`), not a new one, so changes apply to the live service; reviewer traced that T38's speech/vibration race-guards (`_speechGeneration` generation tokens, `awaitSpeakCompletion`, vibration timer cancel-and-reschedule) remain intact under this change. Tests: 8 new — 4 in `crosswalk_app/test/feedback_service_test.dart` (T39 runtime setters) + 4 in new `crosswalk_app/test/settings_screen_test.dart` (initial values, language select, TTS-rate slider, vibration-strength slider). `flutter analyze` re-run locally: same 6 pre-existing known issues, zero new (re-confirmed multiple times). Regression evidence: CI PASSED — GitHub Actions run 29627878912, "Flutter 테스트" step logged "47 tests passed" (directly confirms the 8 new T39 tests ran and passed, not merely trusted). CONSTRAINT (do NOT overstate): settings apply within the current session ONLY — there is no persistence (`shared_preferences`), so restarting the app resets to FeedbackService's defaults (`settings_screen.dart:14-18`). This matches acceptance criterion (6)'s "at least within the session" and the in-row CAVEAT above; cross-restart persistence remains out of scope and would need a separate task if the user requests it. | done |
| T40 | OnboardingScreen (new) — integrates T19 posture guidance + T36 legal disclaimer | P1 | — (disclaimer copy user-approved 2026-07-18) | BACKGROUND: combines T19 (chest-mount posture guidance; Open Q #8 = lanyard/chest-mount, Open Q #14 = rear camera) and T36 (legal safety disclaimer; Open Q #9 = disclaimer required at first launch) into one first-run screen. On entry the guidance must ALSO be read aloud via TTS (design intent). ACCEPTANCE: (1) on app launch (see persistence caveat) this screen appears before `CameraScreen`, and the user must press a "확인했습니다" confirm button to proceed to `CameraScreen`; (2) disclaimer uses the user-approved copy VERBATIM: "이 앱은 횡단보도 이탈을 감지해 음성·진동으로 알려주는 보조 도구입니다. 흰지팡이·안내견·동행인의 판단을 대신하지 않으며, 최종 판단과 주의는 항상 보행자 본인에게 있습니다." (English translation to be written naturally at implementation time); (3) a code comment must mark the copy as "디자인 초안 문구 — 법률 검토 필요" (design-draft copy, legal review pending); (4) content read aloud via TTS on entry. CAVEAT (needs separate decision, NOT a blocker for this task): whether the screen shows only on FIRST launch (persisted flag) or on EVERY launch is undecided — implement one, note which, and flag persistence as a follow-up decision if first-launch-only is chosen. DONE (2026-07-18): implemented on branch `feat/T40-onboarding-screen`, merged to master via PR #39 (squash-merge, master `08e7bae..a06557a`), synced into develop (commit `c686e91`; reported `git diff origin/master origin/develop --stat` = 0 lines). WHAT LANDED against the 4 acceptance criteria: (1) SATISFIED — new file `crosswalk_app/lib/screens/onboarding_screen.dart` is the app root before `CameraScreen` (`crosswalk_app/lib/main.dart` root changed from `CameraScreen` to `OnboardingScreen`); a "확인했습니다" confirm button navigates to `CameraScreen`. (2) SATISFIED — disclaimer uses the user-approved copy VERBATIM: "이 앱은 횡단보도 이탈을 감지해 음성·진동으로 알려주는 보조 도구입니다. 흰지팡이·안내견·동행인의 판단을 대신하지 않으며, 최종 판단과 주의는 항상 보행자 본인에게 있습니다." — reviewer programmatically compared this char-by-char against the approved original and confirmed an exact match (97 chars, zero mismatch). (3) SATISFIED — the "디자인 초안 문구 — 법률 검토 필요" code comment is present (reviewer-confirmed). (4) SATISFIED — posture guidance + disclaimer are read aloud via TTS on screen entry (verified). REFACTOR: language-detection logic moved from `CameraScreen` to `OnboardingScreen`; `CameraScreen` now takes an `initialLanguage` optional parameter (falls back to its own detection when null), preserving backward compatibility. CRITICAL ISSUE FOUND & FIXED (reviewer, source-verified not on trust): the initial implementation had `OnboardingScreen` and `CameraScreen` each construct their own `FeedbackService()`; reviewer read the actual `flutter_tts` 4.2.5 source (`flutter_tts.dart:330` static `MethodChannel`, `FlutterTtsPlugin.kt:38` single `speakResult` slot) and showed these two instances implicitly SHARE the one native TTS channel, so a screen transition could race. Fix: `CrosswalkApp` (`main.dart`) now constructs the `FeedbackService` exactly ONCE and passes the SAME instance `OnboardingScreen`→`CameraScreen` (ownership tracked via an `_ownsFeedback` flag so the shared instance is NOT disposed on screen transition). Reviewer traced this fix end-to-end along the real app path (main.dart→OnboardingScreen→CameraScreen) and returned PASS. Tests: 4 new in `crosswalk_app/test/onboarding_screen_test.dart` (content display, TTS-on-entry, confirm-button navigation, and a shared-instance `identical()` check added specifically to cover the reviewer-flagged gap), plus 2 verbatim disclaimer regression guards added to `crosswalk_app/test/app_strings_test.dart`. `flutter analyze` re-run locally: same 6 pre-existing known issues, zero new (re-confirmed multiple times). Regression evidence: CI PASSED — GitHub Actions run 29631590513, "Flutter 테스트" step logged "54 tests passed" (directly confirms the 4 new onboarding tests ran and passed, not merely trusted). CONSTRAINT / PERSISTENCE CAVEAT (do NOT overstate — resolves the acceptance-criteria persistence caveat by choosing the every-launch option): the screen is shown on EVERY launch, NOT first-launch-only. Reason: a first-launch-only flag would require adding a new persistence package (e.g. `shared_preferences`), which is out of this task's scope; this is stated honestly in a code comment and here. Switching to first-launch-only requires a separate decision plus package approval. | done |
| T41 | CameraScreen 방향 안내 오버레이 + 유리질감(glass) HUD 리프레시 (design v2 §00) | P2 | — (design v2 user-approved; reuses T38 palette constants `_colorFront=#35C46A`/`_colorLeft=#FF5A5F`/`_colorRight=#FF9F40`/`_colorAccent=#3AA0FF` in `crosswalk_app/lib/screens/camera_screen.dart`) | BACKGROUND: approved design v2 §00 proposes overlaying a "direction-guidance corridor" (5 chained chevrons forming a guide line — front=straight+green / left=curving-left+red / right=curving-right+orange) on the camera preview, replacing the current flat black bottom status tray with a translucent blurred glass HUD bar, and adding a status-colored peripheral vignette (edge glow). CRITICAL HONESTY CONSTRAINT: `Classifier` is a 3-class CLASSIFIER (front/left/right + confidence), NOT an object detector or segmenter — it outputs NO coordinate/geometry data. Therefore this overlay is NOT a render of an actually-detected crosswalk; it is a GUIDANCE graphic that converts the classification result into a directional symbol. This must be reflected in code comments and naming: use `guidance`-family names, and PROHIBIT `detection`-family names for this overlay. ACCEPTANCE: (1) implement the direction-guidance corridor overlay on `CameraScreen` using a `CustomPainter`, driven ONLY by `ClassificationResult(label, confidence)` (already returned by `Classifier.processFrame()`), with chevron offset/rotation transitioning smoothly (animated) per state (front/left/right); (2) add a status-colored peripheral vignette at the screen edges; (3) replace the existing bottom status tray with a glass (blur) HUD bar — keep the logic, swap only the visuals; (4) change the confidence indicator to a thin status-colored line; (5) code comments must state "this overlay is a classification-result-based guidance symbol, NOT real object detection", using `guidance`-family naming and NO `detection`-family naming. Verify per docs/DefinitionOfDone.md. DONE (2026-07-18): implemented on branch `feat/T41-camera-guidance-overlay`, merged to master via PR #45 (squash-merge, master `9c27805..fde82e8`), synced into develop (commit `0d1e18e`; reported `git diff origin/master origin/develop --quiet` exit code 0 = no difference). WHAT LANDED against the 5 acceptance criteria: (1) SATISFIED — `GuidanceCorridorPainter` (new `CustomPainter`, `crosswalk_app/lib/screens/camera_screen.dart`) draws 5 chained chevrons, driven only by `ClassificationResult(label, confidence)` via `_updateGuidanceTarget()`; offset/rotation transition smoothly via `AnimationController` + `Tween<double>`/`ColorTween` (350ms). (2) SATISFIED — status-colored peripheral vignette via a `RadialGradient` `DecoratedBox`. (3) SATISFIED — bottom status tray replaced with a `BackdropFilter`(blur sigma 18)+translucent `Container` glass HUD; existing loading/status/retry logic verified byte-for-byte unchanged (reviewer traced this explicitly). (4) SATISFIED — confidence now also shown as a thin status-colored progress-bar-style line, alongside the existing percentage text. (5) SATISFIED — class/method doc comments explicitly state the guidance-not-detection honesty constraint; reviewer grepped the full diff for `detect`/`recogni` and confirmed every hit is a disclaiming comment, and all new identifiers are `guidance`-prefixed (`_guidanceLabel`, `_guidanceAnimController`, `_updateGuidanceTarget`, `GuidanceCorridorPainter`, etc.). REVIEWER FINDINGS (1 round, PASS with 2 Major follow-ups): Major #1 (BackdropFilter re-evaluated on every classification-frame `setState`, a real-time/battery risk on this always-on assistive app) — FIXED in the same PR by wrapping the glass-HUD subtree in a `RepaintBoundary` to isolate its paint layer from the per-frame rebuild. Major #2 (no widget test drives a real front→left→right transition through `_onFrame`/`_updateGuidanceTarget`, since `Classifier.processFrame()` never runs in these widget tests) — NOT fixed; recorded as a KNOWN LIMITATION rather than forced, because closing it would require adding a test-only hook into production `CameraScreen` code for a non-safety-critical animation, and this environment cannot run `flutter test` locally to iterate on it (CLAUDE.md). Minor findings (both left unchanged, out of scope): a grey vignette can show during loading while the corridor itself is hidden (cosmetic-only inconsistency); one pre-existing `withOpacity` call in the untouched error-overlay code was left as noted follow-up cleanup. Tests: 5 new in `crosswalk_app/test/camera_screen_test.dart` (2 widget tests: corridor hidden during `_hasError`, glass HUD `BackdropFilter` present even in error state; 3 `GuidanceCorridorPainter.shouldRepaint` unit tests: curve-only change, color-only change, no change). `flutter analyze` re-run locally (twice — before and after the RepaintBoundary fix): 4 issues, DOWN from the 6 pre-existing known issues (two `camera_screen.dart` issues the T41 diff itself resolved: an unused-labelColors-adjacent issue and a `withOpacity` call in the replaced bottom-tray code), zero new issues introduced. Regression evidence: CI PASSED — GitHub Actions run 29643067800, "Flutter 테스트" step logged "61 tests passed" (directly confirms the 5 new T41 tests actually ran and passed, not merely trusted — local `flutter test` cannot run in this dev environment per CLAUDE.md). CONSTRAINT (do NOT overstate): the guidance corridor's live animated direction/color transition under real classification results has NO automated test coverage (see Major #2 above) — this is an intentionally disclosed gap, not a hidden one; the corridor's *rendering logic* (chevron geometry, `shouldRepaint` contract) is unit-tested, but the *end-to-end drive path* from a real camera frame through to a specific painted curve/color is not. | done |
| T42 | Extend model to 4 classes (front/left/right/none) to recognize "no crosswalk" — retrain ONNX + wire the new class through classifier/UI/feedback/strings | P2 | T1 (model retrain, in-progress — 3-class recall/false-alarm still below target; MUST be merged with this dataset+retrain work to avoid retraining twice); T41 (in review, NOT yet done — corridor/glass HUD overlay assumes 3 states front/left/right; this task must NOT start until T41 is merged, to avoid overlay-render conflicts) | BACKGROUND (user-confirmed & decided, 2026-07-18): the user added 59 new images to the repo `image/` folder (captured 2026-07-08~09). 36 of them form a brand-new category `image/none/` (scenes with NO crosswalk visible — night sidewalks / paving blocks / parked cars, opened & confirmed by directly viewing the images this session); the remainder are new captures added to existing front(+7)/left(+8)/right(+8). Asked via AskUserQuestion how to use these images, the user explicitly chose "expand the model to 4 classes (front/left/right/none)" so the app can recognize a distinct "no crosswalk (none)" state, rather than the current 3-class Classifier. THIS IS A DIRECTION, NOT A COMPLETE SPEC — the items below are OPEN and must be decided at implementation time, not invented now. ACCEPTANCE (sub-parts): (1) RETRAIN: regenerate a 4-class ONNX via the `train/` pipeline (reuse/extend `train/eval_model.py` + `train/train_model.py`/`train/export_onnx.py`); this MUST be done jointly with T1's still-open 3-class retrain (T1 currently in-progress, recall/false-alarm below target) so the model is retrained ONCE for both goals — do NOT retrain separately. (2) CLASSIFIER: `crosswalk_app/lib/services/classifier.dart` output classes 3→4, softmax dimension, and threshold logic updated — OPEN: how the current asymmetric thresholds (front=0.85 / left,right=0.55, `classifier.dart:27-28`) should map onto 4 classes is to be re-derived at implementation time, NOT fixed here. (3) UI: `crosswalk_app/lib/screens/camera_screen.dart` — decide how the 'none' state renders relative to T41's corridor/vignette/glass HUD (e.g. hide the corridor entirely vs a distinct visual language); OPEN, decide at implementation time. (4) FEEDBACK: `crosswalk_app/lib/services/feedback_service.dart` `decideMessage()` currently stays silent only on 'front' — the voice/vibration behavior for 'none' (silent like front, or a distinct cue) is UNDECIDED (CAVEAT, decide at implementation time). ⚠ EXTREME-CAUTION: `feedback_service.dart` is safety-critical code hardened across 6 review rounds this session to remove race conditions — modify with extreme care, MINIMAL change only. (5) STRINGS: `crosswalk_app/lib/localization/app_strings.dart` needs new ko/en label(s) for the 'none' state. CAVEAT — KNOWN LIMITATION (do NOT force re-capture in this task): the new front images show the same near-vertical downward framing issue already flagged in T35 (inconsistent with the confirmed chest-mount facing-forward posture); like T35/T1, record this only as a known limitation, do NOT mandate re-shooting here. DONE (2026-07-19): implemented on branch `feat/T42-four-class-model`, merged to master via PR #48 (squash-merge, master `f0865bd..6790324`; 1 CI run failed first — `classifier_test.dart`'s threshold-reachability test used a logit skew (1.0) that no longer cleared the unchanged 0.55 deviation threshold once softmax denominator grew from 3 terms to 4 (dilution), fixed by raising the test's logit to 2.0 with an explanatory comment — a genuine test bug, not a product bug), synced into develop (commit `f5f2aa2`; reported `git diff origin/master origin/develop --quiet` exit code 0 = no difference). WHAT LANDED against the 5 acceptance sub-parts: (1) RETRAIN SATISFIED — `train/train_model.py` CLASSES extended to `["front","left","right","none"]`, retrained on `image/` (front=269/left=55/right=32/none=36, T42's user-confirmed dataset), best checkpoint saved at finetune epoch 4 (val_acc=0.9730); `train/export_onnx.py` (mobile-compatible legacy exporter, NUM_CLASSES=4) re-exported to `model/crosswalk_model.onnx`, independently verified via `onnx.load()`: IR version 7 (≤9 mobile limit), opset 12, output shape [1,4] — copied into `crosswalk_app/assets/model/crosswalk_model.onnx` (+ regenerated `.sha256`). Combined with T1's still-open 3-class retrain as instructed (one retrain, not two). (2) CLASSIFIER SATISFIED — `classifier.dart:_labels` now `['front','left','none','right']` (verified to match torchvision `ImageFolder`'s alphabetical class_to_idx ordering — reviewer independently cross-checked against `train_model.py`'s actual `ImageFolder` usage, confirmed correct, flagged as the single highest-risk area). Thresholds re-derived from evidence, not guessed: `_frontThreshold` 0.85→0.65, new `_noneThreshold`=0.50, `_deviationThreshold` unchanged at 0.55 — chosen by dumping actual per-image softmax probabilities from the retrained ONNX (`train/eval_model.py` + an ad-hoc diagnostic script) and picking values that recovered under-confident-but-correct predictions without needing to guess. (3) UI SATISFIED — `camera_screen.dart` adds `_colorNone`(#8A94A6)/`Icons.search_off`/`labelNone`; T41's guidance corridor and peripheral vignette are explicitly hidden when `_guidanceLabel=='none'` (`_showGuidance` and the vignette's `if` guard both gated), preserving T41's honesty constraint (no guidance symbol implying a crosswalk that isn't there) — reviewer traced this end-to-end and confirmed `_guidanceLabel` only changes from real classification results. (4) FEEDBACK SATISFIED, MINIMAL CHANGE HONORED — `feedback_service.dart`'s `decideMessage()` diff is exactly 1 line (`if (detectedClass == 'front') return null;` → `if (detectedClass == 'front' || detectedClass == 'none') return null;`); reviewer verified this early-return sits above all cooldown/generation-guard state and cannot interact with the vibration-timer/speech-generation races hardened earlier this session. (5) STRINGS SATISFIED — `app_strings.dart` `labelNone` added ko:'횡단보도 없음' / en:'No crosswalk detected'. TESTS: `classifier_test.dart` updated (4-element logits throughout, new none-threshold test, the CI-caught softmax-dilution fix above) + `feedback_service_test.dart` new none-silence test (silent even immediately after an active 'left' alert). `flutter analyze` re-run locally multiple times: same 4 known pre-existing issues (baseline dropped from 6→4 as part of T41), zero new. CI: PASSED on 2nd run — GitHub Actions run 29647790771, "Flutter 테스트" step logged "63 tests passed". ⚠ SAFETY FINDING — RECALL TARGET NOT MET (recorded honestly, not softened): re-measuring the retrained 4-class model against its actual shipped thresholds (`python train/eval_model.py`, no env overrides) gives front recall 78.6%(22/28), **left recall 50.0%(3/6) — FAILS the T1 ≥90% target**, **right recall 50.0%(2/4) — FAILS**, none recall 40.0%(2/5). Root-caused (not assumed): PyTorch raw argmax accuracy on the same retrained model was actually good (left 100%, right 100%, macro F1 0.87) — the shortfall is softmax-probability dilution from going 3→4 output classes, confirmed by inspecting individual image confidences; lowering thresholds (tested: front 0.65, none 0.50 as now shipped) recovered under-confident-but-correct front/none predictions but did NOT move left/right recall at all, because those 4 misses are genuine wrong-class predictions (e.g. two true-right images classified "front" at conf≈0.70, well above any reasonable threshold) — a data/model problem, not a threshold problem. Sample sizes are tiny (test: left n=6, right n=4, none n=5) and statistically weak. ESTIMATED DATA NEED (reasoned estimate, not a hard spec): to reach a test-set size that's even loosely statistically meaningful for a ~90% recall target (±10pp margin, 95% CI, n≈35 per class in test alone under an 80/10/10 split ⇒ ≈350 total per class), left/right/none each need roughly **6-10x their current total image count** (left 55→~350, right 32→~350, none 36→~350); tighter ±5pp confidence would need ~1400 per class. Also recommend diversifying capture conditions (angle/lighting/distance), not just repeating similar shots, since the current wrong-class errors look like genuine feature confusion rather than pure sample-size starvation. NOT YET DONE (explicitly out of this task's scope, follow-up needed): no additional data has been collected; left/right recall remains below the T1 target even after this retrain — T1 itself should stay `in-progress`, not be closed by this. KNOWN LIMITATION: no automated widget test exercises the 'none'-hides-corridor UI behavior end-to-end (same class of gap as T41's — `_onFrame`'s classifier pipeline can't be driven synthetically in the current test infra); verified only by code-tracing (both by implementer and independently by reviewer). | done |
| T43 | `train/train_model.py` 내장 ONNX 익스포터가 모바일 비호환 모델을 생성하는 함정 제거 | P2 | — (독립) | BACKGROUND (2026-08-02 실측 확인): `train/train_model.py`의 `export_onnx()`(파일 하단, `torch.onnx.export(..., opset_version=17)`)는 최신 torch에서 opset 17 변환에 실패하고 **IR 10 / opset 18** 모델을 `model/crosswalk_model.onnx`에 저장한다. onnxruntime 모바일이 지원하는 IR 상한은 9이므로 이 모델을 앱 asset에 넣으면 로딩이 실패한다. 이 프로젝트의 정식 경로는 별도 스크립트 `train/export_onnx.py`(레거시 TorchScript 익스포터, `dynamo=False`, opset 12 → IR 7)이며, 과거에는 `train_model.py`의 이 단계가 콘솔 인코딩(cp949) 크래시로 죽어서 문제가 드러나지 않았다. 2026-08-02 재학습 시 `PYTHONIOENCODING=utf-8`로 실행하자 익스포터가 실제로 성공해 비호환 모델을 덮어썼고, `export_onnx.py`로 재출력해 수동 복구했다(IR 7/opset 12/출력 [1,4] 확인). 위험: 다음에 누군가 `train_model.py`만 실행하고 산출물을 asset에 복사하면 앱이 조용히 모델 로딩에 실패한다. ACCEPTANCE: (1) `train_model.py`의 ONNX 변환 단계를 제거하거나, `export_onnx.py`와 동일한 레거시 익스포터 설정(opset 12, `dynamo=False`)으로 일치시킬 것 — 둘 중 어느 쪽으로 갈지는 구현 시점 판단(OPEN); (2) 어느 쪽이든 산출된 ONNX의 IR version이 9 이하인지 검증하는 단계를 추가할 것; (3) 어떤 스크립트가 앱 배포용 정식 산출 경로인지 코드 주석 또는 `docs/`에 명시할 것. Verify per docs/DefinitionOfDone.md. | todo |
| T44 | right 클래스 데이터 보강 — 노란색(어린이보호구역) 횡단보도·근접/기울어진 프레임 우선 수집 | P1 | T1 (in-progress — right recall 81.8%로 ≥90% 목표 미달 상태이며, 이 태스크가 그 미달을 해소하기 위한 데이터 수집 작업) | BACKGROUND (2026-08-02 실측 진단): 확장 데이터셋(751장) 재학습 후 `python train/eval_model.py` 기준 right recall은 81.8%(9/11)로 T1 목표(≥90%) 미달. **미달 원인을 개별 이미지 단위로 진단한 결과, 신규 데이터 부족이 아니었음**: right 테스트셋 11장 중 2026-07 이후 신규 촬영분 8장은 **전부 정탐**(확신도 0.774~1.000)이었고, 미탐 2건은 모두 기존(구) 데이터였으며 모델이 둘 다 'left'로 판단함(확신도 0.443 / 0.494 — 임계값 0.55 미달로 최종 무판정 처리). 두 미탐 이미지를 직접 열어 확인한 결과 **라벨은 정확했고**, 각각 (a) `right (11).jpg` = 흰색 횡단보도 가장자리·정방향 촬영, (b) `right (2675).jpg` = **노란색 어린이보호구역 횡단보도**를 근접·기울여 촬영한 프레임이었음. 색상 비율 기반 추정으로 노란 횡단보도 커버리지를 실측한 결과 right 11/110장(약 10%), left 13/114, front 27/320으로 전 클래스에서 희소하며, right 테스트셋에 포함된 노란 횡단보도 1장이 그대로 실패함. 검증했으나 원인이 아니었던 가설: 신규/기존 이미지의 EXIF 회전 불일치를 의심했으나 실측 결과 신규·기존 모두 orientation=6으로 일치(front 1장만 orientation=1 예외). ACCEPTANCE: (1) 수집 우선순위를 아래 순서로 지킬 것 — ①노란색 어린이보호구역 횡단보도의 우측 이탈 장면(현재 right 클래스 내 11장뿐), ②근접·기울어진·흔들린 프레임(미탐 2건 모두 이 특성이며 실제 보행 중 흔한 조건), ③흰색 횡단보도 우측 이탈 일반(이미 확신도 0.99+로 잘 맞추므로 **우선순위 최하**). (2) 수량 목표: 통계적으로 의미 있는 판정(±10%p, 95% CI, 테스트셋 n≈35)을 위해 right 총 ~350장이 필요하며 현재 110장으로 약 240장 부족 — 단, 위 우선순위대로 채우면 350장 도달 전에도 목표를 넘길 수 있으므로 **중간에 재학습·재평가하여 확인할 것**. (3) 수집 후 `train/train_model.py` 재학습 → `train/export_onnx.py` 재출력(⚠️ T43 참고: `train_model.py` 내장 익스포터는 모바일 비호환 모델을 생성하므로 반드시 `export_onnx.py`로 재출력) → `train/eval_model.py` 재평가 순서로 진행하고, right recall을 재측정해 기록할 것. CAVEAT (do NOT overstate): 위 원인 진단은 테스트셋 11장이라는 매우 작은 표본에 기반하므로, 노란 횡단보도·근접 프레임이 유일한 원인이라고 단정할 수 없음 — 데이터 보강 후 재측정에서 다른 실패 패턴이 드러날 수 있음. 또한 left recall 100%(12/12) 역시 n=12 기준이므로 이 작업으로 데이터가 늘면 left 수치도 함께 변동할 수 있음. **데이터 요구량 정량화 + 실패 유형 재분류 (2026-08-21):** 위 BACKGROUND/ACCEPTANCE(2026-08-02)는 right 데이터 수집 우선순위만 담고 있었고, 2026-08-21 재측정(T1 "REMEASURED (2026-08-21, 843장 데이터셋)" 참조)으로 다음이 구체화됨 — 아래 항목은 기존 우선순위를 **대체하지 말고 병기**할 것. **(1) 필요 수량(통계적 근거):** recall 90%를 95% 신뢰구간 ±10%로 측정하려면 클래스당 test 표본 n≈35 필요(n ≈ 1.96²×0.9×0.1/0.1²). 현재 분할이 80/10/10이므로 클래스당 총 ~350장 필요 — **left 127장 → +223장, right 155장 → +195장 추가 필요.** 현재 n=14/16으로는 이미지 1~2장 배정 변화가 recall을 7~13%p 흔들어 목표 달성 여부를 판정할 수 없음. **(2) 수집 우선순위(실측 실패 사례 기반):** ①**야간 + 마모된 횡단보도** — 실패 2건(`20260803_203917.jpg` right 미검출 / `20260808_000211.jpg` front→right 오경보). T37로 손전등을 출시했으므로 야간은 예상 사용 시나리오임. ②**근접 프레임 — 수집 대상 아님, 오히려 제거 검토 대상 (2026-08-21 사용자 확정으로 정정됨)**: right 미검출 4건 중 2건(`20260709_203021.jpg`, `right (559).jpg`)이 줄무늬만 화면을 채워 소실점·도로 경계가 안 보이는, 거의 수직 하방 촬영 프레임임. **최초 기록 시 "가슴 장착이므로 근접 프레임이 정상 동작 조건"이라 적었으나 이는 오판이었음** — 2026-08-21 사용자가 실제 사용 각도를 "정면에서 살짝 아래"로 확정했고, 이는 이미 출시된 온보딩 안내 문구(`crosswalk_app/lib/localization/app_strings.dart:279` — '가슴 정면에 가깝게, 살짝 아래를 향하도록')와도 일치함. 즉 수평에 가까운 시야로 소실점·도로 경계가 보이는 프레임이 정상 조건이며, 위 2건 같은 수직 하방 근접 프레임은 **실사용에서 발생하지 않는 사양 외(off-spec) 데이터**임. 따라서 이 유형은 추가 수집하지 말 것이며, 학습셋에 남아 있는 동종 이미지는 오히려 제거 후 재측정해 영향을 확인할 것. T35(자세 기준 전처리 재검토)와 연관. ③**진입 시점 / 비스듬한 각도** — front→편향 오경보 2건(`20251119_161750_001/015`)과 right 미검출 1건(`20260722_133703.jpg`)이 횡단보도 가장자리에서 줄무늬가 비스듬히 뻗는 프레임. **(3) 라벨 기준 — RESOLVED (2026-08-21 사용자 확정):** 제기된 경계 사례 `20251119_131206_041_saved.jpg`(횡단보도가 프레임 왼쪽 가장자리에만 걸치고 나머지는 빈 아스팔트)는 **`front`가 맞음**이 확정됨. 즉 라벨은 "횡단보도가 프레임 어디에 보이는가"가 아니라 **보행자의 진행 방향이 횡단보도 축에서 벗어났는가**를 나타냄. 함께 확정된 7건 전수(아래)에서 **기존 라벨과 사용자 확정 라벨이 100% 일치** — 즉 이 실패들의 원인은 라벨 오류가 아니라 모델의 분류 능력 문제임: `20260803_203917.jpg`=right(현 라벨 right), `20260808_000211.jpg`=front(현 front), `20251119_161750_001_saved.jpg`=front(현 front), `20251119_161750_015_saved.jpg`=front(현 front), `20260722_133703.jpg`=right(현 right), `20251119_131206_041_saved.jpg`=front(현 front). 별도 Open Question 등록 불필요. **(4) 관찰된 데이터 편중 (추정, 확인 필요):** front 332장 중 245장(74%)이 파일명에 `_saved`가 붙은 2025-11-19~20 단일 촬영 세션에서 나옴. left/right/none에는 이 배치가 0장. 해상도는 전 클래스 동일(4000×3000)이라 명백한 지름길 특징은 확인되지 않았으나, front 오답 8건 중 7건이 이 배치에서 나옴. **이것이 데이터 편중 때문인지 그 세션이 단순히 어려운 장면이었는지는 현재 데이터로 구분 불가 — 추정임.** 확인 방법: `_saved` 배치를 제외하고 재학습해 front 오경보율 변화를 비교. **⛔ 데이터 요구량 재산정 (2026-08-22) — "장수" 기준 폐기, "세션 다양성" 기준으로 대체:** **이전 기록(2026-08-21)의 "left +223장 / right +195장"은 과대 추정이자 잘못된 단위였음. 폐기할 것.** 이유: 그 계산은 이미지가 서로 독립이라고 가정하고 클래스당 test n≈35를 만들려던 것이었으나, 실제로는 같은 세션 이미지들이 강하게 상관되어 있어 장수를 늘려도 유효 표본이 그만큼 늘지 않음(근거: T1 "데이터 누수 발견 및 누수 없는 재측정 (2026-08-22)" 블록). **현재 세션 구조 (간격 60초 기준) —** 총 843장 → 147개 세션; front 62개 세션 / left 60개 / right 66개 / none 70개 세션에 분포(세션은 여러 클래스를 포함할 수 있음); 촬영 기간 2025-11-19 ~ 2026-08-19, 23일이며 날짜당 평균 약 6개 세션; 특정 날짜 편중 — 2025-11-19 279장(33%), 2026-07-31 178장(21%). **새 기준: 한 장소에서 여러 장 찍는 것보다 서로 다른 장소를 여러 곳 찍는 것이 훨씬 값짐.** 한 횡단보도에서 20장 연사 → 세션 약 1개 증가(유효 표본 거의 안 늘어남); 서로 다른 횡단보도 7곳에서 3장씩 → 세션 7개 증가. 신뢰구간 폭은 대략 세션 수의 제곱근에 반비례하므로 구간을 절반으로 좁히려면 세션이 약 4배 필요함 — right 기준 66개 → 약 260개. 이는 큰 부담이므로, 현실적으로는 **목표 판정을 위한 무한정 수집보다 아래 우선순위 유형을 채우는 데 집중**하는 편이 나음. 수집 우선순위는 2026-08-21 기록의 ①야간+마모 ②정면에서 살짝 아래 각도의 진입/비스듬한 프레임을 그대로 유지하되, **각 유형을 서로 다른 장소·다른 날에 나눠 촬영할 것.** 같은 장소에서 조건만 바꿔 대량 촬영하는 것은 효과가 낮음. **추가 권고 —** 앱의 프레임 자동 저장(`_saved`) 방식으로 수집하면 근사 중복이 대량 생성됨(그 배치의 40.4%가 중복 보유). 수동 촬영이 데이터 품질 면에서 더 나음. | todo |
| T45 | 설정 화면에 빌드 식별자(커밋 SHA) 표시 — 설치된 APK가 어느 빌드인지 기기에서 확인 가능하게 | P2 | — (독립) | BACKGROUND (2026-08-02 확인): `crosswalk_app/pubspec.yaml`의 `version: 1.0.0+1`은 초기 구현 이후 한 번도 변경된 적이 없어(버전 변경 커밋 이력 3건 모두 초기 개발분) 모든 CI 빌드가 동일 버전으로 표시되며, 그 결과 기기에 설치된 APK가 어느 빌드인지 폰에서 확인할 방법이 전혀 없다 — Android 설정 > 앱 정보에는 항상 `1.0.0`으로만 보이고 앱 화면 어디에도 빌드 정보가 없다. GitHub 쪽에서는 Actions 아티팩트 이름이 `crosswalk-app-${{ github.sha }}` 형식(`.github/workflows/build_apk.yml:100`)이라 커밋 SHA로 빌드를 구분할 수 있으나 그 정보가 앱 안으로 전달되지 않는다. 실제로 이 문제 때문에 사용자가 "지금 깔린 게 재학습 모델 버전인지" 확인할 수 없는 상황이 발생했다. ACCEPTANCE: (1) CI 빌드 시 커밋 SHA를 앱에 주입할 것 — `.github/workflows/build_apk.yml`의 `flutter build apk --release` 단계에 `--dart-define`으로 `github.sha`를 전달하는 방식을 기본 후보로 하되 최종 방식은 구현 시 판단(OPEN). (2) Dart 코드에서 `String.fromEnvironment(...)`로 읽고, 로컬 빌드처럼 값이 주입되지 않은 경우의 기본값(예: `dev`)을 반드시 정의해 빈 화면이 되지 않게 할 것. (3) `crosswalk_app/lib/screens/settings_screen.dart` 하단에 빌드 식별자를 표시할 것 — 표시 형식(전체 40자 SHA vs 앞 7자 축약)은 구현 시 판단(OPEN). (4) 이 앱은 시각장애인 대상이므로 해당 표시에 `Semantics` 라벨을 적절히 부여하거나, 스크린리더가 SHA를 한 글자씩 읽어 소음이 되지 않도록 처리 방침을 정할 것(OPEN — 화면 표시 전용으로 두고 `excludeSemantics` 처리하는 선택지도 포함). (5) `crosswalk_app/lib/localization/app_strings.dart`에 필요한 ko/en 라벨을 추가할 것. CAVEAT (do NOT overstate): 이 태스크는 표시만 다루며, `pubspec.yaml`의 versionCode/versionName 자동 증가는 별개 사안이다 — 동일 versionCode로 인해 기존 앱 위에 덮어 설치가 거부될 수 있는 문제는 이 태스크로 해결되지 않으며, 필요하면 별도 태스크로 등록해야 한다. | todo |
| T46 | 설정 화면 언어 선택 버튼 대비 미달 수정 | P1 | — | BACKGROUND (2026-08-02 실측): `crosswalk_app/lib/screens/settings_screen.dart:139-140`의 `SegmentedButton`이 accent 파랑(`_colorAccent` #3AA0FF) 배경 위에 `selectedForegroundColor: Colors.white`를 쓰는데, WCAG 명도비를 직접 계산한 결과 2.74:1로 **일반 텍스트 기준(4.5:1) 미달**이다. 동일한 문제를 `camera_screen.dart:532`와 `onboarding_screen.dart:327-329`에서는 이미 어두운 남색 `_colorAccentOnText`(#08182A, 대비 확인 시 통과)로 수정해뒀는데, 언어 선택 버튼만 그 수정이 누락되어 있다. 저시력 사용자가 현재 선택된 언어를 읽기 어려울 수 있다. ACCEPTANCE: (1) `settings_screen.dart`의 `selectedForegroundColor`를 흰색 대신 `camera_screen.dart`의 `_colorAccentOnText`(#08182A)와 동일한 색으로 교체할 것; (2) 교체 후 실제 WCAG 명도비를 계산해(온라인 계산기 또는 스크립트) 4.5:1 이상인지 근거로 남길 것 — 색을 바꿨다는 사실만으로 통과 처리하지 말 것; (3) 다른 SegmentedButton/버튼 스타일(예: 언박스 상태)이 이 변경으로 부수적으로 나빠지지 않는지 확인할 것. Verify per docs/DefinitionOfDone.md. DONE (2026-08-02): `settings_screen.dart`에 `_colorAccentOnText`(#08182A, camera_screen.dart와 동일 값) 상수를 추가하고 `selectedForegroundColor: Colors.white` → `_colorAccentOnText`로 교체. 명도비 재계산: 2.74:1 → **6.52:1**(4.5:1 기준 충족). 다른 `activeColor` 사용처(torch/TTS-rate/vibration 슬라이더, 4곳)는 텍스트가 아닌 Switch/Slider 트랙 색이라 영향 없음을 확인. `flutter analyze` 재실행: 3 issues, 전부 기존 파일의 무관한 기존 이슈, 신규 0건. | done |
| T47 | 화면 파일에 흩어진 디자인 토큰(색상 상수)을 공유 테마 파일로 통합 | P2 | T46 (완료 후 진행 — 토큰 통합 시 T46에서 고친 값을 반영해야 하므로 순서상 T46이 먼저) | BACKGROUND (2026-08-02 실측): `_colorAccent = Color(0xFF3AA0FF)`가 `camera_screen.dart`/`onboarding_screen.dart`/`settings_screen.dart` 3개 파일에 **각각 따로** 정의되어 있고, 대비 보정용 `_colorAccentOnText`(#08182A)는 `camera_screen.dart`에만 존재한다. 공유 테마 파일이 없어 색상 하나를 바꾸려면 3곳을 찾아 고쳐야 하고, 실제로 이 구조 때문에 T46의 대비 누락 사고가 발생했다(한 파일에서만 수정하고 다른 파일에는 반영을 잊음). ACCEPTANCE: (1) 새 파일(예: `crosswalk_app/lib/theme/app_colors.dart` — 구체적 경로/이름은 구현 시 판단, OPEN)을 만들어 `_colorFront`/`_colorLeft`/`_colorRight`/`_colorNone`/`_colorAccent`/`_colorAccentOnText`/`_colorWarning` 등 현재 각 화면에 중복 정의된 색상 상수를 한 곳으로 모을 것; (2) `camera_screen.dart`/`onboarding_screen.dart`/`settings_screen.dart`가 그 공유 상수를 참조하도록 갱신하고, 기존 로컬 중복 정의는 제거할 것; (3) 이 작업은 순수 리팩터링이므로 화면에 보이는 색상/동작이 하나도 바뀌면 안 된다 — 리팩터링 전후로 각 색상 hex 값이 동일한지 diff로 확인할 것; (4) `flutter analyze` 신규 이슈 0건, 기존 테스트 전부 통과(CI 로그로 확인). CAVEAT: T41의 `GuidanceCorridorPainter`처럼 애니메이션 색상 보간(`ColorTween`)에 쓰이는 상수는 통합 시 타입/보간 동작이 바뀌지 않는지 특히 주의할 것. | todo |
| T48 | 카메라 화면 유리질감(glass) HUD 텍스트의 실외 가독성 검증 및 필요시 보강 | P2 | — (독립. 단, ACCEPTANCE에 실기기 테스트가 선행 조건임을 명시) | BACKGROUND (2026-08-02 실측 및 한계): T41에서 하단 상태 HUD를 `Colors.black.withValues(alpha: 0.38)` 반투명 유리질감으로 만들었다(camera_screen.dart:755 부근). 검정 배경을 가정한 WCAG 명도비 계산으로는 모든 상태색이 충분한 대비를 보였으나(front 9.26:1, left 6.88:1, right 10.29:1, none 6.87:1, accent 7.65:1, warning 14.56:1 — 전부 3:1/4.5:1 기준 통과), **이는 배경이 순수 검정일 때의 값이며 실제로는 카메라 프리뷰(밝은 노면, 직사광선 등)가 그 뒤에 비친다.** 반투명도 0.38에서 밝은 배경이 얼마나 대비를 깎아먹는지는 코드 분석으로 확정할 수 없다. 텍스트 그림자·외곽선 등 가독성 보조 장치는 현재 0건(`shadows:` 검색 결과 없음). ACCEPTANCE: (1) **코드 수정에 앞서 실기기로 맑은 날 야외에서(밝은 콘크리트/아스팔트 노면 위) 하단 HUD 텍스트 가독성을 실측할 것** — 이 검증 없이 코드부터 고치지 말 것(현재 이 프로젝트는 로컬 flutter build/run이 검증된 적 없음, CLAUDE.md 기록 참고 — 검증 방법 자체도 이 태스크에서 결정 필요, OPEN); (2) 가독성 문제가 실측으로 확인되면 보강 방식을 선택 — 후보: HUD 배경 불투명도 상향, 텍스트에 그림자/외곽선 추가, 반투명도를 상황(주간/야간)에 따라 동적으로 조절 등, 최종 방식은 실측 결과를 보고 판단(OPEN); (3) 문제가 확인되지 않으면 이 태스크는 "실측 결과 문제없음"으로 근거와 함께 닫을 것 — 코드 변경을 위한 코드 변경을 하지 말 것. CAVEAT: 이 태스크는 실기기 접근이 필요해 이 개발 세션에서 곧바로 완결하기 어려울 수 있다. | todo |

## Rules
- One task = one implementer invocation. Keep tasks small.
- A task is `done` only when docs/DefinitionOfDone.md is satisfied.
- ⛔BLOCKED tasks require the referenced Open Question answered before starting.
