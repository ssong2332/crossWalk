# Tasks — crosswalk_app

Owner: planner (see AGENTS.md). Status values: `todo` / `in-progress` / `review` / `done`.
Derived from gaps found in code inspection (2026-07-16). Each row cites the motivating evidence.
Tasks marked ⛔BLOCKED depend on an Open Question in `docs/PRD.md` and must not start until answered.

## A. Blocked on decisions (resolve Open Questions first)
| ID | Task | Priority | Depends on | Acceptance Criteria | Status |
|---|---|---|---|---|---|
| T1 | Define & document model accuracy target (recall for left/right) | P0 | ⛔Open Q #3 | Numeric target recorded in PRD; measurable on test set | todo |
| T2 | Confirm target platforms & min OS; align CI accordingly | P0 | ⛔Open Q #1,#2 | Platforms/min OS stated; CI matches | todo |
| T3 | Decide accessibility standard & screen-reader requirement | P1 | ⛔Open Q #5 | Standard chosen; conformance checklist created | todo |
| T4 | Decide multi-language scope | P2 | ⛔Open Q #6 | Language list fixed | todo |
| T5 | Decide in-app safety disclaimer requirement | P1 | ⛔Open Q #9 | Yes/no + copy if yes | todo |
| T6 | Decide low-light/adverse-condition scope for v1 | P2 | ⛔Open Q #12 | In/out of scope recorded | todo |

## B. Ready — safety & correctness (no decision needed)
| ID | Task | Priority | Depends on | Acceptance Criteria | Status |
|---|---|---|---|---|---|
| T7 | Populate real SHA-256 for bundled model; enable integrity verification | P0 | Open Q #10 answered | `crosswalk_model.onnx.sha256` holds real 64-hex hash; app rejects tampered model. Evidence gap: `assets/model/crosswalk_model.onnx.sha256:1` = `placeholder_hash` | todo |
| T8 | Confirm committed `crosswalk_model.onnx` is the real trained model (not dummy) | P0 | Open Q #10 | Verified match with `model/crosswalk_model.onnx` | todo |
| T9 | Add unit tests for `Classifier` (smoothing, thresholds, throttle) | P1 | — | Tests exist under `crosswalk_app/test/` and pass. Gap: no test dir. NOTE: T9 is now also a prerequisite for closing T21 — the Classifier unit tests should include a case exercising the corrected softmax path (verify probabilities sum to ~1.0 and thresholds 0.55/0.85 are reachable) given T21's fix. DONE: tests added in commit `eb688a7` (`crosswalk_app/test/classifier_test.dart`, 7 tests via behavior-preserving extract-method refactor exposing `softmax`/`decideFromLogits`/`shouldProcessFrame` as `@visibleForTesting`). CI-verified: GitHub Actions run 29465632108 (PR #2, branch `feat/T9-classifier-tests`) — `flutter test` passed 7/7 covering softmax (sums to ~1.0, numerical stability, thresholds 0.55/0.85 reachable), decideFromLogits smoothing (5-frame sliding window + transition blend), threshold gating (returns null below threshold), and shouldProcessFrame throttle; subsequent APK build/sign/upload steps also passed. Smoothing/threshold/throttle behavior now covered. | done |
| T10 | Add unit tests for `FeedbackService` (cooldown, class-change, front-silence) | P1 | — | Tests cover `feedback_service.dart:17-39` | todo |
| T11 | Add widget test for `CameraScreen` error/retry states | P2 | — | Covers `_hasError` overlay + retry (`camera_screen.dart:202-278`) | todo |
| T21 | Fix CONFIRMED logits-vs-probability-threshold defect (release blocker, highest priority — above T1/T2/T7/T8) | P0 | T9 | CONFIRMED DEFECT (reviewer loaded shipped `assets/model/crosswalk_model.onnx` via onnx/onnxruntime): graph's final ops are `['Flatten','Gemm','HardSigmoid','Mul','Gemm']` — no Softmax node; real inputs produce outputs like `[-0.0838, 0.4418, -0.4263]` (negative, not summing to 1) = raw logits, not probabilities. `train/train_model.py:105-109` builds plain `nn.Linear` final layer, trains with `CrossEntropyLoss` (expects logits); `export_onnx` (~L218-227) exports as-is with no softmax. `classifier.dart:107-108` compares this raw logit against thresholds 0.55 (deviation) / 0.85 (front) (defined `classifier.dart:27-28`) as if probabilities. Safety-critical deviation alert may rarely/never fire in field = false-negative safety bug (not cosmetic). Fix: add softmax at export OR re-derive logit-scale thresholds from validation data, then re-validate detection rate. Acceptance: activation/threshold semantics made consistent and detection rate re-validated on test set. UPDATE: the semantic fix (softmax applied in `classifier.dart`, commit `33786d7`) has landed and passed independent reviewer re-verification (PASS — correct variable/timing/numerical stability confirmed), but empirical detection-rate re-validation against a labeled test set (via T9) is still required before this task can move to `done`. DONE: unit-test-level re-validation satisfied via T9 — CI run 29465632108 (PR #2) ran `flutter test` and passed 7/7, including the two tests specifically validating this fix: `softmax produces probabilities summing to ~1.0 for T21 example logits` and `softmax thresholds 0.55/0.85 are reachable given a sufficiently skewed logit vector`. NOTE: this is unit/synthetic-level validation only — it is NOT field/real-camera accuracy validation on a labeled real-world test set (that concern remains open under T1/T12 and is not covered here). | done |
| T23 | Add re-entrancy guard to `_initCamera()` (concurrency bug, safety-adjacent) | P0 | — | `camera_screen.dart:52-130` is async with no `_isInitializing` flag; can be re-invoked mid-flight from `didChangeAppLifecycleState` (`camera_screen.dart:155-156`) or retry button (`camera_screen.dart:265`) while a prior call is still awaiting `CameraController.initialize()`/`Classifier.init()`. Rapid resume/pause cycling or a double-tap on retry can create two concurrent `CameraController`s/ONNX sessions racing to assign `_controller`, orphaning one controller's dispose (compounds T24). Acceptance: concurrent entry is blocked/serialized; no orphaned controller/session under rapid resume/retry | todo |
| T24 | Release `OrtSession`/`OrtEnv` native resources before re-init (memory leak) | P1 | — | `classifier.dart:37-43` `init()` unconditionally creates a new `OrtSession` without checking/releasing an existing `_session`. Re-entry paths: `camera_screen.dart:75` in `_initCamera()`, called on every app-resume lifecycle transition (`camera_screen.dart:155-156`) and retry button tap (`camera_screen.dart:265`). `Classifier.dispose()` (`classifier.dart:177-180`) only runs once at widget teardown. Repeated background/foreground cycles or retry taps leak native ONNX Runtime memory, risking OOM kill during a long walk. Acceptance: prior session/env released before re-init; no native memory growth across repeated resume/retry cycles | todo |
| T25 | Configure `flutter_tts` queue mode to flush/interrupt (stale safety cue risk) | P1 | — | `feedback_service.dart:11-15` sets language/rate/volume only, no `setQueueMode`/`awaitSpeakCompletion`; `alert()` (`feedback_service.dart:17-39`) calls `_tts.speak(message)` without interrupting prior speech. With Android default QUEUE_ADD (not QUEUE_FLUSH) and per-class-only cooldown (`feedback_service.dart:20-28`), a user oscillating near a decision boundary can hear a stale "move left" AFTER already correcting right. Acceptance: flush/interrupt semantics set explicitly and re-verified against the 3s cooldown design | todo |
| T26 | Reset `Classifier` smoothing state on re-init (stale probabilities after resume) | P2 | T23 | `_recentProbs`, `_frameCount` (`classifier.dart:34-35`) are instance fields on a single long-lived `Classifier` (`camera_screen.dart:18`); `init()` never clears them. Stale pre-pause probability vectors get averaged into the first ~5 throttled post-resume frames (smoothing window `classifier.dart:90-98`), delaying correct detection right after the user picks the phone back up mid-crossing. Acceptance: smoothing state cleared on init/re-init; no stale-vector contamination after resume/retry | todo |
| T27 | Offer `openAppSettings()` after permanently-denied camera permission (accessibility recovery) | P2 | — | `camera_screen.dart:62-72` shows/speaks "카메라 권한이 필요합니다. 설정에서 허용해주세요." but never calls permission_handler's `openAppSettings()`; retry button just re-calls `Permission.camera.request()`, a no-op once permanently denied on Android. Leaves a visually-impaired user with no actionable in-app recovery path. Acceptance: permanent-denial path offers/triggers `openAppSettings()` as recovery | todo |
| T28 | Make release Gradle signing fail loudly instead of silently falling back to debug keystore | P3 | — | `android/app/build.gradle.kts:33-39` `release { signingConfig = signingConfigs.getByName("debug") }` with `// TODO: Add your own signing config` still present; real signing only via manual CI step (`.github/workflows/build_apk.yml:60-91`, gated on `KEYSTORE_BASE64`). Running `flutter build apk --release` locally without CI yields a debug-signed APK. Acceptance: default config fails loudly outside CI rather than silently debug-signing | todo |

## C. Ready — performance
| ID | Task | Priority | Depends on | Acceptance Criteria | Status |
|---|---|---|---|---|---|
| T12 | Define performance targets (FPS, latency, battery, min device) | P1 | Open Q #11 | Targets in PRD | todo |
| T13 | Investigate moving YUV→RGB + inference off UI isolate | P2 | T12 | Measured frame-drop/latency improvement vs baseline. Gap: `_onFrame` runs sync on UI thread (`camera_screen.dart:132-148`) | todo |

## D. Documentation sync (docs agent)
| ID | Task | Priority | Depends on | Acceptance Criteria | Status |
|---|---|---|---|---|---|
| T14 | Fix `ARCHITECTURE.md` drift: throttle 10→5, threshold 0.70→0.85/0.55, init order, remove `export_onnx.py`, build command `--no-shrink`→`flutter build apk --release` | P1 | — | Doc matches code (see PRD "Documentation Drift"). Gap: `ARCHITECTURE.md` states `--no-shrink` but CI runs `flutter build apk --release` (`.github/workflows/build_apk.yml:58`) | todo |
| T15 | Fill `CLAUDE.md` project overview + verified build/test/run commands | P2 | — | Placeholders replaced with real values | todo |
| T22 | Trace/document actual ONNX export path from `train/` to shipped `crosswalk_model.onnx` | P1 | — | Gap: training script targets opset 17; git log only records "IR version 10→7 downgrade"; no `export_onnx.py` found in repo. Document (or restore) the real export script and its opset/IR settings so the model can be reproduced | todo |

## E. Deferred / decision-gated features
| ID | Task | Priority | Depends on | Acceptance Criteria | Status |
|---|---|---|---|---|---|
| T16 | Native Semantics / screen-reader compatibility | P2 | T3 | Meets chosen standard | todo |
| T17 | Multi-language TTS | P3 | T4 | Languages selectable/auto | todo |
| T18 | Optional periodic "on-track" reassurance feedback | P3 | Open Q #13 | Configurable behavior | todo |
| T19 | Onboarding / phone-posture guidance screen | P3 | Open Q #8,#14 | Guidance flow present | todo |
| T20 | Remove or repurpose unused `crosswalk_app_scaffold/` | P3 | — | Dir removed or documented. Evidence: default counter app (`crosswalk_app_scaffold/lib/main.dart`) | todo |
| T29 | Fix or explicitly document-as-dead the dormant BGRA (iOS) preprocessing buffer-offset bug before any iOS work | P2 | Open Q #1 (PRD); Architecture Open Q C | `classifier.dart:119-125` passes `image.planes[0].bytes.buffer` (whole underlying ByteBuffer) instead of a properly offset view — misaligns pixels if `offsetInBytes` ≠ 0 or row padding ≠ width*4. Currently unreachable (camera forced to yuv420 at `camera_screen.dart:99`, CI Android-only) but must be fixed or documented dead before iOS work starts. Acceptance: path fixed with offset-correct view, or explicitly marked dead in code + Architecture Open Q C | todo |

## Rules
- One task = one implementer invocation. Keep tasks small.
- A task is `done` only when docs/DefinitionOfDone.md is satisfied.
- ⛔BLOCKED tasks require the referenced Open Question answered before starting.
